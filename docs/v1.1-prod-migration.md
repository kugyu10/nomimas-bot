# v1.1 Twiplaポーリングcron — prod適用手順

対象マイグレーション: `supabase/migrations/20260825010000_add_twipla_scrape_polling_cron.sql`
（dev では夜間に適用・冪等性を確認済み。詳細: `docs/v1.1-polling-design.md` 付記、および夜間の報告）

prod project ref: `hgojtooexbknqotzkkja`（`scripts/setup-prod-vault.ts` 等の既存スクリプトと同じ値）

---

## 0. 適用前チェックリスト

本マイグレーションは **テーブルもデータも変更しない**（`cron.schedule` の登録のみ）。
それでも実行前に以下を確認する。特に1点目・2点目が欠けていると、cronジョブは登録されるが
発火した `net.http_post` が全て失敗し続ける（サイレントな機能不全）になるため必須。

- [ ] **`vault.decrypted_secrets` に `project_url` と `cron_function_key` が存在する**
      （既存の `confirm-broadcast-daily` が同じシークレットを使っているので、通常は
      `scripts/setup-prod-vault.ts` を過去に実行済みであれば入っている。確認クエリ:
      `select name from vault.decrypted_secrets where name in ('project_url','cron_function_key');`
      — 2行返ること）
- [ ] **`scraper` Edge Function が prod にデプロイ済み**で、`POST {"url": "..."}` に応答する
      （`supabase functions list --project-ref hgojtooexbknqotzkkja` で `scraper` が
      ACTIVE であることを確認。ゲートウェイのJWT検証が有効なままなので、
      `cron_function_key`（anonキー）が正しい値である必要がある）
- [ ] **prod の `events` に `event_date` が未来日のイベントが存在する**かどうかを事前に把握する
      （0件でもマイグレーション適用自体は安全だが、「本当に動いているか」を確認する対象が
      無いと初動の観測ができない。`select count(*) from public.events where event_date >= current_date;`）
- [ ] 既存の `confirm-broadcast-daily` ジョブに影響しないことを確認する
      （本マイグレーションは `jobname = 'twipla-scrape-poll'` のみを対象にした
      `do $$ ... $$` ブロック + `cron.schedule` であり、他ジョブ名には触れない —
      ファイル内容をレビューして確認する）

---

## 1. 適用: 1コマンド（推奨経路）

朝、本人が実行する前提。既存のリリース経路と同じ `supabase db push` を使う。

```
supabase db push --project-ref hgojtooexbknqotzkkja
```

- `supabase/migrations/` 配下の未適用マイグレーションを version 順に全て当てる。
  dev で先行適用した `20260825010000_add_twipla_scrape_polling_cron.sql` 以外に
  他の未適用ファイルが無いことを事前に `git log` / `ls supabase/migrations/` で確認しておくこと
  （このタスクでは本ファイル1つのみを想定している）。
- 実行後、2節の確認を行う。

### 1コマンドが使えない場合の代替（原則使わない）

`scripts/v11/apply-dev-migrations.ts` は **dev専用の安全弁**
（`scripts/db/sql.ts` の `connectDev()` が `DEV_PROJECT_REF !== 'cmsxvxtcdniqgvhxjqri'` で
即 `Deno.exit(1)` する）を持つ。このスクリプトを prod に向けて動かすことは想定しておらず、
安全弁を外して無理に使うことは **絶対にしない**。

`supabase db push` がどうしても使えない場合（CLI障害等）は、安全弁を外すのではなく、
`connectDev()` と同じ形の **prod専用の別関数**（`scripts/setup-prod-vault.ts` 等が使っている
`PROD_PROJECT_REF = "hgojtooexbknqotzkkja"` を検証するパターン）を新たに書き、
このマイグレーションSQLを1トランザクションで実行した上で
`supabase_migrations.schema_migrations` に version（`20260825010000`）を記録する、
という同じ手順を踏む。**dev用スクリプトの安全弁を書き換えて流用しない**
（レビューが無いまま prod 安全弁ごと消える事故を防ぐため、別ファイルとして新規に書く）。

---

## 2. 適用直後に見るべき観測点

1. **`cron.job` に登録されたか**
   ```sql
   select jobid, jobname, schedule, active
   from cron.job
   where jobname = 'twipla-scrape-poll';
   ```
   `schedule = '*/15 * * * *'`, `active = true` の1行が返ること。

2. **`cron.job_run_details`（発火して成功しているか）**
   ```sql
   select start_time, status, return_message
   from cron.job_run_details
   where jobid = (select jobid from cron.job where jobname = 'twipla-scrape-poll')
   order by start_time desc
   limit 10;
   ```
   `status = 'succeeded'` が続くか、最初の1〜2回で `failed` が出ていないかを見る。

3. **`net._http_response`（scraperへのPOSTが実際に届いて2xxを返しているか）**
   ```sql
   select id, status_code, timed_out, created
   from net._http_response
   order by id desc
   limit 20;
   ```
   `status_code = 200` が中心であること。`timed_out = true` や 4xx/5xx が続く場合は
   Vaultのシークレット不整合（0節1点目）か scraper 未デプロイ（0節2点目）を疑う。

4. **`participants.scraped_at` が更新されているか**
   ```sql
   select event_platform_url_id, max(scraped_at) as last_scraped
   from public.participants
   group by event_platform_url_id
   order by last_scraped desc
   limit 10;
   ```
   ポーリング対象イベントの `event_platform_url_id` について、直近15分以内の
   `last_scraped` が増えていくこと。

5. **`notification_logs` に `kind = 'scrape_changes'` が増えているか**
   ```sql
   select created_at, event_id, kind, recipients, sent, failed, skipped_no_line_id, detail
   from public.notification_logs
   where kind = 'scrape_changes'
   order by created_at desc
   limit 10;
   ```
   実際に参加者の増減・ステータス変化が起きたタイミングで行が増えること
   （変化が無いポーリングは通知をスキップする仕様なので、行が増えないポーリングがあっても異常ではない）。

---

## 3. ロールバック手順

```sql
select cron.unschedule('twipla-scrape-poll');
```

これだけで即時にジョブが停止する。

**安全である理由**:
- 本マイグレーションはテーブル定義・既存データを一切変更しない
  （`cron.schedule` の登録・`do $$ ... $$` による存在チェックのみ）。
- `cron.unschedule` は `cron.job` から該当ジョブの行を削除するだけで、
  `participants` / `notification_logs` 等のアプリケーションデータには触れない。
- 既存の `confirm-broadcast-daily`（jobname が異なる）には一切影響しない。
- ロールバック後に再度有効化したい場合は、同じマイグレーションSQL
  （`select cron.schedule('twipla-scrape-poll', ...)` の部分）を再実行すればよい
  （`schema_migrations` の version 行は残ったままでよく、削除不要 — cron.job の状態と
  マイグレーション台帳は独立している）。

---

## 4. prod特有の注意: 最初は `active = false` で入れる選択肢

このマイグレーションを適用すると、prodの実イベントに対して実際にTwiplaへのHTTPリクエストが
15分ごとに走り始める。様子を見ながら有効化したい場合は、適用直後に以下を実行して
一時的に無効化できる（ジョブ自体は登録されたまま、発火だけ止める）。

```sql
select cron.alter_job(
  (select jobid from cron.job where jobname = 'twipla-scrape-poll'),
  active := false
);
```

再度有効化する場合:

```sql
select cron.alter_job(
  (select jobid from cron.job where jobname = 'twipla-scrape-poll'),
  active := true
);
```

`cron.alter_job` は `jobid`（bigint）を第一引数に取るため、`jobname` から都度 `jobid` を
サブクエリで引く。`cron.unschedule` によるロールバック（3節）とは異なり、ジョブ定義自体は
消えないので、`cron.job` の行を見ればスケジュール内容はいつでも確認できる。

運用判断: 0節のチェックリスト（Vaultシークレット・scraperデプロイ済み）が全て確認できていれば
`active=false` を経由せずそのまま有効な状態で適用してよい。不安があれば
「適用 → `active=false` → 2節の観測点をSELECTだけで手動確認 → 問題なければ `active=true`」
の順で段階的に有効化する。

---

## 追記（2026-08-25 夜間の最終状態）— 対象マイグレーションは**4本**になった

この文書の本文は `20260825010000` だけを対象に書かれている。夜間の作業が進んだ結果、
**prod に当てる候補は4本**になった。適用は**必ず version の昇順**で行うこと。

| version | 内容 | prod で何が起きるか |
|---|---|---|
| `20260825010000` | Twipla 自動ポーリング cron（15分ごと） | **prod のイベントに対して実際に Twipla を叩き始める** |
| `20260825020000` | `notification_logs.kind` に `confirm_broadcast` を追加 | CHECK 制約の入れ替えのみ。データ不変 |
| `20260825030000` | 確認配信 catchup cron（当日参加者の取りこぼし対策） | 確認配信の起動が「1日1回」から「日中は30分おき」に増える |
| `20260825040000` | catchup の発火分を :15/:45 に固定（同一分衝突の解消） | `030000` と同じ最終状態にする冪等な再設定 |

### 適用（1コマンド）

```bash
supabase db push --project-ref <prod ref>
```

`supabase_migrations.schema_migrations` の台帳で未適用のものだけが順に当たる。

### 適用前チェックリスト（4本ぶん）

- [ ] prod の `vault.decrypted_secrets` に `project_url` / `cron_function_key` /
      **`cron_shared_secret`** の3本があること
      （`030000`/`040000` は `x-cron-key` ヘッダを使うため3本目が必須）
- [ ] prod に `scraper` と `message-sender` がデプロイ済みであること
- [ ] prod の `events` に未来日のイベントがあるか（無ければ `010000` は当面 `0 rows` で無害）
- [ ] **`notification_logs` に `confirm_broadcast` を書くのは `message-sender` の
      新しいコード。`020000` を当てても、`message-sender` を prod にデプロイしない限り
      その kind の行は増えない**（制約を広げるだけで無害）

### 二重送信についての注意（`030000` / `040000`）

確認配信を起動する cron が2本になる（`confirm-broadcast-daily` と
`confirm-broadcast-catchup`）。**同一分に発火しないことが二重送信を防ぐ前提**なので、
適用後に必ず次を確認すること:

```bash
set -a; source .env.local; set +a   # ※このスクリプトは dev 専用。prod では手で SELECT する
deno run --allow-net --allow-read --allow-env --config deno.json \
  scripts/v11/check-cron-no-collision.ts
```

`scripts/v11/check-cron-no-collision.ts` は **dev 専用の安全弁（`connectDev()`）を通す**ため
prod には使えない。prod では同等の SELECT を手で打つこと:

```sql
select jobid, jobname, schedule from cron.job
where active is true and command like '%/functions/v1/message-sender%';
```

返ってきた schedule が**同じ分に発火しない**ことを目で確認する
（`0 1 * * *` と `15,45 0-12 * * *` は衝突しない）。

**根本策は未適用**: message-sender 側の claim-then-send（排他）は入っていない。
1回の実行が15分以上かかると次の catchup と重なりうる。
詳細は `docs/v1.1-polling-design.md` の付記3。

### ロールバック（4本ぶん）

```sql
select cron.unschedule('twipla-scrape-poll');        -- 010000 を無効化
select cron.unschedule('confirm-broadcast-catchup'); -- 030000/040000 を無効化
```

`020000`（CHECK 制約）を戻す必要は通常ない（値を広げただけ）。戻すなら制約を
`('answer','completion','scrape_changes')` の3値に戻す。ただし `confirm_broadcast` の行が
既にあると制約違反で失敗するので、先にその行の扱いを決めること。

段階的にいくなら `cron.alter_job(<jobid>, active := false)` で入れてから、
様子を見て `active := true` にする。

---

## 適用実績

| 日時 | version | 結果 |
|---|---|---|
| 2026-08-25 07:20 JST | `20260825010000`（ポーリング cron） | 適用済み |
| 2026-08-25 07:20 JST | `20260825020000`（`kind` に `confirm_broadcast` 追加） | 適用済み |
| 2026-08-25 09:25 JST | `20260825030000`（catchup cron） | 適用済み |
| 2026-08-25 09:25 JST | `20260825040000`（catchup の衝突解消） | 適用済み |

適用時の prod の状態（事前チェックの実測）:

- Vault の3シークレット（`project_url` / `cron_function_key` / `cron_shared_secret`）すべて存在
- **ポーリング対象イベントは0件** — `twipla-scrape-poll` は当面 `'0 rows'` で
  Twipla を叩かない。直近イベントが窓（`greatest(confirm_days_before, 3)` 日以内）に
  入った時点で自動的に叩き始める
- `oa_members` は2行とも `line_user_id` が null（主催者通知は構造的に届かない状態のまま。
  `docs/v1.1-owner-notification-gap.md` 参照）

`030000` / `040000` を後から当てるときは、**`040000` まで必ずセットで**当てること
（`030000` 単体でも衝突しない定義に直してあるが、`040000` は旧定義で適用済みの環境を
そろえるために残してある）。適用後は必ず「同じ分に発火するジョブが無いこと」を確認する。

### 2026-08-25 09:25 JST 時点の prod の最終状態

**Edge Function を先にデプロイしてから catchup を適用した**（claim-then-send が入っていない
コードに配信頻度を上げる変更を当てると、レースを本番に持ち込むため）。

| 関数 | 版 | 備考 |
|---|---|---|
| `webhook` | v4 | `46ec76d`（message イベントでも `line_users` 登録）が初めて本番に入った |
| `message-sender` | v3 | claim-then-send / `confirm_broadcast` の記録 |
| `scraper` | v3 | セクション0件を取得失敗として扱う誤検知防御 |

cron（3本・**同一分の衝突なし**を確認済み）:

```
confirm-broadcast-daily    '0 1 * * *'
twipla-scrape-poll         '*/15 * * * *'
confirm-broadcast-catchup  '15,45 0-12 * * *'
```

適用時点の実データ:

- **確認配信の対象は0人** — 適用当日に送信は発生しない
- 次のイベントは **9/22（28日後・`confirm_days_before=3`）**。
  ポーリングも確認配信も **9/19 から動き出す**。約1ヶ月の検証余裕がある
- `participants` は completed 15 / pending 1（pending は 8/22 の過去イベント＝窓の外）

**最初に効く日（9/19）に見るべき観測点**: `cron.job_run_details` の
`twipla-scrape-poll` が `'0 rows'` から `'1 row'` に変わること、
`net._http_response` が 200 を返すこと、`notification_logs` に
`confirm_broadcast` の行が増えること、そして `skippedConcurrent` が 0 のままであること。
