---
phase: 04-notifications-integration
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - admin/lib/schemas/oa.ts
  - admin/lib/schemas/template.ts
  - admin/lib/data/templates.ts
  - admin/lib/actions/templates.ts
  - admin/components/oa/template-save-dialog.tsx
  - admin/components/oa/template-apply-select.tsx
  - admin/components/oa/question-list-editor.tsx
  - admin/components/oa/oa-settings-form.tsx
  - admin/app/(app)/oa/settings/page.tsx
  - admin/tests/unit/template-schema.test.ts
  - admin/tests/integration/rls.test.ts
  - admin/tests/integration/data.test.ts
autonomous: true
requirements: [OA-03]
must_haves:
  truths:
    - "OA設定の質問エディタから現在の質問セットを名前付きテンプレートとして保存できる（zod サーバー再検証 → question_templates INSERT）"
    - "自分がアクセスできる全OAのテンプレートが適用候補に出て、選択→確認→クライアント置換で別OA・別イベントに再利用できる（OA-03）"
    - "root は全OAの oa_configs/events/participants/answers/question_templates を SELECT でき、owner は他OAを見られない — 同一テスト内の対比で権限差を検証（ROADMAP 成功条件3 / OA-02 root 完成）"
    - "root は UPDATE 0行・INSERT 拒否（SELECT-only）、root_users 自体は authenticated から不可視、authenticated は root_users に INSERT できない"
    - "user1 は OA2 のテンプレートを SELECT 0行・OA2 への INSERT は拒否される（テンプレートの OA スコープ）"
  artifacts:
    - path: "admin/lib/schemas/template.ts"
      provides: "templateSchema（name + questionsSchema 再利用）"
      exports: ["templateSchema"]
    - path: "admin/lib/schemas/oa.ts"
      provides: "questionsSchema の export 切り出し（oaSettingsSchema と同一実体）"
      exports: ["questionsSchema"]
    - path: "admin/lib/actions/templates.ts"
      provides: "saveQuestionTemplate server action"
      exports: ["saveQuestionTemplate"]
    - path: "admin/lib/data/templates.ts"
      provides: "listQuestionTemplates（フィルタなし SELECT — RLS がスコープ）"
      exports: ["listQuestionTemplates"]
    - path: "admin/components/oa/template-save-dialog.tsx"
      provides: "テンプレート保存 Dialog（04-UI-SPEC §1）"
    - path: "admin/components/oa/template-apply-select.tsx"
      provides: "テンプレート適用 Select + AlertDialog（04-UI-SPEC §2）"
  key_links:
    - from: "admin/lib/schemas/template.ts"
      to: "admin/lib/schemas/oa.ts"
      via: "questionsSchema import（独自スキーマ禁止 — locked）"
      pattern: "questionsSchema"
    - from: "admin/app/(app)/oa/settings/page.tsx"
      to: "admin/lib/data/templates.ts"
      via: "RSC で listQuestionTemplates → prop 渡し"
      pattern: "listQuestionTemplates"
    - from: "admin/components/oa/template-apply-select.tsx"
      to: "question-list-editor の onChange"
      via: "クライアント側 questions 置換（サーバー呼び出しなし — UI-SPEC locked）"
      pattern: "onChange"
---

<objective>
質問テンプレートの保存・クロスOA再利用（OA-03）を admin に実装し、RLS テストへ root 軸（OA-02 root 完成 — ROADMAP 成功条件3「rootユーザーは全OA・全イベント・全データを横断閲覧でき、owner/co-owner スコープとの権限差がテストで検証できる」）とテンプレート/通知ログの RLS 検証を追加する。

注: listMyOas のコード変更は不要 — RLS の OR 拡張だけで root には全OAが返る（RESEARCH Pattern 5 で dev 実機プローブ済み。CONTEXT が拡張点と想定した listMyOas は無変更で成立）。
Output: テンプレート一式（schema/data/action/UI）、root 軸 + テンプレート RLS テスト、クロスOA data テスト。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-notifications-integration/04-CONTEXT.md
@.planning/phases/04-notifications-integration/04-RESEARCH.md
@.planning/phases/04-notifications-integration/04-UI-SPEC.md

前提（04-01 完了済み）: question_templates / root_users / notification_logs がスキーマに存在し、setup-dev で dev-root@nomimas.test が root_users に投入済み。user1=OA1 owner / user2=OA2 owner + OA1 co-owner（複数OA所属 — クロスOA検証に使う）。

制約: dev のみ。RLS_TEST 実行は `set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a` 後に `cd admin && RLS_TEST=1 npx vitest run`。admin/ 内からの git 確認はルートアンカー（`git -C /Users/kugyu10/work/nomimas-bot ...`）。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: questionsSchema 切り出し + templateSchema + data層 + server action + unit テスト</name>
  <files>admin/lib/schemas/oa.ts, admin/lib/schemas/template.ts, admin/lib/data/templates.ts, admin/lib/actions/templates.ts, admin/tests/unit/template-schema.test.ts</files>
  <read_first>
    - admin/lib/schemas/oa.ts（questions 配列スキーマのインライン構造 — max 20 / superRefine id 一意 / LINE 上限）
    - admin/lib/actions/oa.ts saveOaSettings（同型にする server action パターン: safeParse → data層 → revalidatePath）
    - admin/lib/data/oa.ts（data 層の型・SupabaseClient 受け渡し流儀）
    - .planning/phases/04-notifications-integration/04-RESEARCH.md — Pattern 6 / Code Examples「テンプレート保存 server action」
    - admin/tests/unit/oa-schema.test.ts（unit テストの書式）
  </read_first>
  <behavior>
    - templateSchema: name 必須（min 1 / max 100）・空文字拒否 / questions は oaSettingsSchema と完全同一検証（id 重複拒否・max 20・LINE 上限）— D: CONTEXT「保存スキーマは oaSettingsSchema.questions と同一バリデーション」locked
    - oa-schema.test.ts 既存テストが切り出し後も無修正 green（リファクタの等価性証明）
  </behavior>
  <action>
    1. oa.ts: インラインの questions 配列スキーマ（superRefine 含む）を `export const questionsSchema` に切り出し、oaSettingsSchema から参照（挙動不変 — T-03-14 の bot 側同型性維持）
    2. template.ts: `templateSchema = z.object({ name: z.string().min(1).max(100), questions: questionsSchema })`
    3. data/templates.ts: `listQuestionTemplates(supabase)` — **フィルタなし** `from("question_templates").select("id, oa_config_id, name, questions").order("created_at", { ascending: false })`。RLS の oa_members チェーンが「自分がアクセスできる全OA」へ自動スコープするため where 句不要（Pattern 6 — クロスOA適用候補の充足）。返り値型 `QuestionTemplate` を export
    4. actions/templates.ts: `saveQuestionTemplate(oaConfigId, rawInput)` — Code Examples どおり: templateSchema.safeParse → insert + `.select("id")`（INSERT は RLS 違反で 403 エラーになる — silent-0-row しない）→ 失敗時はユーザー向け文言（UI-SPEC: 「テンプレートの保存に失敗しました。もう一度お試しください」）→ 成功時 `revalidatePath("/oa/settings")`
    5. tests/unit/template-schema.test.ts を先に書く（RED）: name 空/101字拒否、questions の id 重複拒否、有効入力 pass、oaSettingsSchema との検証同一性（同一不正 questions が両方で reject）
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/nomimas-bot/admin && npx vitest run tests/unit/template-schema.test.ts tests/unit/oa-schema.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - template-schema.test.ts green + 既存 oa-schema.test.ts 無修正 green
    - `grep -n "export const questionsSchema" admin/lib/schemas/oa.ts` が1件、template.ts が import している
    - data/templates.ts の select に `.eq(` フィルタが存在しない（RLS スコープ任せの確認）
    - tsc --noEmit green
  </acceptance_criteria>
  <done>テンプレートのスキーマ・data・action が saveOaSettings と同型で実装され unit green</done>
</task>

<task type="auto">
  <name>Task 2: テンプレート保存 Dialog + 適用 Select UI（04-UI-SPEC §1・§2 準拠）</name>
  <files>admin/components/oa/template-save-dialog.tsx, admin/components/oa/template-apply-select.tsx, admin/components/oa/question-list-editor.tsx, admin/components/oa/oa-settings-form.tsx, admin/app/(app)/oa/settings/page.tsx</files>
  <read_first>
    - .planning/phases/04-notifications-integration/04-UI-SPEC.md §1（保存 Dialog: トリガー配置・構造・状態表・文言）/ §2（適用 Select + AlertDialog: 配置・文言・確認フロー）/ Copywriting Contract（canonical ラベル: 保存しない / テンプレートとして保存 / テンプレートを適用 / 適用しない — 省略形「キャンセル」「適用」禁止）/ Accessibility Contract
    - admin/components/oa/question-list-editor.tsx（controlled component の value/onChange 契約・「質問を追加」ボタン位置・既存 aria-label パターン line 174-183）
    - admin/components/oa/oa-settings-form.tsx（QuestionListEditor の結線 line 193 近傍・Alert 表示パターン）
    - admin/app/(app)/oa/settings/page.tsx（RSC のデータ取得 → form への prop 渡し）
  </read_first>
  <action>
    UI-SPEC §1・§2 を文言・variant・状態まで忠実に実装（D: CONTEXT「質問テンプレート」UI locked）:
    - template-save-dialog.tsx: トリガー「テンプレートとして保存」（variant=outline size=sm / BookmarkPlus アイコン / 「質問を追加」と同列 flex gap-2 / questions 0件時 disabled + Tooltip「質問がありません」）。Dialog: タイトル・Input（autoFocus / label「テンプレート名」/ placeholder「例: 歓迎会の定型質問セット」/ helper「現在の質問 {n} 件を保存します」）・footer [保存しない(ghost)][テンプレートとして保存(default)]。name 空 or in-flight で CTA disabled + Loader2 スピナー。Enter submit 対応。成功: close + 親に成功 Alert「テンプレートを保存しました」（4秒 auto-dismiss）。失敗: open 維持 + footer 下に destructive 文言。DialogContent に aria-label
    - template-apply-select.tsx: 質問リスト上部にセクションラベル「テンプレートを適用」+ Select（placeholder「テンプレートを選択...」/ option 形式「{name}（{n}件の質問）」/ max-w-[280px] / 空時 disabled option「保存済みテンプレートがありません」）+ ボタン「テンプレートを適用」（outline sm / 未選択 disabled）。クリックで AlertDialog: タイトル「現在の質問を上書きしますか？」/ description は UI-SPEC §2 の全文（テンプレート名・件数を補間）/ [適用しない(outline)][テンプレートを適用(default — destructive にしない)]。confirm: onChange でクライアント置換のみ（サーバー呼び出しなし — 永続化は既存「設定を保存」）+ Select リセット。cancel: 選択保持
    - question-list-editor.tsx / oa-settings-form.tsx: 上記2コンポーネントを結線（templates prop と saveQuestionTemplate を通す。editor の value/onChange 契約は不変）
    - page.tsx: listQuestionTemplates を RSC で取得し props 経由で渡す
    - 新規 shadcn インストール禁止（Dialog/Input/Select/AlertDialog/Button/Tooltip は Phase 3 導入済み — UI-SPEC Registry Safety）
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/nomimas-bot/admin && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - tsc + build green
    - canonical ラベル4種が存在: `grep -rl "テンプレートとして保存" admin/components/oa/ | wc -l` >= 1、`grep -rn "保存しない\|適用しない" admin/components/oa/template-*.tsx` が双方ヒット
    - `grep -rn "shadcn" admin/package.json` 差分なし（新規 add なし — `git -C /Users/kugyu10/work/nomimas-bot diff --stat admin/package.json` が空）
    - template-apply-select.tsx に fetch/action 呼び出しがない（適用はクライアント置換のみ）: `grep -c "use server\|saveQuestionTemplate" admin/components/oa/template-apply-select.tsx` が 0
  </acceptance_criteria>
  <done>保存/適用 UI が UI-SPEC の文言・状態契約どおりに結線され build green</done>
</task>

<task type="auto">
  <name>Task 3: RLS root 軸 + テンプレート/通知ログ RLS + クロスOA data テスト</name>
  <files>admin/tests/integration/rls.test.ts, admin/tests/integration/data.test.ts</files>
  <read_first>
    - admin/tests/integration/rls.helpers.ts（asUser ハーネス — sub 差し替えで実行者切替 / connectDev 二重ガード）
    - admin/tests/integration/rls.test.ts（既存マトリクスの構造・OA1_ID/OA2_ID 定数・fixture 投入流儀）
    - .planning/phases/04-notifications-integration/04-RESEARCH.md — Code Examples「RLS マトリクスへの root 軸追加」（beforeAll の root id 動的取得含む全文）/ Pattern 5 プローブ結果 / Pitfall 7
    - admin/tests/integration/data.test.ts（data 層テストの書式）
  </read_first>
  <action>
    rls.test.ts に追記（既存テストは無修正 — 不変が回帰証明）:
    1. beforeAll: `select auth_user_id from public.root_users limit 1` で root id を動的取得（空なら setup-dev 未実行エラー — Code Examples どおり）
    2. describe「root 横断閲覧（OA-02完成・成功条件3）」: root が両OA（OA1/OA2）の oa_configs・events・participants・answers・question_templates を SELECT できる（oa_configs は 2行）/ **同一クエリで owner(user1) は 1行** — 権限差の対比 assert を同一テスト内に置く / root の oa_configs UPDATE が 0行 / root の events INSERT が reject（with check 違反）/ root_users が root 自身（authenticated）からも 0行（存在秘匿）/ 通常ユーザー(user1) が root_users へ INSERT できない（権限昇格防止 — T-04-01 の退行検知）
    3. describe「question_templates RLS」: service role fixture で OA1/OA2 に各1テンプレート投入（afterAll で削除）→ user1: OA2 テンプレ SELECT 0行 / OA2 への INSERT reject / 自OA(OA1) INSERT 成功 / root: 全件 SELECT
    4. describe「notification_logs RLS」: service role fixture 1行（OA2 配下・**固定UUIDで投入**）→ **assert は全て fixture 固定UUIDにスコープする**（`where id = FIXTURE_LOG_ID`）: user1 SELECT 0行 / root SELECT 1行 / user1 の INSERT reject（書込ポリシーなし）。Wave 2 並列で 04-02 の E2E が OA1 配下に logs 行を生成するため、unscoped な件数 assert は禁止（flake 防止）

    data.test.ts に追記:
    5. listQuestionTemplates: user2（OA2 owner + OA1 co-owner）で実行すると両OAのテンプレートが返る（クロスOA適用候補 — OA-03「別イベント・別OAで再利用」の data 層検証）。user1 で実行すると OA1 分のみ
  </action>
  <verify>
    <automated>set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a; cd /Users/kugyu10/work/nomimas-bot/admin && RLS_TEST=1 npx vitest run tests/integration/rls.test.ts tests/integration/data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - RLS_TEST 実行で新規 describe 3+1 ブロックすべて green、既存マトリクスは無修正 green
    - root の権限差対比（root=2行 / owner=1行）・UPDATE 0行・INSERT reject・root_users 不可視・root_users INSERT 不可の5系統 assert が存在
    - user2 のクロスOA テンプレート取得 assert が存在
    - fixture は afterAll で削除され再実行 green（冪等）
  </acceptance_criteria>
  <done>成功条件3（root と owner/co-owner の権限差）と OA-03 の RLS/data 検証が自動テストで green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server action | テンプレート name/questions のユーザー入力 |
| authenticated → question_templates / root_users (RLS) | OA スコープと root SELECT-only の境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering（テンプレート注入） | saveQuestionTemplate | mitigate | templateSchema（= oaSettingsSchema.questions と同一実体）でサーバー再検証。クライアント値を信用しない。unit テストで同一性を検証 |
| T-04-12 | Elevation of Privilege | root_users / is_root() | mitigate | rls.test.ts に「authenticated が root_users に INSERT 不可」「root UPDATE 0行 / INSERT reject」を追加し恒久退行検知（Task 3） |
| T-04-13 | Information Disclosure | クロスOA テンプレート | mitigate | listQuestionTemplates はフィルタなしだが RLS の oa_members チェーンがスコープ（user1 が OA2 テンプレ 0行を Task 3 で検証） |
| T-04-14 | Tampering | RLS OR 拡張の owner スコープ退行 | mitigate | 既存マトリクステスト無修正 green を acceptance に明記（security_requirement の regression matrix） |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージゼロ（shadcn 追加 add も禁止 — UI-SPEC Registry Safety） |
</threat_model>

<verification>
- unit（template-schema / oa-schema）+ tsc + build green
- RLS_TEST: root 軸 + テンプレート + 通知ログ + 既存マトリクス全 green
- data.test.ts クロスOA green
</verification>

<success_criteria>
- OA-03: テンプレート保存→別OAで適用候補に出る、が UI + data + RLS の3層で検証済み
- ROADMAP 成功条件3: root 全OA横断 SELECT と owner/co-owner との権限差が同一テスト内対比で green
- root は SELECT-only（UPDATE 0行 / INSERT reject）が自動テストで担保
</success_criteria>

<output>
Create `.planning/phases/04-notifications-integration/04-03-SUMMARY.md` when done
</output>
