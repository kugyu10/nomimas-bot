---
phase: 04-notifications-integration
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - supabase/functions/_shared/notify/notifier.ts
  - supabase/functions/webhook/index.ts
  - supabase/functions/scraper/index.ts
  - supabase/functions/tests/e2e_full_chain_test.ts
autonomous: true
requirements: [NOTIF-01]
must_haves:
  truths:
    - "窓内イベント（event_date = 今日+1）の回答 postback 後、notification_logs に kind='answer' の行が1行でき sent>=1（DRY_RUN でも送信実行の証跡）"
    - "窓外イベント（event_date = 今日+10）への postback では answers は保存されるが、その event_id の notification_logs は 0 行 — 成功条件1の対比（2日前より前では通知されない）"
    - "最終問の postback では kind='completion' が1行のみ増え、kind='answer' は増えない（二重通知なし）"
    - "再スクレイプで status 変化+新規参加者が生じると kind='scrape_changes' が1行（1スクレイプ=最大1サマリ通知）でき detail の件数が一致する"
    - "通知の失敗・例外が webhook の常時200契約と reply 送信、scraper の保存処理を一切壊さない"
    - "実Twipla URL https://twipla.jp/events/731057 のスクレイプ→紐付け→配信→postback×3→answers 3行→通知、の全鎖が E2E_TEST=1 で green"
  artifacts:
    - path: "supabase/functions/_shared/notify/notifier.ts"
      provides: "notifyConfirmUpdate / notifyScrapeChanges + NotifyResult"
      exports: ["notifyConfirmUpdate", "notifyScrapeChanges"]
    - path: "supabase/functions/tests/e2e_full_chain_test.ts"
      provides: "全鎖 E2E（窓内通知/窓外非通知/scrape差分/二重通知防止の SQL assert）"
      min_lines: 150
  key_links:
    - from: "supabase/functions/webhook/index.ts"
      to: "_shared/notify/notifier.ts"
      via: "reply 送信後の try/catch 付き notifyConfirmUpdate 呼び出し"
      pattern: "notifyConfirmUpdate"
    - from: "supabase/functions/scraper/index.ts"
      to: "_shared/notify/notifier.ts"
      via: "select-before-upsert 差分 → notifyScrapeChanges"
      pattern: "notifyScrapeChanges"
    - from: "_shared/notify/notifier.ts"
      to: "notification_logs"
      via: "service role INSERT（送信実行時のみ・窓外は書かない）"
      pattern: "notification_logs"
---

<objective>
notifier 本体を実装し、webhook（回答保存/完了時）と scraper（再スクレイプ差分時）に結線して dev へデプロイ、実Twipla URL での全鎖 E2E（通知発火/非発火の対比含む）を green にする。

Purpose: NOTIF-01 の本体。「開催2日前以降にメンバーの出欠・最終確認に更新があると owner/co-owner の LINE へ都度通知が届く」（ROADMAP 成功条件1）と全鎖 E2E（成功条件4の supabase 側）を機械検証可能な形で完成させる。
Output: notifier.ts、結線済み webhook/scraper（dev デプロイ済み）、e2e_full_chain_test.ts green。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-notifications-integration/04-CONTEXT.md
@.planning/phases/04-notifications-integration/04-RESEARCH.md

前提（04-01 完了済み）: notification_logs / oa_members.line_user_id がスキーマに存在し、setup-dev で user1 の line_user_id 投入済み（user2 は null = skipped 経路）、_shared/notify/{window,diff,messages}.ts 実装済み、dev secrets に LINE_DRY_RUN=1。

制約: dev のみ。env: `set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a`。デプロイは `supabase functions deploy <fn> --project-ref "$DEV_PROJECT_REF" --use-api --import-map supabase/functions/deno.json`（webhook のみ `--no-verify-jwt` 追加）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: notifier.ts（受信者解決→窓判定→push→notification_logs 記録）</name>
  <files>supabase/functions/_shared/notify/notifier.ts</files>
  <read_first>
    - .planning/phases/04-notifications-integration/04-RESEARCH.md — Pattern 1（NotifyResult / 公開シグネチャ全文）/ Code Examples「notifier 本体の照会クエリ」/ Pitfall 4（チャネル不一致ガード）
    - supabase/functions/_shared/line/client.ts（pushMessage の引数・DRY_RUN・宛先マスク挙動）
    - supabase/functions/_shared/line/token.ts（issueStatelessToken — scraper 経路で notifier 内発行）
    - supabase/functions/message-sender/index.ts（per-target 継続パターン — 1宛先の失敗で全体を止めない流儀）
    - supabase/functions/_shared/notify/{window,diff,messages}.ts（04-01 成果物 — 再利用する純関数）
  </read_first>
  <action>
    Pattern 1 のシグネチャどおりに実装する（D: CONTEXT「更新通知」locked）:
    - `NotifyResult { kind, inWindow, recipients, sent, failed, skippedNoLineId }` を export
    - `notifyConfirmUpdate(supabase, getToken, { participantId, kind: "answer" | "completion" })`: participants → event_platform_urls(events(id, title, event_date, oa_config_id)) を1クエリで解決（Code Examples のネスト select）。解決不能はログのみで inWindow:false return
    - 共通フロー（b〜f）: (b) `isWithinNotifyWindow(ev.event_date, todayJst())` 偽なら `console.log` で件数ログのみ・**notification_logs に行を書かず** return（成功条件1「窓外0行」の検証規約 — Pattern 4）。(c) 対象 OA の `oa_configs.line_channel_id` が env LINE_CHANNEL_ID と不一致ならスキップ+ログ（Pitfall 4）。(d) `oa_members.eq(oa_config_id)` から line_user_id 非null を recipients、null 数を skippedNoLineId。(e) 宛先ごとに pushMessage（messages.ts の文面ビルダー使用・per-target try/catch で failed カウントし継続）。(f) notification_logs に1行 INSERT（kind / recipients / sent / failed / skipped_no_line_id / detail — scrape は `{"new": n, "statusChanged": m}` の件数のみ・answer/completion は null。参加者名・userId を detail に入れない）
    - `notifyScrapeChanges(supabase, params)`: params は Pattern 1 どおり（eventId / oaConfigId / eventTitle / eventDate / newParticipants / statusChanges）。トークンは notifier 内で issueStatelessToken（scraper はキャッシュを持たない）。通知は buildScrapeChangesNotification で**1サマリ1通**
    - ログ出力に LINE userId・参加者生データを含めない（T-02-08/T-02-14 踏襲）
  </action>
  <verify>
    <automated>deno check --config supabase/functions/deno.json supabase/functions/_shared/notify/notifier.ts && deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/</automated>
  </verify>
  <acceptance_criteria>
    - deno check が exit 0、既存 Deno テスト全件 green（notifier は I/O 層 — 検証本体は Task 3 の E2E）
    - `grep -n "notification_logs" supabase/functions/_shared/notify/notifier.ts` で INSERT が存在し、窓外 return が INSERT より前にある（コード順で確認）
    - `grep -c "line_user_id" supabase/functions/_shared/notify/notifier.ts` の参照はあるが、console.log/error の引数に userId 変数を渡す行が 0
  </acceptance_criteria>
  <done>notifier 2関数が Pattern 1 契約どおりに実装され型チェック green</done>
</task>

<task type="auto">
  <name>Task 2: webhook/scraper への結線 + dev デプロイ</name>
  <files>supabase/functions/webhook/index.ts, supabase/functions/scraper/index.ts</files>
  <read_first>
    - supabase/functions/webhook/index.ts — handleEvent の postback 分岐: answers upsert（line 329-352, 失敗時 answerPersistFailureResult 差替え）→ participants update → reply 送信（line 375-421, reply==="none" 早期 return / token 不可 早期 return あり）
    - supabase/functions/scraper/index.ts — ステップ(4): epu lookup（line 68-72）→ rows 構築 → upsert（line 105-107）→ レスポンス（line 124-135, platform/count/saved）
    - .planning/phases/04-notifications-integration/04-RESEARCH.md — Pattern 1「webhook 挿入点」「scraper 挿入点」/ Pattern 2（select-before-upsert 全文）/ Code Examples「webhook 挿入」/ Pitfall 2・3・8 / Assumptions A5
  </read_first>
  <action>
    webhook（Code Examples「webhook 挿入」どおり）:
    - answers upsert 箇所に `answerPersistFailed` フラグを導入（upsertError 時 true — result 差替えだけでは保存成否を後段で判別できないため明示化）
    - 通知呼び出しは postback 分岐の**末尾**: `result.answer && !answerPersistFailed` のときのみ `notifyConfirmUpdate(supabase, getToken, { participantId, kind: result.reply === "completion" ? "completion" : "answer" })` — kind 排他で最終問は completion 1通のみ（Pitfall 8）。`reply === "none"` 経路は answer なしのため自然に通知なし
    - 位置は reply 送信の**後**（ユーザーへの次質問提示を遅延させない — Pitfall 3）。現状の reply ブロックは token 不可・次質問不在で早期 return するため、reply 処理を早期 return しない構造（reply 部をローカル関数化 or フラグ制御）に変え、reply の成否に関わらず通知呼び出しへ到達させる
    - 通知全体を try/catch で包み console.error のみ（200 契約・再配達ストーム防止）。成功時は NotifyResult のサマリ（kind/inWindow/sent/failed/skipped 件数のみ）をログ

    scraper（Pattern 2 どおり）:
    - epu lookup の select を `"id, events(id, title, event_date, oa_config_id)"` に拡張（追加クエリなし）
    - upsert 前に `from("participants").select("natural_key, status, display_name").eq("event_platform_url_id", epu.id)` — existErr 時は差分検出を諦め通知スキップ（upsert は続行 — 取得保存優先）
    - upsert 成功後に `diffParticipants(existing, incoming)`。`existing.length === 0`（初回取り込み）は通知スキップ+件数ログ（Pitfall 2 / RESEARCH Open Question 2 確定）。変化ありなら notifyScrapeChanges を try/catch で呼ぶ（1スクレイプ=最大1通）
    - レスポンスに `changes: { new, statusChanged }, notified`（sent 数）を追加。既存キー platform/count/saved は不変（A5 — Open Question 1 確定: 含める）

    デプロイ（結線後）:
    - `set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a`
    - `supabase functions deploy webhook --project-ref "$DEV_PROJECT_REF" --use-api --import-map supabase/functions/deno.json --no-verify-jwt`
    - `supabase functions deploy scraper --project-ref "$DEV_PROJECT_REF" --use-api --import-map supabase/functions/deno.json`
  </action>
  <verify>
    <automated>deno check --config supabase/functions/deno.json supabase/functions/webhook/index.ts supabase/functions/scraper/index.ts && deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/</automated>
  </verify>
  <acceptance_criteria>
    - deno check + 既存 Deno テスト全件 green（twipla_live_test / line_events_test 等の回帰含む）
    - webhook: `grep -n "notifyConfirmUpdate" supabase/functions/webhook/index.ts` が replyMessage 呼び出しより後の行番号にある
    - scraper: `grep -n "diffParticipants\|notifyScrapeChanges" supabase/functions/scraper/index.ts` が存在し、既存レスポンスキー platform/count/saved が不変
    - デプロイ2本が exit 0
  </acceptance_criteria>
  <done>webhook/scraper が notifier に結線され dev にデプロイ済み、既存テスト回帰 green</done>
</task>

<task type="auto">
  <name>Task 3: e2e_full_chain_test.ts（実Twipla 全鎖 + 窓内/窓外対比 + scrape差分）</name>
  <files>supabase/functions/tests/e2e_full_chain_test.ts</files>
  <read_first>
    - supabase/functions/tests/e2e_confirm_flow_test.ts（generateSignature / sendSignedEvent / makePostbackEvent / encodePostback / SQL assert / teardown 復元 — 全面流用するパターン）
    - .planning/phases/04-notifications-integration/04-RESEARCH.md — Pattern 7（シナリオ (a)〜(g) 全文）/ Code Examples「E2E: 通知発火/非発火の SQL assert」/ Pitfall 1・10
    - supabase/seed.sql（固定UUID: OA1=...0001 / event=...0002 / epu=...0003（twipla 731057 登録済み）/ line_user=...0004 / participant=...0005）
  </read_first>
  <action>
    Pattern 7 のシナリオ (a)〜(g) を `Deno.env.get("E2E_TEST") === "1"` ゲートで実装:
    (a) setup: seed event ...0002 の event_date を current_date+1 に UPDATE（窓内化 — Pitfall 1）/ 窓外フィクスチャ（OA1 配下に event(current_date+10) + epu(架空URL) + participant(line_user ...0004 紐付け・confirm_status='sent'・index 0)）を固定UUIDで作成 / 対象 notification_logs 削除 / participant ...0005 を pending/index0 にリセット + answers 削除
    (b) 実Twipla スクレイプ: POST scraper {url: https://twipla.jp/events/731057}（Bearer anonKey）→ 200 / saved:true / count>0
    (c) scrape 差分通知（決定的化）: SQL で実スクレイプ行1行の status を別値に UPDATE + 1行 DELETE → 再 POST scraper → レスポンス changes が statusChanged>=1 / new>=1、notification_logs に kind='scrape_changes' 1行（sent>=1・detail 件数一致）を SQL assert
    (d) message-sender 配信（x-cron-key + anonKey）→ participant ...0005 が 'sent'
    (e) 署名付き postback×3: Q1 後に answers 1行 + kind='answer' 1行（sent>=1、skipped_no_line_id=1 — user2 が null のため）/ Q3 後に completed + answers 3行 + kind='completion' 1行、かつ kind='answer' の行数が増えていないこと（二重通知防止 — Pitfall 8）
    (f) 窓外非通知（成功条件1の裏面）: 窓外フィクスチャ participant へ署名付き postback → answers は保存される / 該当 event_id の notification_logs が 0 行を assert（Code Examples の SQL そのまま）
    (g) teardown: event_date を current_date+3 に復元 / 窓外フィクスチャを FK 順に削除 / (c) で変異させた行の整合確認（再スクレイプで実態一致済み）/ participant リセット / notification_logs 掃除（Pitfall 10）

    実行して green を確認: `set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a; E2E_TEST=1 LINE_DRY_RUN=1 deno test --allow-net --allow-read --allow-env --config supabase/functions/deno.json supabase/functions/tests/e2e_full_chain_test.ts`
  </action>
  <verify>
    <automated>set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a; E2E_TEST=1 LINE_DRY_RUN=1 deno test --allow-net --allow-read --allow-env --config supabase/functions/deno.json supabase/functions/tests/e2e_full_chain_test.ts</automated>
  </verify>
  <acceptance_criteria>
    - E2E が green で、(e) 窓内 kind='answer'/'completion' 行あり・(f) 窓外 0 行・(c) kind='scrape_changes' 行あり、の3 assert がすべて含まれる（成功条件1の対比 + scrape 差分）
    - (e) の skipped_no_line_id assert が setup-dev の投入内容（user2=null → 1）と一致
    - teardown 後に再実行しても green（冪等性 — Pitfall 10）
    - 注: twipla.jp 731057 到達不能で (b) が fail した場合は環境起因（Assumption A2）— 別の公開イベントURLへの差し替えを SUMMARY の deviation に記録し人間に報告（テストを skip 化してごまかさない）
  </acceptance_criteria>
  <done>実Twipla URL での全鎖 E2E（スクレイプ→紐付け済みseed→配信→postback×3→answers→通知 logs assert）が green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LINE platform → webhook | 署名検証済み入力（既存・不変）— 通知はこの内側で発火 |
| anon caller → scraper | 第三者がスクレイプ起動可能（Phase 3 計上済み）→ 通知スパム面 |
| notifier → LINE push | PII（userId宛先）が外部 API へ渡る |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | 通知文面 / notification_logs.detail / console | mitigate | 文面3要素のみ（messages.ts 経由を強制）。detail は件数 jsonb。console に userId を渡さない（Task 1 acceptance で機械検査） |
| T-04-07 | DoS | webhook 200 契約 | mitigate | notify は reply 後 + try/catch で握る。失敗は console.error のみ — 再配達ストーム再来防止（Pitfall 3） |
| T-04-08 | DoS（通知スパム） | scraper 起動経由の owner 通知 | mitigate | 登録URL完全一致 + 差分発生時のみ + 初回スキップ + 1スクレイプ=1サマリで律速。webhook 経路は isRedelivery スキップで自然冪等（1イベント=最大1通） |
| T-04-09 | Spoofing | postback → 通知発火 | mitigate | 既存 T-02-11 なりすまし照合（保存前ガード）を通過した postback のみ通知に到達（コード順で構造的に担保） |
| T-04-10 | Tampering | scraper レスポンス拡張 | mitigate | 既存キー platform/count/saved 不変（A5）。参加者生データはレスポンスに含めない（T-01-08 維持） |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージゼロ |
</threat_model>

<verification>
- Deno 全件（既存 + notify unit）green
- e2e_full_chain_test.ts green（窓内発火 / 窓外非発火 / scrape差分 / 二重通知防止の4系統 assert）
- webhook/scraper デプロイ exit 0
</verification>

<success_criteria>
- ROADMAP 成功条件1: 窓内更新→通知 logs 行あり・窓外→0行が E2E で機械検証済み
- ROADMAP 成功条件4（supabase 側）: 実Twipla URL 全鎖 green（admin データ層は Plan 03 の data.test.ts が担保）
- 通知失敗が webhook/scraper の主経路を壊さない
</success_criteria>

<output>
Create `.planning/phases/04-notifications-integration/04-02-SUMMARY.md` when done
</output>
