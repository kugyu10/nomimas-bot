---
phase: 04-notifications-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260611171037_create_core_tables.sql
  - supabase/migrations/20260611171038_enable_rls.sql
  - supabase/functions/_shared/notify/window.ts
  - supabase/functions/_shared/notify/diff.ts
  - supabase/functions/_shared/notify/messages.ts
  - supabase/functions/tests/notify_window_test.ts
  - supabase/functions/tests/notify_diff_test.ts
  - supabase/functions/tests/notify_messages_test.ts
  - scripts/setup-dev.ts
autonomous: true
requirements: [NOTIF-01, OA-03]
must_haves:
  truths:
    - "dev で `supabase db reset --linked --yes` がゼロから成功し、root_users / question_templates / notification_logs / oa_members.line_user_id が存在する"
    - "通知窓判定の純関数が境界網羅でテスト済み（diff=0/1/2→通知、diff=3/-1/null→非通知）— 成功条件1の根拠ユニット"
    - "差分検出純関数が 新規/出欠変化/不変/初回(existing空) を正しく分類する"
    - "通知文面は参加者表示名・更新種別・イベント名の3要素のみで LINE userId を含まない"
    - "setup-dev 後に root テストユーザー(dev-root@nomimas.test)が root_users に存在し、user1 の oa_members.line_user_id が非null"
    - "Phase 1-3 の全回帰テスト（Deno 82+件 / admin vitest / RLS_TEST マトリクス）が無修正で green（SELECT OR 拡張が既存 deny を壊していない）"
  artifacts:
    - path: "supabase/functions/_shared/notify/window.ts"
      provides: "todayJst() + isWithinNotifyWindow() 純関数"
      exports: ["todayJst", "isWithinNotifyWindow"]
    - path: "supabase/functions/_shared/notify/diff.ts"
      provides: "diffParticipants() 純関数 + ExistingRow/DiffResult 型"
      exports: ["diffParticipants"]
    - path: "supabase/functions/_shared/notify/messages.ts"
      provides: "通知テキスト組み立て純関数（3要素のみ）"
    - path: "supabase/migrations/20260611171037_create_core_tables.sql"
      provides: "root_users / question_templates / notification_logs / oa_members.line_user_id"
      contains: "create table public.notification_logs"
    - path: "supabase/migrations/20260611171038_enable_rls.sql"
      provides: "is_root() + SELECT 9本の OR 拡張 + 新テーブルポリシー"
      contains: "create or replace function public.is_root()"
  key_links:
    - from: "supabase/migrations/20260611171038_enable_rls.sql"
      to: "既存 SELECT ポリシー7本"
      via: "or (select public.is_root()) の追記（for select のみ）"
      pattern: "or \\(select public\\.is_root\\(\\)\\)"
    - from: "scripts/setup-dev.ts"
      to: "root_users / oa_members.line_user_id"
      via: "service role による冪等投入"
      pattern: "root_users"
---

<objective>
Phase 4 の全機能の土台を作る: スキーマ拡張（通知先列・root・テンプレート・通知ログの4点を既存マイグレーション in-place 編集）、通知の純関数モジュール（窓判定・差分検出・文面）+ ユニットテスト、setup-dev 拡張、そして [BLOCKING] dev 再適用 + 全回帰。

Purpose: NOTIF-01（通知窓・差分・文面・notification_logs による機械検証基盤）、OA-03（question_templates）、OA-02 root 完成（root_users + is_root() + SELECT-only OR 拡張 — ROADMAP 成功条件3）のスキーマ前提をすべて Wave 1 で確定し、Wave 2 の webhook/scraper 統合と admin 側を並列実行可能にする。
Output: 編集済みマイグレーション2本、_shared/notify 純関数3本 + テスト3本、拡張 setup-dev.ts、dev 再適用済み + 回帰 green。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-notifications-integration/04-CONTEXT.md
@.planning/phases/04-notifications-integration/04-RESEARCH.md

制約（全タスク共通）:
- dev のみ（cmsxvxtcdniqgvhxjqri）。prod（hgojtooexbknqotzkkja）には一切触れない。
- env 読込: `set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a`
- public リポジトリ — シークレット値・実 LINE userId をコミットしない（dev用架空値 `U...ownr1` は seed の line_users と同流儀で可）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: マイグレーション in-place 編集（4テーブル変更 + is_root() + RLS OR拡張）</name>
  <files>supabase/migrations/20260611171037_create_core_tables.sql, supabase/migrations/20260611171038_enable_rls.sql</files>
  <read_first>
    - .planning/phases/04-notifications-integration/04-RESEARCH.md — Pattern 4（notification_logs DDL+ポリシー全文）/ Pattern 5（root_users + is_root() — dev実機プローブ済みSQL全文）/ Pattern 6（question_templates DDL+ポリシー全文）/ Pattern 8（oa_members.line_user_id）/ Pitfall 5・7
    - supabase/migrations/20260611171037_create_core_tables.sql（oa_members 定義 line 31〜 / 既存テーブル7本の規約）
    - supabase/migrations/20260611171038_enable_rls.sql（SELECT ポリシー7本: line 25, 38, 45, 58, 72, 85, 100 / `(select auth.uid())` initplan 規約 / register_owner_by_identity の search_path 規約 line 234）
  </read_first>
  <action>
    20260611171037_create_core_tables.sql に in-place 追加（D: CONTEXT「更新通知」「質問テンプレート」「root権限」の各 locked 決定に対応）:
    1. oa_members 定義に `line_user_id text` 列を追加（nullable・check 制約なし — Pitfall 5。コメントで「通知先 LINE userId / null=スキップ」を明記）
    2. `root_users(auth_user_id uuid primary key references auth.users(id) on delete cascade, created_at timestamptz not null default now())` — Pattern 5 の DDL そのまま
    3. `question_templates(id uuid pk default gen_random_uuid(), oa_config_id uuid not null references oa_configs(id) on delete cascade, name text not null, questions jsonb not null default '[]'::jsonb, created_at timestamptz not null default now())` — Pattern 6
    4. `notification_logs(id, oa_config_id fk cascade, event_id fk cascade, participant_id fk set null, kind text check in ('answer','completion','scrape_changes'), recipients/sent/failed/skipped_no_line_id integer default 0, detail jsonb, created_at)` — Pattern 4 の DDL そのまま（detail は件数のみ・PII なしをコメント明記）

    20260611171038_enable_rls.sql に in-place 追加:
    5. 3新テーブルの `enable row level security`。root_users にはポリシーを一切作らない（authenticated から不可視 = root の存在秘匿。INSERT 経路は service role のみ — 権限昇格防止 T-04-01）
    6. `is_root()` 関数: language sql / stable / security definer / `set search_path = ''` / スキーマ修飾（`public.root_users`）/ `revoke all ... from public, anon` + `grant execute to authenticated` — Pattern 5 の全文どおり
    7. 既存 SELECT ポリシー7本（oa_configs / oa_members / events / event_platform_urls / line_users / participants / answers）の using 句末尾に `or (select public.is_root())` を追加。**for select ポリシーのみ**。INSERT/UPDATE ポリシーには絶対に足さない（Pitfall 7 — root は SELECT-only が locked）。裸の `or is_root()` は禁止 — 必ず `(select ...)` で initplan 化
    8. question_templates: select ポリシー（oa_members チェーン + or is_root()）と insert ポリシー（with check oa_members チェーンのみ — is_root なし）。UPDATE/DELETE ポリシーは作らない（v1 は保存+適用のみ — deny-by-default）
    9. notification_logs: select ポリシーのみ（oa_members チェーン + or is_root()）。書込ポリシーなし（service role の Edge Functions のみ INSERT）

    注意: `or (select public.is_root())` という文字列を SQL コメントに書かない（grep ゲートが自己無効化するため）。
  </action>
  <verify>
    <automated>grep -v '^\s*--' supabase/migrations/20260611171038_enable_rls.sql | grep -c "or (select public.is_root())" | grep -qx 9 && grep -q "create table public.root_users" supabase/migrations/20260611171037_create_core_tables.sql && grep -q "line_user_id text" supabase/migrations/20260611171037_create_core_tables.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*--' supabase/migrations/20260611171038_enable_rls.sql | grep -c "or (select public.is_root())"` が 9（既存7 + question_templates select + notification_logs select）
    - `grep -v '^\s*--' supabase/migrations/20260611171038_enable_rls.sql | grep -c "or is_root()"`（select ラップなしの裸形）が 0
    - core_tables に create table 3本（root_users / question_templates / notification_logs）と oa_members の line_user_id text が存在
    - enable_rls の for insert / for update / for delete ポリシー本文に is_root が出現しない: `awk '/for (insert|update|delete)/,/;/' supabase/migrations/20260611171038_enable_rls.sql | grep -c is_root` が 0
    - root_users に対する create policy 文が 0 件
  </acceptance_criteria>
  <done>マイグレーション2本が Pattern 4/5/6/8 の DDL を含み、SELECT のみ OR 拡張・root_users ポリシーゼロが grep で確認できる（適用は Task 3）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _shared/notify 純関数3本（窓判定・差分・文面）+ ユニットテスト3本</name>
  <files>supabase/functions/_shared/notify/window.ts, supabase/functions/_shared/notify/diff.ts, supabase/functions/_shared/notify/messages.ts, supabase/functions/tests/notify_window_test.ts, supabase/functions/tests/notify_diff_test.ts, supabase/functions/tests/notify_messages_test.ts</files>
  <read_first>
    - .planning/phases/04-notifications-integration/04-RESEARCH.md — Pattern 2（diffParticipants 実装全文）/ Pattern 3（todayJst + isWithinNotifyWindow 実装全文）/ Pitfall 11（文面3要素の例文）
    - supabase/functions/tests/confirm_state_test.ts（既存 Deno テストの書式・assert 流儀）
    - supabase/functions/deno.json（test 実行 config）
  </read_first>
  <behavior>
    - window: isWithinNotifyWindow — diff=0（当日）/1/2 → true、diff=3 / 負（終了後）/ eventDate null → false（成功条件1「2日前より前では通知されない」の根拠境界）。todayJst が YYYY-MM-DD 形式を返す
    - diff: 新規参加者の分類 / status 変化の分類（from/to 保持）/ 不変は両リスト空 / existing 空（初回）は全員 newParticipants（呼び出し側スキップ判断用に分類自体は行う）/ バッチ内に既存キーと新キーの混在
    - messages: answer / completion / scrape_changes の3種が「イベント名・参加者表示名（scrape は件数）・更新種別」を含むこと、出力に "U" 始まり33文字の userId パターンが含まれない（入力にも取らないシグネチャであることを型で担保）
  </behavior>
  <action>
    window.ts と diff.ts は RESEARCH Pattern 3 / Pattern 2 の実装をそのまま採用（todayJst は `Intl.DateTimeFormat("en-CA", { timeZone: "Asia/Tokyo" })`、日数差は `Date.parse` の UTC 同形比較）。messages.ts は通知文面組み立ての純関数3つ: buildAnswerNotification(eventTitle, participantName) / buildCompletionNotification(eventTitle, participantName) / buildScrapeChangesNotification(eventTitle, counts: { newCount, statusChangedCount })。文面は Pitfall 11 の例（`【dev-event】◯◯さんが最終確認の回答を更新しました` / `…最終確認を完了しました` / `参加者情報が更新されました（新規N名・出欠変更M名）`）に準拠 — 参加者名・更新種別・イベント名の3要素のみ（locked）。質問内容・回答値・LINE userId は引数にも文面にも入れない。

    テストは RED→GREEN: 先に3テストファイルを behavior の境界ケースで書き、失敗確認後に実装。既存テストと同じく外部 I/O ゼロ（--allow-read のみで走る）。
  </action>
  <verify>
    <automated>deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/notify_window_test.ts supabase/functions/tests/notify_diff_test.ts supabase/functions/tests/notify_messages_test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 上記 deno test が pass（窓境界 diff=0/1/2/3/-1/null の6ケース以上、diff 分類4系統、messages 3種）
    - `deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/` で既存テスト含め全件 green
    - messages テストに「文面に /U[0-9a-zA-Z]{32}/ がマッチしない」assert が存在（PII 漏えい T-04-03 の退行検知）
  </acceptance_criteria>
  <done>純関数3本がユニットテストで境界網羅され、既存 Deno テストも green</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] setup-dev 拡張 + dev スキーマ再適用 + 全回帰</name>
  <files>scripts/setup-dev.ts</files>
  <read_first>
    - scripts/setup-dev.ts（ensureUser 冪等パターン・oa_members 投入箇所・実行方法はファイル先頭コメント）
    - scripts/verify-cron.ts（再適用後の検証スクリプト）
    - .planning/phases/04-notifications-integration/04-RESEARCH.md — Pattern 8（投入値の確定仕様）/ Pitfall 6 / Assumptions A1
  </read_first>
  <action>
    setup-dev.ts を冪等のまま拡張（Pattern 8 どおり）:
    1. `dev-root@nomimas.test` を既存 ensureUser で作成し、`root_users` へ `insert ... on conflict do nothing`（service role 経由 — authenticated に INSERT 経路を作らない）
    2. user1（dev-owner-1 / oa ...0001 の owner 行）の oa_members に `line_user_id = 'U00000000000000000000000000ownr1'` を UPDATE（E2E 通知先）
    3. user2 の oa_members 行は line_user_id を null のまま残す（skipped_no_line_id 経路の E2E 検証用 — 変更しない）

    [BLOCKING] 再適用シーケンス（順序厳守 — Pitfall 6）:
    a. linked ref 確認: `cat supabase/.temp/project-ref` の出力が `cmsxvxtcdniqgvhxjqri` と一致しなければ即中断（prod 防御）
    b. `set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a` → `supabase db reset --linked --yes`
    c. `deno run --allow-net --allow-read --allow-env scripts/setup-dev.ts`
    d. `deno run --allow-net --allow-read --allow-env scripts/verify-cron.ts`
    e. SQL 検証（scripts/db/sql.ts の connectDev ハーネス経由の一時スクリプト or deno eval）: `select count(*) from public.root_users` >= 1 / `select count(*) from public.oa_members where line_user_id is not null` >= 1 / to_regclass で question_templates・notification_logs が非null / `select public.is_root()` が関数として存在（pg_proc）
    f. 全回帰: `deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/`（82+件）と `cd /Users/kugyu10/work/nomimas-bot/admin && npx vitest run` と `cd /Users/kugyu10/work/nomimas-bot/admin && RLS_TEST=1 npx vitest run` — **既存テストは一切編集せず green であること**（SELECT OR 拡張が owner/co-owner スコープを広げていない証明 — security_requirement の回帰マトリクス）
    g. A1 確認: `supabase secrets list --project-ref "$DEV_PROJECT_REF"` に LINE_DRY_RUN が存在しなければ `supabase secrets set LINE_DRY_RUN=1 --project-ref "$DEV_PROJECT_REF"` で再設定
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/nomimas-bot && deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/ && cd admin && npx vitest run && RLS_TEST=1 npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db reset --linked --yes` が exit 0（マイグレーション2本の編集が構文・依存順ともに健全である機械的証明）
    - setup-dev / verify-cron が exit 0、SQL 検証 e の4項目すべて成立
    - Deno 全件 + admin vitest 全件 + RLS_TEST マトリクスが**無修正で** green
    - `supabase secrets list --project-ref "$DEV_PROJECT_REF"` の出力に LINE_DRY_RUN が含まれる
  </acceptance_criteria>
  <done>dev がゼロから新スキーマで再現され、root/通知先データが投入済み、Phase 1-3 回帰が全 green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated → Postgres (RLS) | OR 拡張がここを通過する全 SELECT に影響する |
| service role scripts → DB | root_users / line_user_id の唯一の投入経路 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | root_users | mitigate | ポリシーゼロ（deny-by-default）。投入は setup-dev（service role）のみ。Task 1 acceptance で「root_users への create policy 0件」を機械検査 |
| T-04-02 | Elevation of Privilege | is_root() | mitigate | security definer + `set search_path = ''` + スキーマ修飾 + revoke public/anon（search_path ハイジャック防止 — register_owner_by_identity と同規約） |
| T-04-03 | Information Disclosure | messages.ts / notification_logs.detail | mitigate | 文面は3要素のみ・userId 非含有をユニットテストで退行検知。detail は件数 jsonb のみ |
| T-04-04 | Tampering | 既存 SELECT ポリシー7本の OR 拡張 | mitigate | for select のみ編集（awk ゲートで write ポリシーへの混入 0 を機械検査）+ 既存 RLS_TEST マトリクス無修正 green を Task 3 のブロッキング条件にする |
| T-04-05 | Tampering (root書込) | root の UPDATE/INSERT | mitigate | 書込ポリシー不変（dev プローブで root UPDATE=0行 確認済み）。Plan 03 の root 軸テストで恒久退行検知 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 本フェーズ新規パッケージゼロ（RESEARCH Package Legitimacy Audit: none） |
</threat_model>

<verification>
- マイグレーション grep ゲート（Task 1 acceptance 全項目）
- `supabase db reset --linked --yes` + setup-dev + verify-cron が exit 0
- Deno 全件 + vitest 全件 + RLS_TEST が無修正 green
</verification>

<success_criteria>
- 新スキーマ4点 + is_root() + OR 拡張9本が dev に適用済み
- notify 純関数3本がテスト済み（成功条件1の窓境界を含む）
- root テストユーザー・通知先 line_user_id が投入済み
- Phase 1-3 回帰 green + LINE_DRY_RUN 確認済み
</success_criteria>

<output>
Create `.planning/phases/04-notifications-integration/04-01-SUMMARY.md` when done
</output>
