---
phase: 03-admin-ui
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - admin/lib/schemas/oa.ts
  - admin/lib/data/oa.ts
  - admin/lib/actions/oa.ts
  - admin/app/(app)/oa/settings/page.tsx
  - admin/components/oa/oa-settings-form.tsx
  - admin/components/oa/question-list-editor.tsx
  - admin/tests/unit/oa-schema.test.ts
  - admin/tests/integration/rls.helpers.ts
  - admin/tests/integration/rls.test.ts
  - admin/tests/integration/auth.test.ts
autonomous: true
requirements: [OA-01, OA-02, AUTH-01]
user_setup: []

must_haves:
  truths:
    - "OAごとに定型文（最終確認メッセージ冒頭・完了メッセージ）・質問内容・管理者Twitter IDを設定・保存できる"
    - "OA設定の保存対象はヘッダで選択中のOAで、保存結果がDBに反映される"
    - "user A（OA-1 owner）は OA-2 のイベント・参加者・回答・OA設定を SELECT できず（0行）、INSERT は拒否され、UPDATE は 0 行になる（成功条件6マトリクス）"
    - "モックログイン（signInWithPassword）の JWT で PostgREST が自OAのみ返す（成功条件1の機械検証）"
    - "register_owner_by_identity は X identity がないユーザーで 0 行・冪等"
  artifacts:
    - path: "admin/tests/integration/rls.test.ts"
      provides: "成功条件6のRLSマトリクス自動テスト"
      min_lines: 60
    - path: "admin/tests/integration/rls.helpers.ts"
      provides: "pooler JWT 切替ハーネス（asUser）+ dev ref ガード"
      contains: "set_config"
    - path: "admin/tests/integration/auth.test.ts"
      provides: "モック認証→RLSスコープのアプリ経路スモーク"
    - path: "admin/app/(app)/oa/settings/page.tsx"
      provides: "OA設定ページ（基本情報・定型文・質問設定の3カード）"
    - path: "admin/lib/schemas/oa.ts"
      provides: "questions JSONB の zod スキーマ（Phase 2 と同形 {id,text,options[]}）"
  key_links:
    - from: "admin/tests/integration/rls.helpers.ts"
      to: "セッションプーラー"
      via: "postgres@3.4.9 + set local role authenticated + request.jwt.claims（実証済みハーネス）"
      pattern: "set local role"
    - from: "admin/lib/actions/oa.ts"
      to: "oa_configs"
      via: "update().select() 返却行数チェック（0行 = 保存失敗）"
      pattern: "\\.select\\("
---

<objective>
OA設定ページ（OA-01: 定型文・質問・管理者Twitter ID）と、本フェーズの成功条件6を証明する RLS マトリクス自動テスト + モック認証スモーク（成功条件1の機械検証）を実装する。

Purpose: 「RLS が唯一の認可境界」という構造をテストでセキュリティ証明に変える（RESEARCH Key insight）。OA-02 の owner/co-owner スコープを機械検証可能にする。
Output: /oa/settings の完成 + tests/integration/{rls,auth}.test.ts green。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-admin-ui/03-RESEARCH.md
@.planning/phases/03-admin-ui/03-UI-SPEC.md
@supabase/seed.sql
</context>

<critical_rules>
- RLSテストは pooler 経由 JWT 切替ハーネス（Locked・RESEARCH 実証済みコードを流用）。接続は aws-1-ap-northeast-1.pooler.supabase.com:5432 / prepare: false / DEV_PROJECT_REF !== 'cmsxvxtcdniqgvhxjqri' なら abort
- 「書けない」のアサーションは **影響行数 0 / データ不変** で書く（UPDATE はエラーにならない — silent-0-row 罠）。INSERT と with check 違反は逆にエラーになる（非対称）
- dev データを変異させるテストは元値復元か専用フィクスチャ行で行う（夜間反復実行に耐える冪等性）
- UI 文言・カード構成は 03-UI-SPEC.md「OA設定」のロック済み契約に従う
</critical_rules>

<tasks>

<task type="auto">
  <name>Task 1: oa zod スキーマ + データ層 + saveOaSettings action（unit テスト先行）</name>
  <files>admin/lib/schemas/oa.ts, admin/lib/data/oa.ts, admin/lib/actions/oa.ts, admin/tests/unit/oa-schema.test.ts</files>
  <read_first>
    - supabase/seed.sql（questions JSONB の実形状 {id,text,options[]}）
    - supabase/migrations/20260611171037_create_core_tables.sql（oa_configs 列: name / line_channel_id / admin_twitter_id / greeting_message / completion_message / questions）
    - admin/lib/data/oa.ts（03-02 作成の listMyOas / resolveSelectedOaId — 温存して拡張）
    - .planning/phases/03-admin-ui/03-RESEARCH.md「Pitfall 4」
  </read_first>
  <behavior>
    - questionSchema: { id: 非空文字列, text: 非空文字列, options: 非空文字列の配列 min 1 } — Phase 2 の質問形状と完全一致。不正形（options 空配列・text 欠落）は reject
    - oaSettingsSchema: { name 非空, admin_twitter_id（カンマ区切り文字列 — 各要素 trim・空要素除去・@ 前置を除去して正規化）, greeting_message / completion_message（nullable 文字列）, questions: questionSchema 配列 }
  </behavior>
  <action>
    1. lib/schemas/oa.ts を behavior どおり作成（zod 4.4.3）。admin_twitter_id の正規化変換（'@a, b ,' → 'a,b'）も transform で実装
    2. tests/unit/oa-schema.test.ts: accept/reject + 正規化ケースを vitest で検証
    3. lib/data/oa.ts に追加: `getOaSettings(supabase, oaConfigId)` / `updateOaSettings(supabase, oaConfigId, input)` — `.select()` 付き update、返却 0 行なら error（保存に失敗しました）
    4. lib/actions/oa.ts（'use server'）: saveOaSettings — zod 再検証 → updateOaSettings → revalidatePath('/oa/settings')
  </action>
  <verify>
    <automated>cd admin && npx vitest run tests/unit/oa-schema.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - oa-schema.test.ts 全 pass（questions 不正形 reject・admin_twitter_id 正規化を含む）
    - updateOaSettings に .select() + 返却行数チェックが存在（ソースアサーション）
  </acceptance_criteria>
  <done>acceptance_criteria の全項目が満たされ、verify の automated コマンドが exit 0 で完走している</done>
</task>

<task type="auto">
  <name>Task 2: OA設定ページ（基本情報・定型文・質問設定の3カード — UI-SPEC 準拠）</name>
  <files>admin/app/(app)/oa/settings/page.tsx, admin/components/oa/oa-settings-form.tsx, admin/components/oa/question-list-editor.tsx</files>
  <read_first>
    - .planning/phases/03-admin-ui/03-UI-SPEC.md「OA設定 (/oa/settings)」「Copywriting Contract」「Typography」「Spacing Scale」
  </read_first>
  <action>
    1. oa/settings/page.tsx（async RSC）: タイトル「OA設定」(20px/600) + 現在OA名 subtitle（ヘッダ OA セレクタ駆動 — cookie の選択 OA を resolveSelectedOaId で解決）。getOaSettings の結果を oa-settings-form に渡す
    2. oa-settings-form.tsx（client）: カード3枚 —
       (a) 基本情報: OA名 input / チャンネルID read-only 表示（monospace 12px/400）/ 管理者TwitterID(s) カンマ区切り input
       (b) 定型文: 最終確認メッセージ冒頭 textarea（greeting_message）/ 完了メッセージ textarea（completion_message）
       (c) 質問設定: question-list-editor
       フッター: 「設定を保存」accent ボタン（desktop 右寄せ / mobile full-width）。成功で alert、失敗（0行含む）で「保存に失敗しました。入力内容を確認してもう一度お試しください」。破壊的操作なし（OA削除は対象外）
    3. question-list-editor.tsx: 質問の順序付きリスト。各項目 = ドラッグハンドル（HTML5 drag&drop で並び替え — UI-SPEC 規定。キーボード代替として上下移動も可能にする）+ 質問 text input + options 編集（項目ごとの選択肢 input 追加/削除）+ 項目の追加/削除。id は新規追加時に `q_` 接頭辞で生成
    4. 保存値は oaSettingsSchema で検証してから saveOaSettings へ
  </action>
  <verify>
    <automated>cd admin && npm run build && grep -q "設定を保存" components/oa/oa-settings-form.tsx && grep -q "completion_message" components/oa/oa-settings-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - build green。3カード構成 + 「設定を保存」+ 完了メッセージ textarea がソースに存在
    - 質問エディタが並び替え・追加・削除・options 編集を持つ（OA-01「質問内容を設定できる」の充足）
  </acceptance_criteria>
  <done>acceptance_criteria の全項目が満たされ、verify の automated コマンドが exit 0 で完走している</done>
</task>

<task type="auto">
  <name>Task 3: RLS マトリクステスト（成功条件6）+ モック認証スモーク（成功条件1）</name>
  <files>admin/tests/integration/rls.helpers.ts, admin/tests/integration/rls.test.ts, admin/tests/integration/auth.test.ts</files>
  <read_first>
    - .planning/phases/03-admin-ui/03-RESEARCH.md「Pattern 5」「Code Examples: RLS テスト」（実証済みハーネスコード — そのまま流用）「Pitfall 4・11」
    - scripts/db/sql.ts（pooler 接続規約: host / postgres.<ref> / パスワード解決 / ref ガード — Node 版に移植）
    - supabase/seed.sql（固定UUID: dev-oa …0001 / dev-oa-2 …0011 / 両OAのイベント・参加者・line_users）
  </read_first>
  <behavior>
    rls.test.ts（pooler ハーネス。user id は oa_members から SQL で動的取得）:
    - 可視性: A(user1) で oa_configs / events / participants / answers / line_users を OA-2 条件で SELECT → 全テーブル 0 行。自 OA 条件 → 1 行以上（**行数を必ず assert** — 0行罠の裏面）
    - INSERT 拒否: A で OA-2 の oa_config_id にイベント INSERT → エラー（with check 違反）。自 OA への INSERT → 成功（テスト内で削除 or トランザクション破棄）
    - UPDATE 0行: A で OA-2 のイベント title UPDATE → returning 0 行・元データ不変。自 OA → 1 行（元値復元）
    - oa_configs UPDATE: A で OA-2 → 0 行 / 自 OA → 1 行（元値復元）
    - 紐付け整合性（ADMIN-02 / T-03-02）: A が自 OA 参加者 …0005 の line_user_id を OA-2 の line_user …0014 に UPDATE → **エラー**（with check 違反は 0 行ではなく raise — 非対称に注意）。自 OA の line_user …0004 への UPDATE → 1 行（元値復元）
    - oa_members: A が自分を OA-2 に INSERT → エラー（ポリシーなし）
    - RPC: asUser(A) で `select * from register_owner_by_identity()` → 0 行（email identity のみ）。2回呼んで oa_members 行数不変（冪等）
    - co-owner スコープ（成功条件6の「co-owner」を実テストで行使）: user2 は dev-oa の co-owner 行を持つ（setup-dev.ts 投入）— asUser(user2) で dev-oa の events SELECT → 1 行以上（owner と同一の可視性）、dev-oa-2（owner として）と dev-oa（co-owner として）の両方が oa_configs SELECT に現れる
    auth.test.ts（supabase-js アプリ経路スモーク）:
    - anon クライアントで dev-owner-1@nomimas.test + MOCK_USER_PASSWORD を signInWithPassword → oa_configs select が name 'dev-oa' の 1 行のみ
    - 同セッションで OA-2 の oa_configs UPDATE → 返却 0 行（エラーなし — silent-0-row の実挙動を退行検知）
  </behavior>
  <action>
    1. rls.helpers.ts: RESEARCH Pattern 5 の asUser をそのまま実装（postgres@3.4.9 / prepare: false / sql.begin 内 set local role + set_config）。接続情報は env.dev（tests/setup.ts がロード済み）から sql.ts と同じ規約で解決し、DEV_PROJECT_REF 不一致なら throw
    2. rls.test.ts / auth.test.ts を behavior どおり実装（RLS_TEST=1 ゲート配下）。変異系は afterEach/finally で元値復元
    3. 全テストを 2 回連続実行して冪等性を確認
  </action>
  <verify>
    <automated>cd admin && RLS_TEST=1 npx vitest run tests/integration/rls.test.ts tests/integration/auth.test.ts && RLS_TEST=1 npx vitest run tests/integration/rls.test.ts tests/integration/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - RLS マトリクス（SELECT 0行 / INSERT エラー / UPDATE 0行 / 紐付け with check エラー / oa_members INSERT エラー / RPC 冪等0行）が全件 pass — **成功条件6の機械検証**
    - auth.test.ts pass — モックログイン JWT で RLS スコープが実効（成功条件1の機械検証）
    - 2 回連続実行で green（dev データ不変・冪等）
  </acceptance_criteria>
  <done>acceptance_criteria の全項目が満たされ、verify の automated コマンドが exit 0 で完走している</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated JWT → PostgREST | OA設定の読み書き（RLS が認可境界） |
| テストハーネス → pooler | postgres ロールでの直接接続（dev 限定必須） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | クロスOAデータアクセス全般 | mitigate | Task 3 のマトリクステストが deny を全テーブル・全操作で証明（成功条件6） |
| T-03-12 | Repudiation | OA設定の 0行 UPDATE 成功偽装 | mitigate | updateOaSettings の .select() 行数チェック（Task 1）+ auth.test.ts で実挙動を退行検知 |
| T-03-14 | Tampering | questions JSONB の不正形保存（Phase 2 Bot の実行時破壊） | mitigate | questionSchema を Phase 2 の {id,text,options[]} 形に固定し server action で再検証（Task 1） |
| T-03-05 | Elevation of Privilege | テストハーネスの prod 誤接続 | mitigate | tests/setup.ts + rls.helpers.ts の二重 ref ガード（cmsxvxtcdniqgvhxjqri 以外 throw） |
</threat_model>

<verification>
- `cd admin && npx vitest run`（unit）green
- `RLS_TEST=1 npx vitest run tests/integration/` green ×2回（冪等）
- `npm run build` green
<human-check>OA設定の実ブラウザでの保存操作・質問ドラッグ並び替えの操作感は HUMAN-UAT</human-check>
</verification>

<success_criteria>
- OA-01 完成: 定型文（greeting + completion）・質問・管理者Twitter ID が選択中 OA に保存できる
- 成功条件6 がテストで証明済み: owner は自 OA のみ閲覧・書込可、他者データへ SELECT/INSERT/UPDATE 不可
- 成功条件1 の機械検証: モック認証 JWT で RLS が同一に機能（プロバイダー差し替えのみで X OAuth を本番化できる構造）
</success_criteria>

<output>
Create `.planning/phases/03-admin-ui/03-04-SUMMARY.md` when done
</output>
