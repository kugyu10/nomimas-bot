---
phase: 03-admin-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260611171037_create_core_tables.sql
  - supabase/migrations/20260611171038_enable_rls.sql
  - supabase/seed.sql
  - scripts/setup-dev.ts
autonomous: true
requirements: [OA-02, AUTH-01, ADMIN-02, OA-01]
user_setup: []

must_haves:
  truths:
    - "owner/co-owner は自OAの行のみ SELECT/INSERT/UPDATE でき、他OAの行は見えず書けない（RLSポリシーがDBに存在する）"
    - "X identity を持つユーザーの初回ログインで oa_members に owner 自動登録できる RPC が存在し、identity なしでは 0 行・冪等"
    - "db reset 後に setup-dev.ts を実行すると、モックユーザー2名 + oa_members（user1→dev-oa, user2→dev-oa-2）が冪等に再現される"
    - "Phase 1-2 の Deno テスト・cron 登録が reset 後も green（回帰なし）"
  artifacts:
    - path: "supabase/migrations/20260611171038_enable_rls.sql"
      provides: "INSERT/UPDATE ポリシー + register_owner_by_identity RPC + (select auth.uid()) 最適化"
      contains: "register_owner_by_identity"
    - path: "supabase/migrations/20260611171037_create_core_tables.sql"
      provides: "IN-05 FK（oa_members.auth_user_id → auth.users）+ oa_configs.completion_message 列"
      contains: "references auth.users"
    - path: "supabase/seed.sql"
      provides: "dev-oa-2 + 配下フィクスチャ（RLSマトリクステスト用）+ answers 1行"
      contains: "dev-oa-2"
    - path: "scripts/setup-dev.ts"
      provides: "モックユーザー2名の冪等作成 + oa_members 投入"
  key_links:
    - from: "scripts/setup-dev.ts"
      to: "auth admin API (GoTrue)"
      via: "SUPABASE_SERVICE_ROLE_KEY での admin/users 作成（スクリプト領域 — admin/ アプリには置かない）"
      pattern: "admin/users|createUser"
    - from: "supabase/migrations/20260611171038_enable_rls.sql"
      to: "auth.identities"
      via: "SECURITY DEFINER RPC（user_metadata 不使用）"
      pattern: "auth\\.identities"
---

<objective>
Phase 3 のDB基盤を完成させる: RLS を本実装（owner/co-owner の SELECT/INSERT/UPDATE + with check）に置き換え、owner 自動登録 RPC（auth.identities ベース・偽装不能）、IN-05 FK、UI-SPEC が要求する completion_message 列、RLSテスト用 2nd OA フィクスチャ、モックユーザーを投入し、[BLOCKING] db reset で dev に再適用する。

Purpose: 成功条件6（OAスコープのRLS強制）と成功条件1（モック認証→RLS実効）のDB側前提をすべて整える。admin/ に依存しないため Wave 1 単独で完結する。
Output: 更新済みマイグレーション2本・seed.sql・setup-dev.ts、dev に再適用済みのスキーマ + モックユーザー。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-admin-ui/03-CONTEXT.md
@.planning/phases/03-admin-ui/03-RESEARCH.md
</context>

<critical_rules>
- **dev のみ**: ref == `cmsxvxtcdniqgvhxjqri` 以外への接続・reset は絶対禁止。env は `set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a`
- **public リポジトリ**: シークレット値をコミットしない。MOCK_USER_PASSWORD は env.dev（gitignored）にのみ置く
- スキーマ変更は既存マイグレーションの in-place 編集（新規マイグレーションファイルを作らない — 確立パターン）
</critical_rules>

<tasks>

<task type="auto">
  <name>Task 1: マイグレーション in-place 拡張（IN-05 FK + completion_message + RLS書込ポリシー + owner登録RPC）</name>
  <files>supabase/migrations/20260611171037_create_core_tables.sql, supabase/migrations/20260611171038_enable_rls.sql</files>
  <read_first>
    - supabase/migrations/20260611171037_create_core_tables.sql（oa_members / oa_configs 定義）
    - supabase/migrations/20260611171038_enable_rls.sql（既存SELECTポリシー7本 — join チェーンを温存）
    - .planning/phases/03-admin-ui/03-RESEARCH.md「Pattern 3」（register_owner_by_identity の完全SQL）「Pattern 4」（participants UPDATE の with check 完全SQL・設計原則）
  </read_first>
  <action>
    20260611171037_create_core_tables.sql を in-place 編集:
    1. IN-05: oa_members.auth_user_id を `uuid not null references auth.users(id) on delete cascade` に変更（CONTEXT deferred「対応可能なら対応」→ RESEARCH Pattern 4 が対応推奨。seed.sql に oa_members を入れない前提とセット）。
    2. oa_configs に `completion_message text` 列を追加（UI-SPEC「定型文」セクションの完了メッセージ textarea の保存先 — OA-01。Bot 側の完了文面は Phase 2 D-08 の既存仕様のままで本フェーズの対象外。greeting_message と同様、UI からの設定保存が本フェーズの成果物）。

    20260611171038_enable_rls.sql を in-place 拡張:
    3. 既存 SELECT ポリシー 7 本の `auth.uid()` をすべて `(select auth.uid())` に書き換え（initplan 最適化 — RESEARCH Pattern 4 設計原則）。join チェーンは変更しない。
    4. 書込ポリシーを追加（ポリシー名は `<table>_oa_member_<op>` で統一、to authenticated、using と with check は同形の oa_members exists チェーン）:
       - `oa_configs_oa_member_update`（for update）
       - `events_oa_member_insert`（for insert, with check: oa_config_id が自分の oa_members に存在）
       - `events_oa_member_update`（for update）
       - `event_platform_urls_oa_member_insert`（for insert, with check: events join 経由）— DELETE/UPDATE ポリシーは作らない（v1 は URL 追加のみ — RESEARCH Open Question 2 の判断。UI-SPEC も「+URL追加」のみ規定）
       - `participants_oa_member_update`（RESEARCH Pattern 4 の完全SQLどおり: using = 自OAチェーン、with check = 自OAチェーン AND（line_user_id is null OR 紐付け先 line_users.oa_config_id が同一OA）— ADMIN-02 の紐付け整合性ガード）
       - answers / line_users / oa_members には書込ポリシーを追加しない（answers/line_users への書込は service role の Edge Functions のみ。oa_members への登録経路は RPC のみ — 権限昇格防止）
       - DELETE ポリシーは一切作らない（deny-by-default 維持。Phase 3 UI に削除操作なし）
    5. `register_owner_by_identity()` RPC を追加（RESEARCH Pattern 3 の完全SQLを基に）: SECURITY DEFINER / set search_path = '' / auth.identities から provider in ('x','twitter') の identity_data を読み screen_name は `coalesce(identity_data->>'user_name', identity_data->>'preferred_username')`（仮定A1の両キー対応）/ oa_configs.admin_twitter_id の string_to_array(…, ',') と照合 / oa_members へ on conflict do nothing で owner INSERT / returns setof uuid / `revoke all on function … from public, anon; grant execute … to authenticated;`
    user_metadata は一切参照しない（偽装可能 — T-03-01）。
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260611171038_enable_rls.sql | grep -c "create policy" | grep -qx 12 && grep -q "references auth.users" supabase/migrations/20260611171037_create_core_tables.sql && grep -q "completion_message" supabase/migrations/20260611171037_create_core_tables.sql && grep -q "security definer" supabase/migrations/20260611171038_enable_rls.sql && ! grep -v '^--' supabase/migrations/20260611171038_enable_rls.sql | grep -q "user_metadata"</automated>
  </verify>
  <acceptance_criteria>
    - create policy が計12本（既存SELECT 7 + UPDATE 3 + INSERT 2）。answers/line_users/oa_members の書込ポリシーと DELETE ポリシーが存在しない
    - 非コメント行に `auth.uid()` の裸呼び（`(select auth.uid())` 以外）が存在しない
    - register_owner_by_identity が security definer + search_path='' + revoke/grant 付きで定義され、auth.identities のみを参照する（user_metadata 文字列が非コメント行に存在しない）
    - oa_members.auth_user_id に auth.users への FK（on delete cascade）が付く
  </acceptance_criteria>
  <done>acceptance_criteria の全項目が満たされ、verify の automated コマンドが exit 0 で完走している</done>
</task>

<task type="auto">
  <name>Task 2: seed.sql に dev-oa-2 フィクスチャ追加 + setup-dev.ts をモックユーザー対応に拡張</name>
  <files>supabase/seed.sql, scripts/setup-dev.ts</files>
  <read_first>
    - supabase/seed.sql（既存の固定UUID・on conflict do nothing パターン）
    - scripts/setup-dev.ts（既存の env チェック・connectDev・冪等パターン）
    - scripts/db/sql.ts（pooler ヘルパー・ref ガード）
    - .planning/phases/03-admin-ui/03-RESEARCH.md「Pitfall 6」「Pitfall 11」「seed.sql 拡張」節
  </read_first>
  <action>
    seed.sql に追記（既存行は変更しない。すべて固定UUID + on conflict (id) do nothing）:
    - oa_configs: id `…0011` / name 'dev-oa-2' / admin_twitter_id null / questions は dev-oa と同形の JSONB（1問以上）
    - events: id `…0012` / oa_config_id `…0011` / title 'dev-event-2' / event_date current_date + 5
    - event_platform_urls: id `…0013` / event_id `…0012` / platform 'twipla' / url 'https://twipla.jp/events/999999'（url は unique 制約 — 既存 731057 と非衝突）
    - line_users: id `…0014` / oa_config_id `…0011` / line_user_id 'U00000000000000000000000000000oa2'
    - participants: id `…0015` / event_platform_url_id `…0013` / display_name 'oa2テスト参加者' / natural_key 'dn:oa2テスト参加者' / status 'attending' / confirm_status 'pending'
    - answers: id `…0006` / participant_id `…0005`（既存 dev-oa 側参加者）/ question_key 'q_age' / answer '20歳以上です'（ADMIN-01 統合テストのフィクスチャ。answers の既存列名は 20260611171037 の定義を実読して合わせる）
    - **oa_members は seed.sql に入れない**（IN-05 FK のため auth.users 不在の seed 時点では入れられない — RESEARCH Anti-Pattern）

    scripts/setup-dev.ts を拡張（既存の Vault 投入 + line_channel_id 更新はそのまま温存）:
    1. 追加の必須 env: `MOCK_USER_PASSWORD`（なければ「env.dev に MOCK_USER_PASSWORD=<ランダム値> を追記してください」と明示して abort）。実行前に env.dev に同変数がない場合、エグゼキュータは `openssl rand -base64 18` 等で生成した値をローカルの env.dev（gitignored）へ追記してから実行する
    2. モックユーザー2名を冪等作成: `dev-owner-1@nomimas.test`（user_metadata.user_name='dev_owner_x'）と `dev-owner-2@nomimas.test`。GoTrue admin REST（`${SUPABASE_URL}/auth/v1/admin/users` に apikey + Authorization: Bearer SUPABASE_SERVICE_ROLE_KEY、email_confirm: true）を fetch で直接叩く（新規依存を増やさない）。冪等化: 422/email_exists 時は GET `/auth/v1/admin/users?email=…` で既存 id を取得
    3. 取得した user id で oa_members を pooler SQL で投入: user1→oa_config `…0001` role 'owner'、user2→oa_config `…0011` role 'owner'、`on conflict (oa_config_id, auth_user_id) do nothing`
    4. service role キーは env.dev からの読込のみ（コードへの値の埋め込み・ログ出力禁止）。docstring の「実行内容」を更新
  </action>
  <verify>
    <automated>grep -c "do nothing" supabase/seed.sql | awk '$1>=11' && grep -q "dev-oa-2" supabase/seed.sql && grep -q "MOCK_USER_PASSWORD" scripts/setup-dev.ts && grep -q "oa_members" scripts/setup-dev.ts && ! grep -rn "oa_members" supabase/seed.sql && deno check scripts/setup-dev.ts</automated>
  </verify>
  <acceptance_criteria>
    - seed.sql: dev-oa-2 系 5 行 + answers 1 行が固定UUID・冪等で追加され、oa_members 行が seed.sql に存在しない
    - setup-dev.ts が `deno check` を通り、MOCK_USER_PASSWORD / SUPABASE_SERVICE_ROLE_KEY 欠如時に明示メッセージで exit 1 する
    - モックユーザー作成が冪等（2回実行してもエラーにならない設計 — Task 3 で実証）
  </acceptance_criteria>
  <done>acceptance_criteria の全項目が満たされ、verify の automated コマンドが exit 0 で完走している</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] dev スキーマ再適用 + セットアップ + 回帰検証</name>
  <files>（ファイル変更なし — dev 環境への適用と検証）</files>
  <read_first>
    - scripts/verify-cron.ts（cron 再登録検証の既存手順）
    - docs/NIGHT-RUN.md（夜間実行時の確立フロー）
  </read_first>
  <action>
    確立パターンどおりの順序で実行する（すべて `set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a` 済みシェルで）:
    1. linked ref 検証: `supabase migration list --linked` 等の出力または supabase/config.toml で対象が `cmsxvxtcdniqgvhxjqri` であることを確認。不一致なら即 abort（prod 安全弁）
    2. `supabase db reset --linked --yes`
    3. `deno run --allow-net --allow-read --allow-env scripts/setup-dev.ts`（Vault + channel_id + モックユーザー2名 + oa_members）— **2回連続実行して冪等性も確認**
    4. `deno run --allow-net --allow-read --allow-env scripts/verify-cron.ts`（cron はマイグレーションが再作成 — Phase 2 確立）
    5. Deno 回帰: `deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/`
    6. pooler SQL でスキーマ実在確認: pg_policies に新ポリシー（events_oa_member_insert / participants_oa_member_update / oa_configs_oa_member_update）が存在、pg_proc に register_owner_by_identity が存在、oa_configs に completion_message 列が存在、oa_members に 2 行（user1/user2）、auth.users に 2 ユーザー、seed の dev-oa-2 行が存在
  </action>
  <verify>
    <automated>deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/ && deno run --allow-net --allow-read --allow-env scripts/verify-cron.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db reset --linked --yes` が成功（dev ref 検証済みの上で）
    - setup-dev.ts が 2 回連続で exit 0（冪等）
    - verify-cron.ts が cron 登録を確認して exit 0
    - Deno 既存テストスイートが全件 pass（Phase 1-2 回帰なし）
    - pg_policies / pg_proc / information_schema.columns の SQL 確認がすべて期待どおり（SUMMARY に確認結果を記録）
  </acceptance_criteria>
  <done>acceptance_criteria の全項目が満たされ、verify の automated コマンドが exit 0 で完走している</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated JWT → PostgREST | 管理画面の全データアクセスがここを通る（RLS が唯一の認可境界） |
| 初回ログインユーザー → oa_members 登録 | 未登録ユーザーの自己申告が owner 権限に変換される地点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | register_owner_by_identity | mitigate | auth.identities（プロバイダー由来・書換不可）のみ参照。user_metadata 不使用。oa_members への INSERT ポリシーを authenticated に与えない（Task 1） |
| T-03-02 | Tampering | participants UPDATE（紐付け） | mitigate | with check で紐付け先 line_users.oa_config_id の同一OA一致を強制（Task 1） |
| T-03-03 | Tampering | 全テーブル RLS | mitigate | INSERT/UPDATE の with check を using と同形にし書込先OAを検証。マトリクス検証は 03-04 |
| T-03-04 | Information Disclosure | MOCK_USER_PASSWORD / service_role | mitigate | env.dev（gitignored）のみ。コード・ログへの値出力禁止（Task 2） |
| T-03-05 | Elevation of Privilege | prod 誤接続 | mitigate | linked ref == cmsxvxtcdniqgvhxjqri を Task 3 冒頭で検証。connectDev の ref ガード（既存） |
</threat_model>

<verification>
- `supabase db reset --linked --yes` → setup-dev.ts（×2 冪等）→ verify-cron.ts → Deno 回帰の全パイプライン green
- pg_policies = 12 本 / register_owner_by_identity 存在 / oa_members 2 行 / dev-oa-2 フィクスチャ存在
- `git status` にシークレット値を含むファイルが現れない（env.dev は gitignored のまま）
</verification>

<success_criteria>
- RLS 本実装 + RPC + IN-05 + completion_message が dev に適用済みで、Phase 1-2 の全テスト・cron が回帰なし
- モックユーザー2名 + oa_members が setup-dev.ts で冪等再現できる（成功条件1・6 のDB前提完成）
</success_criteria>

<output>
Create `.planning/phases/03-admin-ui/03-01-SUMMARY.md` when done
</output>
