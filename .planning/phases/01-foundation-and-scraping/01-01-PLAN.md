---
phase: 01-foundation-and-scraping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/functions/deno.json
  - supabase/migrations/*_create_core_tables.sql
  - supabase/migrations/*_enable_rls.sql
autonomous: true
requirements: [EVENT-02]

must_haves:
  truths:
    - "supabase db reset --db-url（dev直結・非対話）が成功し、6必須テーブル + oa_members がゼロから再現される"
    - "全publicテーブルでRLSが有効で、6必須テーブルすべてに1件以上のポリシーが存在する"
    - "env.dev / env.prod は git にコミットされておらず、git check-ignore で除外が確認できる"
    - "supabase/ 配下のコードに prod ref（hgojtooexbknqotzkkja）が一切登場しない"
  artifacts:
    - path: "supabase/config.toml"
      provides: "Supabase CLI設定 + webhook関数のverify_jwt無効化"
      contains: "[functions.webhook]"
    - path: "supabase/functions/deno.json"
      provides: "依存バージョン固定（cheerio/zod/supabase-js）"
      contains: "npm:cheerio@1.2.0"
    - path: "supabase/migrations/"
      provides: "コアテーブル + RLSのマイグレーションSQL（2ファイル）"
  key_links:
    - from: "supabase/migrations/*_create_core_tables.sql"
      to: "dev Postgres (cmsxvxtcdniqgvhxjqri)"
      via: "supabase db reset --db-url $SUPABASE_DIRECT_CONNECTION_STRING --yes"
      pattern: "create table.*participants"
    - from: "supabase/migrations/*_enable_rls.sql"
      to: "pg_policies"
      via: "enable row level security + create policy"
      pattern: "enable row level security"
---

<objective>
モノリポ（Supabase標準レイアウト）を初期化し、本番スキーマ6テーブル + oa_members のマイグレーションを作成して、リモートdev DB（cmsxvxtcdniqgvhxjqri）にゼロから再現できることを機械検証する。Phase 1成功条件2（スキーマ再現）・5（シークレット非コミット）を充足し、Wave 2の2プラン（scraper / webhook）が衝突なく並行作業できる土台（config.toml / deno.json）を先に確定する。

Purpose: 以降の全フェーズが乗るDB・設定の土台。dev限定・非対話・夜間無人実行可能な再現手順の確立。
Output: supabase/config.toml, supabase/functions/deno.json, マイグレーション2ファイル, dev DBへの適用済みスキーマ
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-scraping/01-RESEARCH.md
@docs.md
</context>

<critical_constraints>
- 触ってよいのは dev プロジェクト `cmsxvxtcdniqgvhxjqri` のみ。prod ref `hgojtooexbknqotzkkja` をコマンド・コード・スクリプトに一切書かない。
- `supabase link` は使用禁止（対話的・リンク先誤りでprod破壊リスク）。DB操作は毎回 `--db-url "$SUPABASE_DIRECT_CONNECTION_STRING"` を明示する。
- env変数は `set -a; source env.dev; set +a` で読み込む。値をコマンドラインに直書きしない。
- リポジトリはpublic。シークレット・接続文字列をファイル・コミットメッセージに書かない。
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: supabase init + config.toml + deno.json + シークレット衛生検証</name>
  <files>supabase/config.toml, supabase/functions/deno.json</files>
  <read_first>
    - .gitignore（現在の除外設定 — env.dev/env.prod/.env*/*.key/*.pem が除外済みであることを確認）
    - .env.example（env変数名の正: SUPABASE_DIRECT_CONNECTION_STRING / DEV_PROJECT_REF / LINE_CHANNEL_ID / LINE_CHANNEL_SECRET 等）
    - .planning/phases/01-foundation-and-scraping/01-RESEARCH.md の「Recommended Project Structure」「Pattern 3」「Common Pitfalls 4-6」セクション
  </read_first>
  <action>
    リポジトリルートで `supabase init --yes` を実行し supabase/config.toml を生成する（supabase/.temp/ は既存だが init は通る。対話プロンプト回避のため --yes 必須）。生成後、config.toml 末尾に webhook関数のJWT検証無効化セクションを追記する: `[functions.webhook]` / `verify_jwt = false`（LINEプラットフォームはSupabase JWTを送れないため。Wave 2のPlan 03がこの設定に依存する — ここで先に作ることでconfig.tomlの並行編集衝突を防ぐ）。

    supabase/functions/deno.json を新規作成し、imports にバージョン固定の3依存を定義する: `"cheerio": "npm:cheerio@1.2.0"`, `"zod": "npm:zod@4.4.3"`, `"@supabase/supabase-js": "npm:@supabase/supabase-js@2.108.1"`（RESEARCH.mdのPackage Legitimacy Auditで全件Approved済み。pipで同名パッケージを入れないこと — 依存はすべてnpm:指定子でDenoが解決し、npm install自体が不要）。

    シークレット衛生を機械検証する: (1) `git check-ignore env.dev env.prod` が両方を出力すること、(2) `git ls-files | grep -E '^env\.(dev|prod)$'` が空であること、(3) `git ls-files | grep -E '\.(pem|key)$'` が空であること。config.toml に project_id 等の値が入る場合、dev ref（cmsxvxtcdniqgvhxjqri）は公開可（anonキー同様に秘密ではない）だが、接続文字列・キー類が含まれないことを目視+grepで確認する（`grep -iE 'service_role|postgres://' supabase/config.toml` が空）。
  </action>
  <verify>
    <automated>test -f supabase/config.toml && grep -A1 '\[functions.webhook\]' supabase/config.toml | grep -c 'verify_jwt = false' && deno eval 'const j = JSON.parse(Deno.readTextFileSync("supabase/functions/deno.json")); if (j.imports["cheerio"] !== "npm:cheerio@1.2.0" || j.imports["zod"] !== "npm:zod@4.4.3") throw new Error("imports mismatch"); console.log("deno.json OK")' && git check-ignore env.dev env.prod && [ -z "$(git ls-files | grep -E '^env\.(dev|prod)$')" ] && echo HYGIENE_OK</automated>
  </verify>
  <acceptance_criteria>
    - supabase/config.toml が存在し、`[functions.webhook]` セクション直下に `verify_jwt = false` がある
    - supabase/functions/deno.json の imports が cheerio→npm:cheerio@1.2.0 / zod→npm:zod@4.4.3 / @supabase/supabase-js→npm:@supabase/supabase-js@2.108.1 と完全一致する
    - `git check-ignore env.dev env.prod` が exit 0 で両ファイル名を出力する
    - `git ls-files | grep -E '^env\.(dev|prod)$'` の出力が空
    - `grep -iE 'service_role|postgres://' supabase/config.toml` の出力が空
  </acceptance_criteria>
  <done>config.toml（verify_jwt=false付き）と deno.json（3依存固定）が存在し、シークレット衛生チェック3点がすべてパスする</done>
</task>

<task type="auto">
  <name>Task 2: コアテーブル + RLS のマイグレーション作成（7テーブル）</name>
  <files>supabase/migrations/&lt;ts&gt;_create_core_tables.sql, supabase/migrations/&lt;ts&gt;_enable_rls.sql</files>
  <read_first>
    - .planning/phases/01-foundation-and-scraping/01-RESEARCH.md の「DBスキーマ設計ガイド」セクション（リレーション推奨・RLS方針）
    - docs.md の §2（用語・権限モデル root/owner/co-owner）と §9（処理フロー — テーブル間の関係の根拠）
    - supabase/config.toml（Task 1で生成済みであること）
  </read_first>
  <action>
    `supabase migration new create_core_tables` と `supabase migration new enable_rls` で2ファイルを生成し、以下のSQLを書く（migration newはタイムスタンプ付きファイル名を supabase/migrations/ に作るだけでDBには触れない）。

    **1本目（create_core_tables）— 7テーブル。** 全テーブル `id uuid primary key default gen_random_uuid()` と `created_at timestamptz not null default now()` を持つ:
    - `oa_configs`: name text not null, line_channel_id text, admin_twitter_id text, greeting_message text, updated_at timestamptz not null default now()
    - `oa_members`: oa_config_id uuid not null references oa_configs(id) on delete cascade, auth_user_id uuid not null, role text not null check (role in ('owner','co-owner')), unique(oa_config_id, auth_user_id) — root/owner/co-ownerのRLSをPhase 3/4で書くための土台（rootはauth.users側のapp_metadataで表現予定）
    - `events`: oa_config_id uuid not null references oa_configs(id) on delete cascade, title text not null, event_date date, meeting_at timestamptz, meeting_place text, fee text, venue_info text, confirm_days_before integer not null default 3, updated_at timestamptz not null default now()
    - `event_platform_urls`: event_id uuid not null references events(id) on delete cascade, platform text not null, url text not null unique
    - `line_users`: oa_config_id uuid references oa_configs(id) on delete set null, line_user_id text not null unique, display_name text, followed_at timestamptz
    - `participants`: event_platform_url_id uuid not null references event_platform_urls(id) on delete cascade, display_name text not null, screen_name text, profile_url text, status text not null check (status in ('attending','interested','declined','unknown')), source_platform text not null default 'twipla', line_user_id uuid references line_users(id) on delete set null, confirm_status text not null default 'pending', scraped_at timestamptz, updated_at timestamptz not null default now(), unique(event_platform_url_id, display_name) — このunique制約がPlan 02のscraper upsertのonConflictターゲットになる。line_user_id がnullableなのはADMIN-02の手動紐付け（Phase 3）のため
    - `answers`: participant_id uuid not null references participants(id) on delete cascade, question_key text not null, question_text text, answer text, answered_at timestamptz not null default now(), unique(participant_id, question_key) — Phase 2で書き込み。スキーマのみ先行

    **2本目（enable_rls）:** 7テーブル全部に `alter table public.&lt;t&gt; enable row level security;` を発行し、deny-by-defaultの上に最小SELECTポリシーを各テーブル1本ずつ定義する。ポリシー名は `&lt;table&gt;_oa_member_select` で統一。判定はすべて oa_members 経由: oa_configs は `exists (select 1 from oa_members m where m.oa_config_id = oa_configs.id and m.auth_user_id = auth.uid())`、oa_members は `auth_user_id = auth.uid()`、events / line_users は自テーブルの oa_config_id 経由、event_platform_urls は events をjoin、participants は event_platform_urls→events をjoin、answers は participants→event_platform_urls→events をjoinして同条件。to authenticated を指定。INSERT/UPDATE/DELETEポリシーはPhase 1では作らない（Edge Functionsはservice roleでRLSをバイパスするため動作に影響しない。実認証との突き合わせはPhase 3）。

    SQLにシークレット・接続文字列・prod refを書かないこと。
  </action>
  <verify>
    <automated>ls supabase/migrations/*_create_core_tables.sql supabase/migrations/*_enable_rls.sql && grep -c 'create table' supabase/migrations/*_create_core_tables.sql && [ "$(grep -c 'enable row level security' supabase/migrations/*_enable_rls.sql)" -eq 7 ] && [ "$(grep -c 'create policy' supabase/migrations/*_enable_rls.sql)" -eq 7 ] && [ -z "$(grep -rl 'hgojtooexbknqotzkkja' supabase/)" ] && echo MIGRATIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/ にタイムスタンプ付きの create_core_tables / enable_rls の2ファイルが存在する
    - create_core_tables に `create table` が7回現れ、テーブル名は oa_configs / oa_members / events / event_platform_urls / line_users / participants / answers
    - participants に `unique` 制約 (event_platform_url_id, display_name) と status の check制約（attending/interested/declined/unknown）が含まれる
    - enable_rls に `enable row level security` が7回、`create policy` が7回現れる
    - `grep -rl 'hgojtooexbknqotzkkja' supabase/` の出力が空（prod ref不在）
  </acceptance_criteria>
  <done>7テーブルのDDLとRLS有効化+各テーブル1ポリシーのマイグレーション2ファイルが存在し、prod refを含まない</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] リモートdevへのスキーマ適用 + 機械検証（成功条件2）</name>
  <files>(なし — リモートdev DBの状態変更のみ)</files>
  <read_first>
    - supabase/migrations/ の2ファイル（Task 2の成果物 — 適用対象の最終確認）
    - .planning/phases/01-foundation-and-scraping/01-RESEARCH.md の「Common Pitfalls 4・5」（Docker不要化・db reset対象取り違え）と「DBスキーマ・RLS存在の機械検証」セクション
  </read_first>
  <action>
    env.devを読み込み（`set -a; source env.dev; set +a`）、リモートdevに対して `supabase db reset --db-url "$SUPABASE_DIRECT_CONNECTION_STRING" --yes` を実行する。引数なし（ローカル/Docker対象）・`--linked` は使用禁止。実行前に接続文字列のホスト部にdev ref（cmsxvxtcdniqgvhxjqri）が含まれることをシェルでアサートする（`case "$SUPABASE_DIRECT_CONNECTION_STRING" in *cmsxvxtcdniqgvhxjqri*) ;; *) exit 1;; esac` 相当。値そのものはechoしない）。

    適用後の検証はpsqlで行うが、psqlは未インストール（環境調査済み）。`command -v psql` が空なら `brew install libpq` を実行し、`PSQL="$(brew --prefix libpq)/bin/psql"` を使う（libpqはkeg-onlyでPATHに入らないためフルパス参照。非対話・brew既存環境）。

    検証SQL（RESEARCH.md記載のものをそのまま使用）:
    1. information_schema.tables で public スキーマの ('events','event_platform_urls','participants','line_users','oa_configs','answers') の count = 6
    2. pg_policies で public の全6必須テーブルにポリシー1件以上
    3. pg_tables × pg_class.relrowsecurity で RLS未有効のpublicテーブルが0行
    検証クエリの実行は `$PSQL "$SUPABASE_DIRECT_CONNECTION_STRING" -tAc "&lt;sql&gt;"` 形式（-tAcで機械可読出力）。
  </action>
  <verify>
    <automated>set -a; source env.dev; set +a; PSQL=$(command -v psql || echo "$(brew --prefix libpq)/bin/psql"); [ "$("$PSQL" "$SUPABASE_DIRECT_CONNECTION_STRING" -tAc "select count(*) from information_schema.tables where table_schema='public' and table_name in ('events','event_platform_urls','participants','line_users','oa_configs','answers')")" = "6" ] && [ "$("$PSQL" "$SUPABASE_DIRECT_CONNECTION_STRING" -tAc "select count(distinct tablename) from pg_policies where schemaname='public' and tablename in ('events','event_platform_urls','participants','line_users','oa_configs','answers')")" = "6" ] && [ -z "$("$PSQL" "$SUPABASE_DIRECT_CONNECTION_STRING" -tAc "select t.tablename from pg_tables t join pg_class c on c.relname=t.tablename where t.schemaname='public' and not c.relrowsecurity")" ] && echo SCHEMA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db reset --db-url "$SUPABASE_DIRECT_CONNECTION_STRING" --yes` が exit 0（Docker不要でリモートdevに適用）
    - 必須6テーブルの information_schema.tables カウントが 6
    - pg_policies 上で必須6テーブルすべてにポリシーが存在（distinct tablename = 6）
    - RLS未有効のpublicテーブルが0行
    - 実行したコマンド履歴・出力に prod ref / シークレット値が現れない
  </acceptance_criteria>
  <done>db resetがリモートdevで成功し、6テーブル存在・全テーブルRLS有効・全6テーブルにポリシー存在がSQLで機械検証された（Phase 1成功条件2の充足）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| リポジトリ(public) → 全世界 | コミットされた内容はすべて公開される |
| ローカルCLI → Supabase dev/prod | 接続文字列・project-refの指定ミスで誤環境を破壊しうる |
| anon/authenticated クライアント → Postgres | RLSが唯一のデータ境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | env.dev / env.prod / config.toml | mitigate | git check-ignore + git ls-files 検査をTask 1の検証に組込み。config.tomlのservice_role/接続文字列grepが空であること |
| T-01-02 | Elevation of Privilege | supabase db reset の対象DB | mitigate | link禁止・--db-url毎回明示・接続文字列にdev ref含有をアサート・`grep -rl hgojtooexbknqotzkkja supabase/` 空をTask 2検証に組込み |
| T-01-03 | Tampering / EoP | RLS未設定テーブル | mitigate | enable_rlsマイグレーションで全7テーブル有効化 + pg_class.relrowsecurity全件trueをTask 3で機械検証（deny-by-default、SELECTのみ最小許可） |
| T-01-04 | Information Disclosure | シェル履歴・コマンド出力 | mitigate | 接続文字列・キーをecho/直書きしない。env展開のみで使用 |
| T-01-SC | Tampering | npm依存（deno.json imports） | mitigate | RESEARCH.md Package Legitimacy Auditで cheerio/zod/supabase-js 全件[OK]/Approved済み。deno.jsonでバージョン固定。pipの同名偽パッケージ（cheerio/zod）を使わない |
</threat_model>

<verification>
- `supabase db reset --db-url "$SUPABASE_DIRECT_CONNECTION_STRING" --yes` がexit 0（ゼロから再現 = 成功条件2）
- 検証SQL3本（テーブル数6 / ポリシー有6テーブル / RLS未有効0行）がすべて期待値
- `git check-ignore env.dev env.prod` 成功 + `git ls-files | grep -E '^env\.(dev|prod)$'` 空（成功条件5）
- `grep -rl 'hgojtooexbknqotzkkja' supabase/` 空
</verification>

<success_criteria>
- Phase 1成功条件2: スキーマ（6テーブル + RLS）がマイグレーションでゼロから再現できる — 機械検証済み
- Phase 1成功条件5: env.dev/env.prodが非コミット — 機械検証済み
- Wave 2の両プランが前提とする config.toml（verify_jwt=false）/ deno.json（依存固定）/ devスキーマ が確定済み
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-scraping/01-01-SUMMARY.md` when done
</output>
