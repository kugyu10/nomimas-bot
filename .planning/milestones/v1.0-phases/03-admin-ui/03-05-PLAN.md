---
phase: 03-admin-ui
plan: 05
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified:
  - admin/lib/answer-status.ts
  - admin/lib/data/participants.ts
  - admin/lib/actions/linking.ts
  - admin/app/(app)/events/[id]/page.tsx
  - admin/components/events/answer-status-tab.tsx
  - admin/components/events/linking-tab.tsx
  - admin/tests/unit/answer-status.test.ts
  - admin/tests/integration/data.test.ts
autonomous: true
requirements: [ADMIN-01, ADMIN-02]
user_setup: []

must_haves:
  truths:
    - "イベント詳細の回答状況タブで、参加者ごとに Q1..Qn の回答と全体ステータス（未配信/配信済み/回答中/回答済み）が一覧できる"
    - "紐付けタブで未紐付け参加者に LINE 友だちをコンボボックスで割り当てて紐付けでき、結果が participants.line_user_id に反映される"
    - "紐付け済み参加者は確認ダイアログを経て解除でき、解除結果がDBに反映される"
    - "フェーズ全体の機械検証（unit + RLS統合 + build + Deno回帰 + シークレットgrep）が一括で green"
  artifacts:
    - path: "admin/lib/answer-status.ts"
      provides: "回答状況合成の純関数（answers × questions → Q列 + 全体ステータス）"
      exports: ["buildAnswerStatusRows"]
    - path: "admin/components/events/linking-tab.tsx"
      provides: "未紐付け×LINE友だちコンボボックスの1対1割当・解除UI"
    - path: "admin/lib/actions/linking.ts"
      provides: "linkParticipant / unlinkParticipant server actions"
      exports: ["linkParticipant", "unlinkParticipant"]
    - path: "admin/tests/integration/data.test.ts"
      provides: "ネスト埋め込みクエリ（participants×answers×line_users）の統合検証"
  key_links:
    - from: "admin/lib/data/participants.ts"
      to: "PostgREST participants"
      via: "ネスト埋め込み 1 クエリ（Pattern 7）+ user JWT"
      pattern: "answers\\("
    - from: "admin/lib/actions/linking.ts"
      to: "participants.line_user_id"
      via: "update().select() 行数チェック（with check 拒否は raise — UI でエラー表示）"
      pattern: "line_user_id"
    - from: "admin/components/events/answer-status-tab.tsx"
      to: "admin/lib/answer-status.ts"
      via: "純関数の合成結果を Table + Badge で描画"
      pattern: "buildAnswerStatusRows"
---

<objective>
イベント詳細の残り2タブを完成させる: 回答状況一覧（ADMIN-01）と LINE ユーザー × Twipla 参加者の手動紐付け UI（ADMIN-02）。最後にフェーズゲート（全テスト + build + 回帰 + シークレット検査）を一括実行する。

Purpose: 成功条件3（回答状況一覧）・成功条件4（手動紐付けがDBに反映）の実現と、Phase 3 全体の機械検証完了。
Output: /events/[id] の3タブ完成 + フェーズ全体の検証パイプライン green。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-admin-ui/03-RESEARCH.md
@.planning/phases/03-admin-ui/03-UI-SPEC.md
@.planning/phases/03-admin-ui/03-VALIDATION.md
</context>

<critical_rules>
- データアクセスは user JWT クライアントのみ。UPDATE は `.select()` 行数チェック（silent-0-row 罠）
- 紐付け解除は AlertDialog 確認必須（UI-SPEC: 「紐付けを解除しますか？」/「紐付けを解除」/「解除しない」）
- バッジ色・文言は UI-SPEC「Status Badge Reference」の className マッピングをそのまま使用
- 楽観的更新は失敗時に必ず巻き戻す（UI-SPEC Interaction Contract）
</critical_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 回答状況合成の純関数 + participants データ層 + 統合テスト</name>
  <files>admin/lib/answer-status.ts, admin/lib/data/participants.ts, admin/tests/unit/answer-status.test.ts, admin/tests/integration/data.test.ts</files>
  <read_first>
    - .planning/phases/03-admin-ui/03-RESEARCH.md「Pattern 7」（ネスト埋め込みクエリ実コード）
    - supabase/seed.sql（questions {id,text,options[]} / answers フィクスチャ（…0006: participant …0005 が q_age に回答済み））
    - supabase/migrations/20260611171037_create_core_tables.sql（participants / answers 列定義・confirm_status の取り得る値）
  </read_first>
  <behavior>
    buildAnswerStatusRows(participants, questions) の期待挙動（unit — フィクスチャ駆動）:
    - 各参加者につき questions の順で回答セルを生成。answers に該当 question_key（= question.id）の行がなければ「—」
    - 全体ステータス = participant.confirm_status を UI-SPEC バッジ契約（pending→未配信 / sent→配信済み / in_progress→回答中 / completed→回答済み）にマップ
    - ケース: 回答0件 / 一部回答（q_age のみ）/ 全問回答 / questions が空配列
  </behavior>
  <action>
    1. tests/unit/answer-status.test.ts を behavior どおり先に作成（RED）→ lib/answer-status.ts の buildAnswerStatusRows を実装（GREEN）。UI 都合（20字 truncate）は関数に含めない（描画側の責務）
    2. lib/data/participants.ts: `getParticipantsWithAnswers(supabase, eventId)` — RESEARCH Pattern 7 のネスト埋め込み select（participants + line_user:line_users(display_name) + answers + event_platform_urls!inner(event_id) eq filter）/ `getLinkingLists(supabase, eventId, oaConfigId)` — 未紐付け・紐付け済み参加者リストと、紐付け候補の line_users 一覧（紐付け済み line_user_id を除外）
    3. tests/integration/data.test.ts（RLS_TEST=1 ゲート）: user1 JWT（signInWithPassword）で dev-event の getParticipantsWithAnswers → seed 参加者 …0005 が q_age の回答付きで返る（answers ネストの構造 assert）。OA-2 の dev-event-2（…0012）に対する同クエリ → 0 行（クロスOA 不可視）
  </action>
  <verify>
    <automated>cd admin && npx vitest run tests/unit/answer-status.test.ts && RLS_TEST=1 npx vitest run tests/integration/data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - answer-status.test.ts の 4 ケース全 pass（純関数 — ネット不要）
    - data.test.ts: ネスト1クエリで participants×answers×line_users が返り、他OAイベントは 0 行
  </acceptance_criteria>
  <done>acceptance_criteria の全項目が満たされ、verify の automated コマンドが exit 0 で完走している</done>
</task>

<task type="auto">
  <name>Task 2: 回答状況タブ + 紐付けタブ + link/unlink server actions（UI-SPEC 準拠）</name>
  <files>admin/components/events/answer-status-tab.tsx, admin/components/events/linking-tab.tsx, admin/lib/actions/linking.ts, admin/app/(app)/events/[id]/page.tsx</files>
  <read_first>
    - .planning/phases/03-admin-ui/03-UI-SPEC.md「回答状況タブ」「紐付けタブ」「Interaction Contracts: Manual Linking Combobox」「Status Badge Reference」「Copywriting Contract」
    - admin/app/(app)/events/[id]/page.tsx（03-03 のタブ shell — プレースホルダを置換）
  </read_first>
  <action>
    1. answer-status-tab.tsx: Table 列 = 参加者名 | LINE表示名 | Q1..Qn | 全体ステータス(badge)。Qn セルは 20 字 truncate + Tooltip で全文。空状態「まだ回答がありません」（read-only・CTAなし）。questions は選択 OA の oa_configs.questions から取得し buildAnswerStatusRows で合成
    2. lib/actions/linking.ts（'use server'）: `linkParticipant(participantId, lineUserId)` / `unlinkParticipant(participantId)` — participants.line_user_id の update を `.select()` 付きで実行、0 行は「保存に失敗しました…」、with check 違反（他OA line_user — RLS が raise）はキャッチして同エラー文言。成功時 revalidatePath
    3. linking-tab.tsx（client）: 2 カラム（左=未紐付け / 右=紐付け済み）。未紐付け行 = 参加者名 + Combobox（Popover+Command、placeholder「LINE友だちを選択...」、no results「該当するLINE友だちが見つかりません」、紐付け済み除外・display_name クライアントフィルタ）+「紐付け」accent ボタン。紐付け済み行 = 参加者名 → LINE表示名 +「紐付けを解除」destructive/outline → AlertDialog（title「紐付けを解除しますか？」/ 本文「この操作は元に戻せません。…」/ confirm「紐付けを解除」red-600 / cancel「解除しない」）。紐付けは楽観的更新 + 失敗時巻き戻し + inline エラー alert。全員紐付け済みの空状態 =「全員の紐付けが完了しています」green alert / 未紐付けありの案内 =「紐付けされていない参加者がいます。LINEアカウントと紐付けてください」
    4. events/[id]/page.tsx の回答状況・紐付けプレースホルダを実タブに置換（参加者タブ・取得ボタンは 03-03 のまま）
  </action>
  <verify>
    <automated>cd admin && npm run build && grep -q "紐付けを解除しますか" components/events/linking-tab.tsx && grep -q "解除しない" components/events/linking-tab.tsx && grep -q "まだ回答がありません" components/events/answer-status-tab.tsx && ! git -C "$(git rev-parse --show-toplevel)" grep -q service_role -- admin/</automated>
  </verify>
  <acceptance_criteria>
    - build green。UI-SPEC の紐付け文言（紐付け / 紐付けを解除 / 解除しない / LINE友だちを選択...）と空状態文言がソースに存在
    - link/unlink action が .select() 行数チェック + RLS raise のハンドリングを持つ（ソースアサーション）
    - Combobox が紐付け済み line_user を候補から除外する実装
  </acceptance_criteria>
  <done>acceptance_criteria の全項目が満たされ、verify の automated コマンドが exit 0 で完走している</done>
</task>

<task type="auto">
  <name>Task 3: フェーズゲート — 全機械検証の一括実行</name>
  <files>（ファイル変更なし — 検証のみ。失敗時は当該プラン範囲の修正コミットを作る）</files>
  <read_first>
    - .planning/phases/03-admin-ui/03-VALIDATION.md（Full suite command / Per-Task Verification Map / 横断 smoke）
  </read_first>
  <action>
    env.dev を source したシェルで順に実行し、全結果を SUMMARY に記録する:
    1. `cd admin && npx vitest run`（unit 全件）
    2. `cd admin && RLS_TEST=1 npx vitest run`（integration 全件: rls / auth / data / scraper-trigger）
    3. `cd admin && npm run build`
    4. `deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/`（Phase 1-2 回帰）
    5. シークレット検査: `git ls-files admin | grep -E '\.env'` が空 / `git grep -l service_role -- admin/` が空 / `git status --porcelain` に env.dev・admin/.env.local が現れない
    6. 未認証リダイレクト再確認: `next start -p 3100` + curl -I /events → 3xx /login（03-02 と同手順）
    7. ROADMAP 成功条件 1-6 と検証結果の対応表を SUMMARY に記載（1=auth.test.ts+モックログイン経路 / 2=event テスト+scraper-trigger / 3=answer-status+data.test / 4=linking 実装+RLS 紐付けテスト / 5=OA設定+oa-schema+rls / 6=rls.test.ts マトリクス）
  </action>
  <verify>
    <automated>cd admin && npx vitest run && RLS_TEST=1 npx vitest run && npm run build && cd .. && deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/ && [ -z "$(git ls-files admin | grep -E '\.env')" ] && ! git grep -q service_role -- admin/  # (root cwd — correct here)</automated>
  </verify>
  <acceptance_criteria>
    - unit + RLS統合 + build + Deno 回帰の全コマンド exit 0
    - シークレット grep 2 種が空（public リポジトリ安全）
    - 成功条件 1-6 ↔ テストの対応表が SUMMARY に存在し、全行 green
  </acceptance_criteria>
  <done>acceptance_criteria の全項目が満たされ、verify の automated コマンドが exit 0 で完走している</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 紐付け操作 → participants.line_user_id | 異なるOAのデータが結合されうる地点（データ汚染） |
| リポジトリ → GitHub (public) | コミット内容が全世界公開される地点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | linkParticipant（他OA line_user への紐付け） | mitigate | DB 側 with check（03-01）が最終防衛。action 側は RLS raise を捕捉してエラー表示。マトリクステスト（03-04）が証明済み |
| T-03-12 | Repudiation | unlink/link の 0行更新成功偽装 | mitigate | .select() 行数チェック + 楽観的更新の失敗時巻き戻し（Task 2） |
| T-03-15 | Information Disclosure | env / service_role のコミット混入 | mitigate | フェーズゲートの grep 検査（Task 3）を成功条件化 |
| T-03-13 | DoS | 紐付け連打 | accept | in-flight disabled + 1対1割当のみ。実害は自OA内に限定（RLS） |
</threat_model>

<verification>
- フェーズ全体: `cd admin && npx vitest run` / `RLS_TEST=1 npx vitest run` / `npm run build` / Deno 回帰 / シークレット grep — すべて green
<human-check>HUMAN-UAT（フェーズ後）: ①実 X OAuth ブラウザログイン（X認可→callback→一覧。初回ログイン後 auth.identities の identity_data キー名を確認 — 仮定A1。user_name/preferred_username 以外なら RPC の coalesce を補正）②localhost:3000 での通し操作感（イベント作成→参加者取得（実Twipla 731057）→紐付け→回答状況→OA設定）</human-check>
</verification>

<success_criteria>
- ADMIN-01 完成: 誰が回答済み・未回答かが Q 列 + 全体ステータスバッジで一覧できる（成功条件3）
- ADMIN-02 完成: 手動紐付け・解除が UI で完結し DB に反映、他 OA への紐付けは拒否される（成功条件4）
- Phase 3 の機械検証（成功条件1-6 対応の全テスト + build + 回帰 + シークレット検査）が一括 green
</success_criteria>

<output>
Create `.planning/phases/03-admin-ui/03-05-SUMMARY.md` when done
</output>
