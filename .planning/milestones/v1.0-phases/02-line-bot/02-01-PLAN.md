---
phase: 02-line-bot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260611171037_create_core_tables.sql
  - supabase/migrations/20260612120000_setup_cron_and_targets.sql
  - supabase/seed.sql
  - supabase/functions/deno.json
  - scripts/db/sql.ts
  - scripts/setup-dev.ts
  - scripts/verify-cron.ts
autonomous: true
requirements: [LINE-01]
must_haves:
  truths:
    - "db reset --linked 後に cronジョブ 'confirm-broadcast-daily' が登録済みである（マイグレーション由来で再現可能）"
    - "get_confirm_targets() が attending∧pending∧紐付け済み∧N日以内の参加者のみを返す"
    - "oa_configs.questions に定型3問がseedされ、events.confirm_days_before のdefaultが7である"
    - "line_users が unique(oa_config_id, line_user_id) を持つ（IN-06修正）"
    - "コミット対象ファイルにシークレット（キー・prod ref）が一切含まれない"
  artifacts:
    - path: "supabase/migrations/20260612120000_setup_cron_and_targets.sql"
      provides: "pg_cron/pg_net有効化 + get_confirm_targets() + count_unlinked_confirm_targets() + cron.schedule（Vault名参照のみ）"
      contains: "cron.schedule"
    - path: "supabase/migrations/20260611171037_create_core_tables.sql"
      provides: "questions JSONB / confirm_status check + current_question_index / IN-06修正 / confirm_days_before default 7"
      contains: "current_question_index"
    - path: "scripts/setup-dev.ts"
      provides: "Vaultシークレット投入 + oa_configs.line_channel_id更新（冪等・env読み）"
    - path: "scripts/verify-cron.ts"
      provides: "cron.job登録の機械検証"
    - path: "scripts/db/sql.ts"
      provides: "pooler経由SQLヘルパー（prod安全弁付き）"
  key_links:
    - from: "supabase/migrations/20260612120000_setup_cron_and_targets.sql"
      to: "vault.decrypted_secrets"
      via: "cron.scheduleジョブ本文のVault名参照（project_url / cron_function_key）"
      pattern: "vault\\.decrypted_secrets"
    - from: "scripts/setup-dev.ts"
      to: "vault.create_secret"
      via: "pooler接続でのVault投入"
      pattern: "create_secret"
    - from: "supabase/seed.sql"
      to: "oa_configs.questions"
      via: "定型3問のJSONB投入"
      pattern: "questions"
---

<objective>
Phase 2のDB基盤を完成させる: 既存マイグレーションのin-place拡張（questions JSONB・ステートマシン状態列・IN-06修正・confirm_days_before default 7）、cron基盤の新規マイグレーション（pg_cron/pg_net有効化・配信対象抽出SQL関数・Vault参照型cron.schedule）、devセットアップ/検証スクリプト、そして [BLOCKING] のdevスキーマ再適用。

Purpose: LINE-01（N日前自動配信）のスケジュールトリガーと配信対象抽出をDB層で確立する。後続プラン（message-sender / webhook）はこのスキーマとRPCに依存する。
Output: 編集済みマイグレーション2本 + 新規マイグレーション1本 + seed更新 + scripts 3本 + dev環境への適用完了。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-line-bot/02-CONTEXT.md
@.planning/phases/02-line-bot/02-RESEARCH.md
@supabase/migrations/20260611171037_create_core_tables.sql
@supabase/seed.sql

<interfaces>
後続プランが依存する契約（このプランが確定させる）:

- RPC `public.get_confirm_targets()` returns table(participant_id uuid, line_user_id text, event_id uuid, event_title text, event_date date, meeting_at timestamptz, meeting_place text, fee text, venue_info text, oa_config_id uuid) — service_roleのみexecute可
- RPC `public.count_unlinked_confirm_targets()` returns integer — 同条件で line_user_id is null の件数（スキップログ用）
- `oa_configs.questions` jsonb: 配列。各要素 `{ "id": string, "text": string, "options": string[] }`
- `participants.confirm_status`: 'pending' | 'sent' | 'in_progress' | 'completed'（check制約）/ `participants.current_question_index` integer not null default 0
- `answers.question_key` には questions JSONB の `id` を格納する（既存列名を維持。renameしない — Claude裁量で確定）
- `line_users`: unique(oa_config_id, line_user_id)、oa_config_id not null（IN-06修正後）
- seed固定UUID: oa_config=…0001 / event=…0002 / epu=…0003 / line_user=…0004（line_user_id='U00000000000000000000000000000dev'）/ participant=…0005
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 既存マイグレーション+seedのin-place拡張（questions/状態列/IN-06/default 7）</name>
  <files>supabase/migrations/20260611171037_create_core_tables.sql, supabase/seed.sql</files>
  <read_first>supabase/migrations/20260611171037_create_core_tables.sql（現行スキーマ全体）, supabase/seed.sql, .planning/phases/02-line-bot/02-RESEARCH.md の「スキーマ変更ガイド」表, .planning/phases/01-foundation-and-scraping/01-REVIEW.md の IN-06 項</read_first>
  <action>
    20260611171037_create_core_tables.sql をin-place編集する（Phase 1確立パターン: スキーマはdb resetでゼロから再現するため、alter文ではなくcreate table定義自体を書き換える）:

    1. oa_configs に `questions jsonb not null default '[]'::jsonb` 列を追加（D-01: 質問定義はJSONB配列）。列コメントで要素形状 `{id, text, options[]}` を明記。
    2. events の `confirm_days_before integer not null default 3` を `default 7` に変更（D-09: Locked決定。研究がdefault 3との不一致を発見済み）。
    3. participants に `check (confirm_status in ('pending','sent','in_progress','completed'))` を confirm_status 列定義へ追加し、`current_question_index integer not null default 0` 列を追加（D-05: ステートマシン状態はparticipantsに保持、別テーブルは作らない）。
    4. line_users のIN-06修正（D-15）: `line_user_id text not null unique` のグローバルunique を外し、テーブル末尾に `unique(oa_config_id, line_user_id)` を追加。`oa_config_id` は `not null references public.oa_configs(id) on delete cascade` に変更（nullableのままだとNULL行が複数並存しuniqueが機能しないため — 01-REVIEW指摘どおり）。コメントも更新。
    5. answers は構造変更なし。`question_key` 列のコメントに「oa_configs.questions の id を格納」と追記。answered_at の再回答時上書きはアプリ側UPSERTで行う（answersにはset_updated_atトリガーがない — RESEARCH Pitfall 9。トリガーは追加しない）。

    seed.sql を更新:
    - dev-oa の insert に questions JSONB を追加。定型3問（CONTEXT specifics準拠）: ①id='q_age', text=年齢確認の質問文, options=['20歳以上です','未成年です'] ②id='q_drink', text=飲酒有無の質問文, options=['飲む','飲まない'] ③id='q_late', text=遅刻早退予定の質問文, options=['なし','遅刻予定','早退予定']。line_channel_id は null のまま（シークレット非コミット — setup-dev.ts が実行時に更新する）。
    - dev-event の insert に E2Eで配信対象になる値を追加: `event_date = current_date + 3`（confirm_days_before=7の窓内に入る動的式）、meeting_at / meeting_place / fee / venue_info にダミー値（初回配信メッセージの組み立て検証用）。
    - E2E用の固定UUID行を追加: line_users 1行（id=…0004, oa_config_id=…0001, line_user_id='U00000000000000000000000000000dev', display_name='dev-tester'）と participants 1行（id=…0005, event_platform_url_id=…0003, display_name='devテスト参加者', natural_key='dn:devテスト参加者', status='attending', line_user_id=…0004, confirm_status='pending'）。既存行と同じ `on conflict (id) do nothing` 方式で冪等に。
    - 注意: このline_user_idは実在しない値。LINE_DRY_RUN=1での機械検証専用であり、実配信検証（HUMAN-UAT）では実followで作られた行を使う旨をseedコメントに明記。

    禁止事項: マイグレーション/seedにキー文字列・チャネルID・prod ref（hgojtooexbknqotzkkja）を一切書かない（publicリポジトリ）。
  </action>
  <verify>
    <automated>grep -c "current_question_index" supabase/migrations/20260611171037_create_core_tables.sql | grep -qv "^0$" && grep -q "unique(oa_config_id, line_user_id)" supabase/migrations/20260611171037_create_core_tables.sql && grep -q "default 7" supabase/migrations/20260611171037_create_core_tables.sql && grep -q "q_age" supabase/seed.sql && ! git grep -l "hgojtooexbknqotzkkja" -- supabase/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - create_core_tables.sql に questions jsonb / current_question_index / confirm_status check制約 / unique(oa_config_id, line_user_id) / confirm_days_before default 7 がすべて存在する
    - line_user_id のグローバルuniqueが存在しない（grep "line_user_id text not null unique" が0件）
    - seed.sql に3問のquestions JSONB（id: q_age / q_drink / q_late）、event_date = current_date + 3、固定UUIDのline_user+participant行がある
    - supabase/ 以下に prod ref・JWT形文字列が含まれない（git grep で機械確認）
  </acceptance_criteria>
  <done>スキーマ定義がLocked決定（D-01/D-05/D-09/D-15）と完全一致し、seedがE2E可能な初期状態を表現している</done>
</task>

<task type="auto">
  <name>Task 2: cron基盤マイグレーション新規作成 + dev運用スクリプト3本</name>
  <files>supabase/migrations/20260612120000_setup_cron_and_targets.sql, scripts/db/sql.ts, scripts/setup-dev.ts, scripts/verify-cron.ts, supabase/functions/deno.json</files>
  <read_first>.planning/phases/02-line-bot/02-RESEARCH.md の「Pattern 1: シークレット非コミットのcronスケジュール」「配信対象抽出SQL（get_confirm_targets）」「Vault投入スクリプトの核」「cron登録・実行結果の機械検証」セクション（実機検証済みSQL/TSの正本）, supabase/functions/deno.json</read_first>
  <action>
    新規マイグレーション supabase/migrations/20260612120000_setup_cron_and_targets.sql を作成（既存2本より後のタイムスタンプ必須）:

    1. `create extension if not exists pg_cron;` と `create extension if not exists pg_net;`（db resetでpg_cronとcronジョブが消えることを研究が実証済み — マイグレーションに置くことが唯一の再現手段）。
    2. `public.get_confirm_targets()` をRESEARCHの推奨形そのままで作成: returns table(participant_id, line_user_id, event_id, event_title, event_date, meeting_at, meeting_place, fee, venue_info, oa_config_id)。条件は p.status='attending' AND p.confirm_status='pending' AND line_users JOIN成立（紐付け済み）AND e.event_date is not null AND e.event_date >= (now() at time zone 'Asia/Tokyo')::date AND (e.event_date - JST今日) <= e.confirm_days_before（D-11）。`language sql stable set search_path = ''`。`revoke all ... from public, anon, authenticated; grant execute ... to service_role;`。
    3. `public.count_unlinked_confirm_targets()` returns integer を追加: 同条件だが p.line_user_id is null の件数（D-11: 未紐付けスキップは件数のみログ）。同じrevoke/grant。
    4. `select cron.schedule('confirm-broadcast-daily', '0 1 * * *', $$...$$);`（D-10: 01:00 UTC = 10:00 JST）。ジョブ本文はRESEARCH Pattern 1の実機検証済み形: net.http_post(url := vault.decrypted_secrets name='project_url' の値 || '/functions/v1/message-sender', headers := Content-Type + 'Authorization: Bearer ' || name='cron_function_key' の値, body := '{}'::jsonb, timeout_milliseconds := 30000)。timeout は30000ms（RESEARCH Pitfall 10の安全値）。シークレットはVaultの**名前のみ**参照 — URL・キー値のリテラルは絶対に書かない。

    supabase/functions/deno.json の imports に `"postgres": "npm:postgres@3.4.9"` を追加（Package Legitimacy Audit済み[OK]。E2EテストがこのconfigでDB検証するため）。

    scripts/db/sql.ts を作成: pooler経由SQL実行ヘルパー。`connectDev()` をexport — env から DEV_PROJECT_REF とDBパスワード（SUPABASE_DB_PASSWORD があればそれ、なければ SUPABASE_DIRECT_CONNECTION_STRING からパース）を読み、`ref !== 'cmsxvxtcdniqgvhxjqri'` なら即 `Deno.exit(1)`（prod安全弁 — T-02-02）。postgres（npm:postgres@3.4.9）で host='aws-1-ap-northeast-1.pooler.supabase.com', port=5432, database='postgres', username=`postgres.${ref}`, ssl='require', prepare=false の接続を返す（直接接続 db.<ref> はIPv6限定DNSで不可 — 研究実証済み）。

    scripts/setup-dev.ts を作成（冪等・env読み・値はコミットしない）: connectDev() で接続し、(1) vault.secrets から name in ('project_url','cron_function_key') をdeleteしてから vault.create_secret で `https://${ref}.supabase.co` を 'project_url'、SUPABASE_ANON_KEY を 'cron_function_key' として投入（RESEARCHが実機検証した形。anonキーで十分 — 最小権限）。(2) `update oa_configs set line_channel_id = $LINE_CHANNEL_ID where name = 'dev-oa'`（webhookのOA解決用。seedはnullのため実行時投入）。各ステップの結果を成功/失敗で標準出力。

    scripts/verify-cron.ts を作成: connectDev() で (1) `select extname from pg_extension` に pg_cron と pg_net が存在、(2) `select jobname, schedule, active from cron.job where jobname = 'confirm-broadcast-daily'` が1行（schedule='0 1 * * *', active=true）、(3) 参考情報として cron.job_run_details 直近3件と net._http_response 直近3件を表示（存在しなければスキップ）。(1)(2)が満たされなければ非ゼロ終了。
  </action>
  <verify>
    <automated>grep -q "cron.schedule" supabase/migrations/20260612120000_setup_cron_and_targets.sql && grep -q "vault.decrypted_secrets" supabase/migrations/20260612120000_setup_cron_and_targets.sql && ! grep -E "eyJ[A-Za-z0-9_-]{20,}|hgojtooexbknqotzkkja|cmsxvxtcdniqgvhxjqri" supabase/migrations/20260612120000_setup_cron_and_targets.sql && grep -q '"postgres": "npm:postgres@3.4.9"' supabase/functions/deno.json && deno check scripts/db/sql.ts scripts/setup-dev.ts scripts/verify-cron.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 新規マイグレーションに extensions + get_confirm_targets + count_unlinked_confirm_targets + cron.schedule が含まれ、シークレット値・プロジェクトref・URLリテラルが一切含まれない（Vault名参照のみ）
    - get_confirm_targets / count_unlinked_confirm_targets に revoke + service_roleのみgrant がある
    - scripts 3本が deno check を通過し、sql.ts と setup-dev.ts に ref !== 'cmsxvxtcdniqgvhxjqri' での abort がある
    - deno.json に postgres import が追加されている
  </acceptance_criteria>
  <done>cron基盤がマイグレーションのみで再現可能（reset耐性）かつシークレット非コミットで定義され、dev運用スクリプトが型チェックを通る</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] devスキーマ再適用 + Vault/チャネルID投入 + cron登録検証</name>
  <files></files><!-- 読み取り専用チェックのみ（supabase/.temp/linked-project.json は確認対象であり変更しない） -->
  <read_first>supabase/.temp/linked-project.json, .planning/phases/02-line-bot/02-RESEARCH.md の「Pitfall 1」「Environment Availability」セクション, docs/NIGHT-RUN.md（存在すれば実行手順の前提）</read_first>
  <action>
    devリモートへのスキーマ再適用と検証を順に実行する（すべて非対話）:

    1. prod安全弁: `cat supabase/.temp/linked-project.json` で `"ref":"cmsxvxtcdniqgvhxjqri"` を確認。不一致なら即中断（prodは hgojtooexbknqotzkkja — 一切触れない）。
    2. `set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a` でシークレットをシェルに読み込む。
    3. `supabase db reset --linked --yes` を実行（devは使い捨て。マイグレーション2+1本とseedがゼロから適用される）。
    4. `deno run --allow-net --allow-read --allow-env scripts/setup-dev.ts` を実行（Vaultはreset耐性ありだが冪等再投入で確実化 + oa_configs.line_channel_id 更新）。
    5. `deno run --allow-net --allow-read --allow-env scripts/verify-cron.ts` を実行 — pg_cron/pg_net拡張 + cronジョブ 'confirm-broadcast-daily' 登録を機械確認（CONTEXT検証方針: 実時刻待ちはしない）。
    6. scripts/db/sql.ts 経由のワンライナー（deno eval か小スクリプト）でスキーマ検証SQL: (a) information_schema.columns で events.confirm_days_before の column_default が 7、(b) oa_configs の dev-oa 行で jsonb_array_length(questions)=3、(c) pg_constraint に participants の confirm_status check と line_users の (oa_config_id, line_user_id) unique が存在、(d) participants 固定UUID行 …0005 が confirm_status='pending' で存在、(e) oa_configs.line_channel_id が非null。
    7. シークレット非コミット最終確認: `git ls-files | grep -E '^env\.(dev|prod)$'` が空、`git grep -lE 'eyJ[A-Za-z0-9_-]{20,}' -- supabase/ scripts/` が空。

    いずれかが失敗したらマイグレーション/スクリプトを修正して 3 からやり直す（resetは何度でも安全）。
  </action>
  <verify>
    <automated>deno run --allow-net --allow-read --allow-env scripts/verify-cron.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db reset --linked --yes` がエラーなく完了する
    - verify-cron.ts が終了コード0（pg_cron/pg_net + 'confirm-broadcast-daily' active=true）
    - SQL検証: confirm_days_before default=7 / questions 3問 / check制約とcomposite unique存在 / seed participant存在 / line_channel_id非null がすべて真
    - git ls-files に env.dev / env.prod が含まれず、supabase/ scripts/ にJWT形文字列がない
  </acceptance_criteria>
  <done>dev DBが新スキーマ+cron基盤で完全再現され、後続プラン（message-sender/webhook）が依存するRPC・列・seedがリモートに存在する</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publicリポジトリ → マイグレーション/seed | コミット内容は全世界に公開される |
| ローカルスクリプト → リモートDB | prod/devの取り違えが破壊的操作になる |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | cron.schedule本文 / seed.sql | mitigate | Vaultシークレットの**名前のみ**SQLに記載。値はsetup-dev.tsがenv.devから投入。Task 1/2/3でgit grepによるキー形文字列・ref検査を実施 |
| T-02-02 | Elevation of Privilege | scripts/db/sql.ts（DB破壊的操作） | mitigate | connectDev()で ref==='cmsxvxtcdniqgvhxjqri' を強制（不一致は即exit 1）。db reset前に linked-project.json を機械確認 |
| T-02-03 | Tampering | get_confirm_targets() RPC | mitigate | revoke from public/anon/authenticated + grant to service_role のみ。`set search_path = ''` でサーチパス汚染を遮断 |
| T-02-SC | Tampering | npm installs (postgres@3.4.9) | accept | RESEARCH Package Legitimacy Audit で [OK]（porsager/postgres・postinstallなし・slopcheck通過）。npm:指定子のみで package.json への追加もなし — 追加チェックポイント不要 |
</threat_model>

<verification>
- `deno run --allow-net --allow-read --allow-env scripts/verify-cron.ts` → 終了コード0
- `supabase db reset --linked --yes` 成功（linked ref=cmsxvxtcdniqgvhxjqri 確認後）
- pooler SQL: get_confirm_targets() がseed participantを1件返す（event_date=current_date+3 が窓内のため）
- `git grep -lE 'eyJ[A-Za-z0-9_-]{20,}|hgojtooexbknqotzkkja' -- supabase/ scripts/` が空
</verification>

<success_criteria>
- dev DBに新スキーマ（questions/状態列/IN-06修正/default 7）+ cronジョブ + RPC 2本 + seedが存在
- すべてマイグレーション+seed+冪等スクリプトのみで再現可能（reset耐性が機械検証済み）
- シークレット非コミットが機械検証済み
</success_criteria>

<output>
Create `.planning/phases/02-line-bot/02-01-SUMMARY.md` when done
</output>
