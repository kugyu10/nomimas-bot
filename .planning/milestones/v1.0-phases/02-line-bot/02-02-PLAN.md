---
phase: 02-line-bot
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/confirm/postback.ts
  - supabase/functions/_shared/confirm/state.ts
  - supabase/functions/_shared/confirm/messages.ts
  - supabase/functions/_shared/line/events.ts
  - supabase/functions/tests/postback_data_test.ts
  - supabase/functions/tests/confirm_state_test.ts
  - supabase/functions/tests/confirm_messages_test.ts
  - supabase/functions/tests/line_events_test.ts
autonomous: true
requirements: [LINE-02]
must_haves:
  truths:
    - "1問1答の状態遷移のうち sent→in_progress→completed の全遷移と pending/completed 状態での入力無視が純関数 transition() で表現され、自動テストで網羅検証できる（pending→sent は message-sender(02-03) の責務）"
    - "過去質問のQuick Reply再タップ・想定外question_id・完了後入力・webhook再配達に対して遷移が冪等である"
    - "postback data の encode/decode がラウンドトリップし、不正入力（非uuid・欠損キー・負index）が拒否される"
    - "Quick Replyメッセージが LINE制約（items≤13・label≤20字・data≤300字・1リクエスト≤5メッセージ）を満たすことがビルダーのassertで保証される"
  artifacts:
    - path: "supabase/functions/_shared/confirm/state.ts"
      provides: "transition() 純関数（状態遷移の唯一の真実）+ ConfirmStatus/Question/TransitionResult 型"
      exports: ["transition"]
      min_lines: 40
    - path: "supabase/functions/_shared/confirm/postback.ts"
      provides: "postback data の encode/decode + zod検証"
      exports: ["encodePostbackData", "decodePostbackData"]
    - path: "supabase/functions/_shared/confirm/messages.ts"
      provides: "質問/初回バンドル/再誘導/完了メッセージのビルダー"
      exports: ["buildQuestionMessage", "buildInitialMessages", "buildRepromptMessages", "buildCompletionMessages"]
    - path: "supabase/functions/_shared/line/events.ts"
      provides: "webhookイベント（postback/follow/message）のzodスキーマと判別ヘルパー"
      exports: ["parseWebhookEvent"]
  key_links:
    - from: "supabase/functions/_shared/confirm/messages.ts"
      to: "supabase/functions/_shared/confirm/postback.ts"
      via: "Quick Reply postback action の data 生成"
      pattern: "encodePostbackData"
    - from: "supabase/functions/tests/confirm_state_test.ts"
      to: "supabase/functions/_shared/confirm/state.ts"
      via: "全遷移のユニットテスト"
      pattern: "transition"
---

<objective>
1問1答ステートマシンの全ロジックをDB/ネットワーク非依存の純関数として実装する（TDD: RED→GREENコミット）。postbackコーデック、webhookイベントzodスキーマ、ステートマシン遷移、Quick Replyメッセージビルダーの4モジュール。

Purpose: LINE-02の核心（全状態遷移の自動テスト網羅 — ROADMAP成功条件2）を、I/Oから切り離した純関数で達成する。webhook/message-senderはこれらの薄いオーケストレータになる（Wave 2以降）。
Output: _shared/confirm/ 3モジュール + _shared/line/events.ts + ユニットテスト4本（ネット不要・決定的）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-line-bot/02-CONTEXT.md
@.planning/phases/02-line-bot/02-RESEARCH.md
@supabase/functions/deno.json
@supabase/functions/tests/twipla_parser_test.ts

<interfaces>
このプランが**確定させる**契約（Wave 2/3 の webhook・message-sender が実装対象とする）:

From _shared/confirm/state.ts:
```typescript
export type ConfirmStatus = "pending" | "sent" | "in_progress" | "completed";
export interface Question { id: string; text: string; options: string[]; }
export interface TransitionResult {
  nextStatus: ConfirmStatus;
  nextIndex: number;
  answer: { questionId: string; questionText: string; answer: string } | null;
  reply: "next_question" | "completion" | "reprompt" | "none";
}
export function transition(
  current: { status: ConfirmStatus; index: number },
  questions: Question[],
  input: { questionId: string; optionIndex: number },
): TransitionResult;
```

From _shared/confirm/postback.ts:
```typescript
export interface PostbackPayload { participantId: string; questionId: string; optionIndex: number; }
export function encodePostbackData(p: PostbackPayload): string;   // "p=<uuid>&q=<id>&a=<int>" 形式、>300字でthrow
export function decodePostbackData(data: string): PostbackPayload | null;  // zod検証失敗はnull
```

From _shared/confirm/messages.ts（戻り値はLINE Messaging APIのmessageオブジェクト形状）:
```typescript
export function buildQuestionMessage(q: Question, participantId: string): object;       // text + quickReply
export function buildInitialMessages(event: EventInfo, q1: Question, participantId: string): object[];  // ≤3バブル（イベント情報+案内文+Q1）
export function buildRepromptMessages(q: Question, participantId: string): object[];    // 誘導文+現在Q再送（1 reply呼び出し分）
export function buildCompletionMessages(): object[];  // 完了+「修正があれば主催者へ連絡」
export interface EventInfo { title: string; eventDate: string | null; meetingAt: string | null; meetingPlace: string | null; fee: string | null; venueInfo: string | null; }
```

From _shared/line/events.ts:
```typescript
// 判別結果: { kind: "postback", replyToken, userId, data, isRedelivery } | { kind: "follow", userId } | { kind: "message", replyToken, userId, text } | null
export function parseWebhookEvent(raw: unknown): ParsedEvent | null;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: postbackコーデック + webhookイベントzodスキーマ（RED→GREEN）</name>
  <files>supabase/functions/_shared/confirm/postback.ts, supabase/functions/_shared/line/events.ts, supabase/functions/tests/postback_data_test.ts, supabase/functions/tests/line_events_test.ts</files>
  <read_first>.planning/phases/02-line-bot/02-RESEARCH.md の「Pattern 5: postback dataのエンコード」「webhookイベントのzodスキーマ」セクション, supabase/functions/tests/twipla_parser_test.ts（既存テストの書式・assert流儀）</read_first>
  <behavior>
    postback_data_test.ts:
    - encode→decode ラウンドトリップで participantId/questionId/optionIndex が一致する
    - エンコード形式は URLSearchParams の "p=&q=&a=" で、uuid+短いid+1桁indexなら100字未満
    - decode: pが非uuid / qが空 / aが非整数・負数 / キー欠損 / 完全な無関係文字列 → null を返す（throwしない）
    - encode: 合計が300字を超える questionId を渡すと throw（LINE postback data上限 — RESEARCH A1のassert方針）
    line_events_test.ts:
    - 正規のpostbackイベントJSON（replyToken/source.userId/postback.data/deliveryContext.isRedelivery）が kind:"postback" にパースされる
    - followイベント → kind:"follow"、textメッセージイベント → kind:"message"（textを保持）
    - source.userId欠損 / 未知type / 非オブジェクト → null（throwしない）
    - deliveryContext省略時は isRedelivery=false にデフォルトされる
  </behavior>
  <action>
    RED: 上記behaviorのテスト2本を先に書き、`deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/postback_data_test.ts supabase/functions/tests/line_events_test.ts` が失敗することを確認してコミット（test(02-02): ...）。

    GREEN: postback.ts — URLSearchParams で encode、decode は zod（p: z.uuid 相当, q: 非空string, a: 文字列→整数変換+0以上）で safeParse、失敗は null。encodePostbackData は結果長 >300 で Error を throw（D-06: postback dataに participant_id / question_id を埋め込む）。
    events.ts — RESEARCHのPostbackEventSchema/FollowEventSchema準拠のzodスキーマ + textメッセージ用スキーマ（type:'message', message.type:'text', message.text）を定義し、parseWebhookEvent(raw) が判別済みの軽量オブジェクト（kind/replyToken/userId/data|text/isRedelivery）または null を返す。webhook本体が分岐しやすい形に正規化するのが目的。
    テストがパスしたらコミット（feat(02-02): ...）。
  </action>
  <verify>
    <automated>deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/postback_data_test.ts supabase/functions/tests/line_events_test.ts</automated>
  </verify>
  <acceptance_criteria>
    - REDコミット（テストのみ・失敗確認済み）とGREENコミットが分かれている
    - decodePostbackData が不正入力で例外を投げず null を返す（webhookを落とさない）
    - encodePostbackData に300字ガードがある
    - parseWebhookEvent が postback/follow/message を判別し、未知形状で null
  </acceptance_criteria>
  <done>postbackコーデックとイベントパースが単体で網羅テストされ、後続のwebhook実装が信頼できる入力検証層を持つ</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ステートマシン transition() 純関数（RED→GREEN）</name>
  <files>supabase/functions/_shared/confirm/state.ts, supabase/functions/tests/confirm_state_test.ts</files>
  <read_first>.planning/phases/02-line-bot/02-RESEARCH.md の「Pattern 2: ステートマシンを純関数に分離」「Pitfall 5」セクション, .planning/phases/02-line-bot/02-CONTEXT.md のステートマシン設計決定, supabase/functions/_shared/confirm/postback.ts（Task 1の型）</read_first>
  <behavior>
    定型3問相当の questions フィクスチャ（q_age/q_drink/q_late）で全遷移を検証:
    - sent + Q0への正答 → in_progress / index=1 / answer記録（questionText snapshot付き）/ reply="next_question"
    - in_progress(index=1) + Q1正答 → in_progress / index=2 / reply="next_question"
    - in_progress(index=2) + 最終Q正答 → completed / index=3 / reply="completion"（D-04: 全問回答で完了）
    - 質問1問だけの配列で sent + Q0正答 → 即 completed（境界）
    - 過去質問の再タップ（in_progress index=2 で Q0のpostback）→ answer記録（上書き用）/ index・status不変 / reply="reprompt"（RESEARCH Pitfall 5: 再タップ・再配達への冪等ガード）
    - 同一質問の再受信（index一致のpostbackが2回 — 再配達相当）→ 2回目も同じ TransitionResult（純関数なので同入力同出力で冪等）
    - 未知のquestionId（questionsに存在しない）→ answer=null / 状態不変 / reply="reprompt"
    - optionIndex が options の範囲外（負・超過）→ answer=null / 状態不変 / reply="reprompt"
    - completed + 任意のpostback → answer=null / 状態不変 / reply="none"（D-07: 完了後は応答しない）
    - pending + postback → answer=null / 状態不変 / reply="none"（配信前のpostbackはあり得ない入力として無視）
    - questions が空配列 → answer=null / reply="none"（防御）
  </behavior>
  <action>
    RED: 上記全ケースのテストを書き、失敗確認後コミット。

    GREEN: state.ts に ConfirmStatus / Question / TransitionResult 型と transition() を実装（context の interfaces ブロックのシグネチャに厳密一致させる — webhookがこの契約に依存する）。実装方針: (1) status が pending/completed なら即 none。(2) input.questionId が questions[current.index] と一致 → answer生成（answer文字列は questions[].options[optionIndex] から解決、questionText はその質問の text）、index+1 が questions.length 以上なら completed+completion、未満なら in_progress+next_question。(3) questionId が index より前の質問と一致 → answer生成（UPSERT上書き用）+ index/status不変 + reprompt。(4) それ以外（未知id・範囲外index）→ answer=null + reprompt。I/O・Date・乱数を一切使わない純関数にする（D-05準拠、状態の永続化は呼び出し側の責務）。
    テストパス後コミット。
  </action>
  <verify>
    <automated>deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/confirm_state_test.ts</automated>
  </verify>
  <acceptance_criteria>
    - behavior記載の全ケース（正常遷移3種・境界・再タップ・再配達冪等・未知id・範囲外index・completed/pending無視・空questions）がテストに存在しパスする
    - transition() がimportするのは型のみ（Deno API・supabase-js・fetchへの参照ゼロ）
    - RED→GREENの2コミットが残っている
  </acceptance_criteria>
  <done>ROADMAP成功条件2「全状態遷移が自動テストで検証できる」の遷移部分が純関数として完成</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Quick Replyメッセージビルダー（RED→GREEN）</name>
  <files>supabase/functions/_shared/confirm/messages.ts, supabase/functions/tests/confirm_messages_test.ts</files>
  <read_first>.planning/phases/02-line-bot/02-RESEARCH.md の「Quick Reply付き質問メッセージ」コード例と「Pattern 3: reply優先のクォータ戦略」, supabase/functions/_shared/confirm/postback.ts（data生成に使用）</read_first>
  <behavior>
    confirm_messages_test.ts:
    - buildQuestionMessage: type="text"・text=質問文・quickReply.items が options と同数。各itemは action.type="postback"、label=選択肢文字列、displayText=選択肢文字列、data が decodePostbackData で復元可能（participantId/questionId/optionIndexが一致）
    - label が20字超の選択肢を渡すと throw、options が13個超だと throw（LINE制約 — RESEARCH A1の実行時assert方針）
    - buildInitialMessages: 3要素（イベント情報テキスト・案内文・Q1）。先頭テキストに title / 開催日 / 集合場所 / 参加費 / 店情報 が含まれ、null のフィールドは行ごと省略される。配列長 ≤5 を超える構成にするとthrow（push 1リクエスト上限）
    - buildRepromptMessages: 2要素（誘導文テキスト + 現在質問のQuick Reply付きメッセージ）— 1回のreply呼び出しに同梱する前提（RESEARCH Pitfall 3: replyTokenは1回限り）
    - buildCompletionMessages: 1要素。textに「修正」と「主催者」が含まれる（D-08: 修正があれば主催者へ連絡）
  </behavior>
  <action>
    RED: 上記テストを書き、失敗確認後コミット。

    GREEN: messages.ts を実装。buildQuestionMessage は encodePostbackData（Task 1）で data を生成し、items≤13・label≤20字を実行時assert（違反はErrorをthrow — seedの定型3問は最長7字なので通常は発火しない安全網）。buildInitialMessages は EventInfo（title/eventDate/meetingAt/meetingPlace/fee/venueInfo、null許容）から日本語のイベント情報テキストを組み立て（null項目は省略）、案内文（最終確認の趣旨 + これから質問する旨）、Q1メッセージの3バブルを返す（D-02: 初回配信=イベント情報+案内文+Q1を1 pushに同梱、≤5）。文言の具体表現はClaude裁量（CONTEXT明記）。Flexメッセージは使わない（D-03）。
    テストパス後コミット。
  </action>
  <verify>
    <automated>deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/confirm_messages_test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 全ビルダーの出力形状テストがパスし、LINE制約（items≤13・label≤20・バンドル≤5）の実行時assertが存在する
    - buildQuestionMessage の data がコーデックでラウンドトリップする（postback.tsとの結線をテストが証明）
    - 完了メッセージに修正案内文言が含まれる
    - quickReply 以外の特殊メッセージ型（Flex等）を使っていない
  </acceptance_criteria>
  <done>webhook/message-senderが文言・形状を一切考えずに呼べるメッセージ生成層が完成し、unitスイート全体（既存12件+新規4ファイル）がネット不要でグリーン</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LINEクライアント → postback.data | エンドユーザーが改変可能な入力がコーデックを通過する |
| LINE Platform → webhookイベントJSON | 形状は外部仕様依存。zod検証が唯一の型保証 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | decodePostbackData | mitigate | zodでuuid/非空id/0以上整数を強制。検証失敗はnull（後段で保存・遷移が発生しない）。なりすまし照合（source.userId一致）はwebhook層（02-04のT-02-08）が担当 |
| T-02-05 | Tampering | transition()（再配達・再タップによる二重遷移） | mitigate | index一致時のみ前進のガード + 純関数の決定性。テストで再配達冪等を明示検証 |
| T-02-06 | Denial of Service | messages.ts（LINE API 400での送信不能） | mitigate | items≤13・label≤20・バンドル≤5の実行時assertで不正payloadを送信前に検知 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | 本プランの新規依存なし（zodはPhase 1監査済み・バージョン固定） |
</threat_model>

<verification>
- `deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/` — 既存12件 + 新規4ファイルすべてパス（ネット不要・決定的）
- 各タスクに RED コミット（failing test）と GREEN コミットが存在する（git logで確認可能）
</verification>

<success_criteria>
- 全状態遷移・コーデック・メッセージ形状がユニットテストで網羅され、quick run command（--allow-read のみ）でグリーン
- 4モジュールが context の interfaces ブロックどおりの公開シグネチャを持つ（Wave 2/3 がそのまま実装対象にできる）
</success_criteria>

<output>
Create `.planning/phases/02-line-bot/02-02-SUMMARY.md` when done
</output>
