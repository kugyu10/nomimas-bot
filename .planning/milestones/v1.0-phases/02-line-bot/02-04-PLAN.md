---
phase: 02-line-bot
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - supabase/functions/webhook/index.ts
  - supabase/functions/_shared/providers/twipla.ts
  - supabase/functions/tests/twipla_parser_test.ts
  - supabase/functions/tests/e2e_confirm_flow_test.ts
autonomous: true
requirements: [LINE-02, LINE-03]
must_haves:
  truths:
    - "Quick Reply postbackごとにステートマシンが遷移し、次の質問がreplyで送られる（署名付きcurl×3問でE2E検証）"
    - "全問回答で完了メッセージが送られ confirm_status='completed' になる"
    - "各回答が answers に participant_id + question_text スナップショット付きでUPSERT保存され、再回答は行数不変で上書きされる"
    - "source.userId がparticipantの紐付けLINEユーザーと一致しないpostbackは保存されない（なりすまし拒否）"
    - "進行中ユーザーの想定外テキストには現在質問が再送され、進行中でないユーザーには応答しない"
    - "followイベントで line_users に (oa_config_id, line_user_id) がUPSERTされる"
  artifacts:
    - path: "supabase/functions/webhook/index.ts"
      provides: "イベントルーティング + ステートマシン接続（postback/message/follow）"
      contains: "transition"
      min_lines: 120
    - path: "supabase/functions/tests/e2e_confirm_flow_test.ts"
      provides: "フルE2E（sender→postback×3→DB検証、E2E_TEST=1ゲート）"
    - path: "supabase/functions/_shared/providers/twipla.ts"
      provides: "IN-08軽量対応（query/hash付きURL拒否）"
  key_links:
    - from: "supabase/functions/webhook/index.ts"
      to: "supabase/functions/_shared/confirm/state.ts"
      via: "postback処理での transition() 呼び出し"
      pattern: "transition\\("
    - from: "supabase/functions/webhook/index.ts"
      to: "answers"
      via: "onConflict participant_id,question_key のUPSERT（answered_at明示更新）"
      pattern: "question_key"
    - from: "supabase/functions/webhook/index.ts"
      to: "supabase/functions/_shared/line/client.ts"
      via: "replyMessage（DB更新後に1回だけ呼ぶ）"
      pattern: "replyMessage"
    - from: "supabase/functions/webhook/index.ts"
      to: "line_users"
      via: "source.userId照合（なりすまし防止）と follow UPSERT"
      pattern: "line_user"
---

<objective>
webhook雛形を1問1答ステートマシンのオーケストレータに拡張し（postback検証→answers UPSERT→transition→reply、テキスト再誘導、follow記録）、IN-08の軽量修正を入れ、デプロイして「配信→3問回答→完了」のフルE2Eを署名付きcurlで機械検証する。

Purpose: LINE-02/LINE-03を完成させ、Phase 2のゴール「参加者側フローが手作業ゼロで完結」をE2Eで証明する（ROADMAP成功条件2・3・4）。
Output: 完成したwebhook + IN-08修正 + フルE2Eテスト + dev反映。実LINE受信確認はHUMAN-UATへ（D-14）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-line-bot/02-CONTEXT.md
@.planning/phases/02-line-bot/02-RESEARCH.md
@.planning/phases/02-line-bot/02-02-SUMMARY.md
@.planning/phases/02-line-bot/02-03-SUMMARY.md
@supabase/functions/webhook/index.ts
@supabase/functions/_shared/line/signature.ts

<interfaces>
依存契約（Wave 1/2で確定済み — 探索不要）:

From _shared/confirm/state.ts: `transition(current {status,index}, questions, input {questionId, optionIndex}) → {nextStatus, nextIndex, answer|null, reply: "next_question"|"completion"|"reprompt"|"none"}` / 型 ConfirmStatus, Question
From _shared/confirm/postback.ts: `decodePostbackData(data) → {participantId, questionId, optionIndex} | null`
From _shared/confirm/messages.ts: `buildQuestionMessage(q, participantId)` / `buildRepromptMessages(q, participantId)` / `buildCompletionMessages()`
From _shared/line/events.ts: `parseWebhookEvent(raw) → {kind:"postback"|"follow"|"message", replyToken?, userId, data?|text?, isRedelivery?} | null`
From _shared/line/client.ts: `replyMessage(token, replyToken, messages)`（LINE_DRY_RUN対応・≤5 assert）
From _shared/line/token.ts: `issueStatelessToken(channelId, channelSecret)`
From _shared/line/signature.ts: `validateLineSignature(rawBody, channelSecret, signature)`（既存 — 順序厳守ブロックは変更しない）

DBデータ経路（RESEARCH Pitfall 6 — 混同厳禁）:
- LINEの userId（"U…"）= line_users.line_user_id（text）
- participants.line_user_id = line_users.id への uuid FK
- webhookの2段引き: (oa_config_id, source.userId) → line_users.id → participants
- answers UPSERT: onConflict 'participant_id,question_key'、question_key=質問id、answered_at を毎回明示セット（トリガーなし — Pitfall 9）
- oa解決: oa_configs.line_channel_id = env LINE_CHANNEL_ID（setup-dev.tsが投入済み）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: webhookにステートマシンを接続（postback/message/follow）</name>
  <files>supabase/functions/webhook/index.ts</files>
  <read_first>supabase/functions/webhook/index.ts（現雛形 — raw body→署名→parseの順序を厳守したまま拡張）, .planning/phases/02-line-bot/02-RESEARCH.md の「System Architecture Diagram」「Pitfall 3/5/6/7」, supabase/functions/_shared/confirm/state.ts, supabase/functions/_shared/confirm/postback.ts</read_first>
  <action>
    既存の (1)raw body (2)署名検証 (3)zod形状検証 を一切変えず、(4) を実装する。全イベント処理後に必ず200を返す（LINEの再配達ストームを防ぐ — reply失敗もログのみで200）。

    共通準備: _shared/supabase.ts のservice roleクライアント。oa_config解決は `oa_configs` から line_channel_id = Deno.env.get("LINE_CHANNEL_ID") の1行を select（id, questions）。見つからなければ設定エラーとして500前に…ではなく、署名検証済みなのでログ+200（LINE側に再送させない）。questions は zodで {id,text,options[]}[] にパース。replyが必要になった時点で issueStatelessToken を遅延発行（イベントなしリクエストで無駄に発行しない）。

    各イベントを parseWebhookEvent で判別し独立に処理（1件の失敗が他イベントを巻き込まない try/catch）:

    - kind="follow"（D-15のIN-06修正を前提）: line_users へ upsert {oa_config_id, line_user_id: userId, display_name: null, followed_at: now} onConflict 'oa_config_id,line_user_id'。replyしない（挨拶はLINE OA Manager側の領分 — D-07）。

    - kind="postback": decodePostbackData(data) — nullなら無視。participants を id=participantId で select し、line_users をjoinして line_users.line_user_id と line_users.oa_config_id を取得。**なりすまし照合（T-02-11、必須）**: line_users.line_user_id === event.userId かつ line_users.oa_config_id === oaConfig.id でなければ「保存・遷移・replyを一切せず」警告ログ（participant_idのみ・userId非ログ）して次へ — postback dataは改変可能な入力（RESEARCH Pitfall 7。正規署名でも防げない）。照合通過後: transition({status: confirm_status, index: current_question_index}, questions, {questionId, optionIndex}) を呼び、
      (a) result.answer があれば answers へ upsert {participant_id, question_key: answer.questionId, question_text: answer.questionText, answer: answer.answer, answered_at: new Date().toISOString()} onConflict 'participant_id,question_key'（D-13: 再回答は上書き・スナップショット保存。answered_at明示 — Pitfall 9）。
      (b) 状態が変わる場合 participants を update（confirm_status=nextStatus, current_question_index=nextIndex）。**DB更新を先、reply送信を後**（Pitfall 3: reply失敗時はユーザーの次メッセージで再誘導が回復経路）。
      (c) result.reply に応じて1回だけ replyMessage: next_question → [buildQuestionMessage(questions[nextIndex], participantId)] / completion → buildCompletionMessages() / reprompt → buildRepromptMessages(questions[現在index], participantId) / none → 送らない。1イベントにつきreply呼び出しは最大1回（replyTokenは1回限り）。

    - kind="message"（テキスト等の想定外入力 — D-07）: (oaConfig.id, userId) → line_users.id を引き、その line_users.id に紐づく participants で confirm_status in ('sent','in_progress') の行を updated_at desc で1件取得（複数イベント並行時は最新を再送 — RESEARCH Open Question 1の推奨）。あれば buildRepromptMessages(questions[current_question_index], participant.id) を reply。なければ**何も返さない**（進行中でないユーザーへの自動応答はOA Manager側の領分）。messageイベントで answers / participants を書き換えない。

    ログ方針: userId・メッセージ本文・postback dataの生値をログしない。イベント種別・participant_id・遷移結果のみ。
  </action>
  <verify>
    <automated>deno check supabase/functions/webhook/index.ts && grep -q "transition(" supabase/functions/webhook/index.ts && grep -q "onConflict" supabase/functions/webhook/index.ts && grep -c "replyMessage" supabase/functions/webhook/index.ts | grep -qv "^0$" && deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 署名検証より前にビジネスロジックが実行されない（既存ブロックの順序不変）
    - なりすまし照合（line_user_id一致 + oa_config一致）が answers UPSERT・participants update・reply のすべてより前にある
    - answers upsert に answered_at の明示セットと onConflict 'participant_id,question_key' がある
    - DB更新（participants/answers）が replyMessage 呼び出しより前にある
    - messageイベント処理に participants/answers への書き込みがない
    - 常に200を返す（reply失敗・個別イベント例外でも）
  </acceptance_criteria>
  <done>webhookが1問1答のオーケストレータとして完成し、unitスイートがグリーンのまま</done>
</task>

<task type="auto">
  <name>Task 2: IN-08軽量対応 — twipla canHandle で query/hash 付きURLを拒否</name>
  <files>supabase/functions/_shared/providers/twipla.ts, supabase/functions/tests/twipla_parser_test.ts</files>
  <read_first>supabase/functions/_shared/providers/twipla.ts の canHandle 実装, .planning/phases/01-foundation-and-scraping/01-REVIEW.md の IN-08 項, .planning/phases/02-line-bot/02-RESEARCH.md の「IN-08（軽量対応）」</read_first>
  <action>
    IN-08（クエリ付きURLがスクレイプ成功するのにDB照合 eq("url") で不一致になり saved:false になる問題）の軽量対応として、RESEARCHが提示した2案のうち canHandle 側での拒否を採用: canHandle 内の既存チェック群に `parsed.search !== "" || parsed.hash !== ""` なら false を追加（保存される正規URLとscraper入力の表記を一致強制する。正規化関数の導入はPhase 3のURL登録UIで再検討 — ここではPhase 1確立のcanHandle許可リスト方式を維持）。コメントに IN-08 参照を残す。

    twipla_parser_test.ts に canHandle ケースを追加: 'https://twipla.jp/events/731057?utm_source=x' → false、'https://twipla.jp/events/731057#section' → false、既存の正規URL → true のまま。
  </action>
  <verify>
    <automated>deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/twipla_parser_test.ts</automated>
  </verify>
  <acceptance_criteria>
    - canHandle が query/hash 付きURLで false を返すテストがパスする
    - 既存の twipla_parser_test ケースが全て引き続きパスする（回 regression なし）
  </acceptance_criteria>
  <done>IN-08がPhase 1繰越のまま放置されず、scraperの照合不整合経路が遮断されている</done>
</task>

<task type="auto">
  <name>Task 3: デプロイ + フルE2E（sender→署名付きpostback×3→DB検証）+ フェーズ最終検証</name>
  <files>supabase/functions/tests/e2e_confirm_flow_test.ts</files>
  <read_first>.planning/phases/02-line-bot/02-RESEARCH.md の「Validation Architecture」「Pitfall 8」, supabase/functions/_shared/line/signature.ts（署名アルゴリズム — テスト側でHMAC-SHA256/base64を再現する）, supabase/functions/tests/e2e_targets_test.ts（02-03のE2E流儀・seedリセット手順）, supabase/seed.sql（固定UUID）</read_first>
  <action>
    1. `set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a`。linked ref＝cmsxvxtcdniqgvhxjqri を確認。LINE_DRY_RUN=1 が secrets に設定済みであることを確認（02-03で設定済み。なければset）。
    2. デプロイ: `supabase functions deploy webhook --project-ref "$DEV_PROJECT_REF" --use-api --no-verify-jwt --import-map supabase/functions/deno.json` と `supabase functions deploy scraper --project-ref "$DEV_PROJECT_REF" --use-api --import-map supabase/functions/deno.json`（Task 2のIN-08修正反映）。
    3. e2e_confirm_flow_test.ts を作成（E2E_TEST=1ゲート、scripts/db/sql.ts 再利用）。ヘルパー: LINE_CHANNEL_SECRET（env）でボディのHMAC-SHA256をbase64化し x-line-signature を付けて https://$DEV_PROJECT_REF.supabase.co/functions/v1/webhook へPOSTする sendSignedEvent()。postbackイベントは source.userId='U00000000000000000000000000000dev'（seed固定）、replyTokenはダミー、data は encodePostbackData で生成。シナリオ（CONTEXT specifics のE2E + 検証マップの全ケース）:
      (a) セットアップ: pooler SQLで seed participant（…0005）を pending/index0 にリセットし answers の該当行を削除
      (b) curl で message-sender 呼び出し → confirm_status='sent' をSQL assert
      (c) postback Q1(q_age, a=0) → answers 1行（question_text スナップショット非null）+ in_progress/index1
      (d) 再回答上書き: postback Q1(q_age, a=1) 再送 → answers 行数1のまま answer が更新され answered_at が前進、index1 のまま（LINE-03: UPSERT上書き）
      (e) なりすまし拒否: source.userId='U00000000000000000000000000attack' で Q2 のpostback → 200だが answers にQ2行が**ない**・状態不変（T-02-11）
      (f) 想定外テキスト: messageイベント（text）送信 → 200・状態/answers不変
      (g) postback Q2(q_drink) → index2、postback Q3(q_late) → confirm_status='completed' + answers 3行
      (h) 完了後postback → 200・answers/状態不変
      (i) 不正署名のpostback → 401（既存検証の継続確認）
      teardown: participant を pending に戻し answers を削除（再実行可能性）。
    4. 実行: `E2E_TEST=1 deno test --config supabase/functions/deno.json --allow-read --allow-net --allow-env supabase/functions/tests/e2e_confirm_flow_test.ts`。
    5. フェーズ最終検証を一括実行: unitスイート（--allow-read）全グリーン / `E2E_TEST=1` フルスイート / `deno run --allow-net --allow-read --allow-env scripts/verify-cron.ts` / シークレットgrep（`git ls-files | grep -E '^env\.(dev|prod)$'` 空、`git grep -lE 'eyJ[A-Za-z0-9_-]{20,}|hgojtooexbknqotzkkja' -- supabase/ scripts/` 空）。
    6. SUMMARYのHUMAN-UAT引き継ぎ事項を明記: `supabase secrets unset --project-ref "$DEV_PROJECT_REF" LINE_DRY_RUN` 後に実LINEで 友だち追加→手動紐付け（SQL）→配信受信→3問回答→完了 を確認する手順（D-14。実在しないseedのline_user_idには実配信されない点も記載）。
  </action>
  <verify>
    <automated>E2E_TEST=1 deno test --config supabase/functions/deno.json --allow-read --allow-net --allow-env supabase/functions/tests/e2e_confirm_flow_test.ts</automated>
    <human-check>HUMAN-UAT（フェーズ検証後・DRY_RUN解除して実LINEアカウントで受信〜完了まで確認）— 本プラン内では実施しない</human-check>
  </verify>
  <acceptance_criteria>
    - シナリオ(a)〜(i)がすべて自動でパスし、teardown後に再実行してもパスする
    - answers 3行すべてに question_text スナップショットと participant_id（…0005）が入っている
    - なりすましケースで answers が増えず confirm_status が変化しない
    - 不正署名は401、正規署名は200
    - verify-cron.ts とunitスイートがフェーズ最後の状態でグリーン
  </acceptance_criteria>
  <done>ROADMAP Phase 2成功条件1〜4がすべて機械検証済み（実LINE受信のみHUMAN-UATへ引き継ぎ）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LINE Platform / インターネット → webhook | verify_jwt=false の公開エンドポイント。署名検証が第一防壁 |
| LINEエンドユーザー → postback.data | 正規署名付きでも内容はユーザー改変可能 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Spoofing | postback処理（他人のparticipant_idでの回答なりすまし） | mitigate | source.userId ↔ participants→line_users.line_user_id の一致 + oa_config境界を保存・遷移・replyの前に必ず照合。E2Eケース(e)で機械検証 |
| T-02-12 | Spoofing | webhook偽装リクエスト | mitigate | x-line-signature検証（Phase 1実装・順序不変）。E2Eケース(i)で401を継続確認 |
| T-02-13 | Tampering | webhook再配達・Quick Reply再タップによる二重遷移/二重保存 | mitigate | answers UPSERT（unique制約）+ transition()のindex一致ガード。E2Eケース(d)(h)で検証 |
| T-02-14 | Repudiation/Information Disclosure | ログへのuserId・回答本文流出 | mitigate | イベント種別・participant_id・遷移結果のみログ。userId/本文/postback生値は非ログ |
| T-02-15 | Denial of Service | replyToken誤用（使い回し・期限切れ）による応答不能 | mitigate | 1イベント1 reply呼び出しに全バブル同梱。DB更新先行でreply失敗時もテキスト再誘導が回復経路 |
| T-02-SC | Tampering | npm installs | accept | 本プランの新規依存なし |
</threat_model>

<verification>
- `E2E_TEST=1 deno test --config supabase/functions/deno.json --allow-read --allow-net --allow-env supabase/functions/tests/` — フルスイートグリーン
- `deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/` — unitグリーン
- `deno run --allow-net --allow-read --allow-env scripts/verify-cron.ts` — cron基盤無傷
- シークレットgrep（env非コミット・キー文字列・prod ref）すべて空
</verification>

<success_criteria>
- Phase 2成功条件: (1) cron登録+抽出ロジック ✓（02-01/02-03） (2) 全状態遷移の自動テスト ✓（02-02 unit + 本プランE2E） (3) 完了メッセージ+completed遷移 ✓ (4) answers保存のwebhook→DB検証 ✓
- 実LINE受信確認のみHUMAN-UATとしてSUMMARYに引き継ぎ手順が記録されている
</success_criteria>

<output>
Create `.planning/phases/02-line-bot/02-04-SUMMARY.md` when done
</output>
