---
phase: 01-foundation-and-scraping
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - supabase/functions/_shared/providers/types.ts
  - supabase/functions/_shared/providers/registry.ts
  - supabase/functions/_shared/providers/twipla.ts
  - supabase/functions/_shared/supabase.ts
  - supabase/functions/scraper/index.ts
  - supabase/functions/tests/fixtures/twipla_event.html
  - supabase/functions/tests/twipla_parser_test.ts
  - supabase/functions/tests/twipla_live_test.ts
  - supabase/seed.sql
autonomous: true
requirements: [EVENT-02]

must_haves:
  truths:
    - "フィクスチャHTMLに対するパーサテストで、参加者のみが attending として抽出され、興味あり・不参加が混入しない"
    - "実Twipla URL（https://twipla.jp/events/731057）から参加者名・Xアカウント名が取得できる"
    - "scraper Edge Functionがdevにデプロイされ、登録済みURLの呼び出しで participants テーブルに行が保存される"
    - "scraperは twipla.jp 以外のURLを 400 で拒否する（SSRF防止）"
  artifacts:
    - path: "supabase/functions/_shared/providers/types.ts"
      provides: "ParticipantListProvider / ScrapedParticipant / ScrapeResult 型（プロバイダー抽象化 = EVENT-02の拡張性要件）"
      exports: ["ParticipantListProvider", "ScrapedParticipant", "ScrapeResult", "ParticipantStatus"]
    - path: "supabase/functions/_shared/providers/twipla.ts"
      provides: "parseTwiplaHtml純関数 + TwiplaProvider実装"
      exports: ["parseTwiplaHtml", "twiplaProvider"]
    - path: "supabase/functions/tests/fixtures/twipla_event.html"
      provides: "匿名化済みフィクスチャ（実名・実アカウント不含）"
    - path: "supabase/functions/scraper/index.ts"
      provides: "scraper Edge Function（registry経由・DB upsert）"
  key_links:
    - from: "supabase/functions/scraper/index.ts"
      to: "_shared/providers/registry.ts"
      via: "resolveProvider(url) — Twipla固有コードを関数本体に書かない"
      pattern: "resolveProvider"
    - from: "supabase/functions/_shared/providers/twipla.ts"
      to: "div.member_list セクション"
      via: "cheerio — セクション単位スコープで興味あり混入を防止"
      pattern: "member_list"
    - from: "supabase/functions/scraper/index.ts"
      to: "public.participants"
      via: "supabase-js upsert onConflict event_platform_url_id,display_name"
      pattern: "upsert"
---

<objective>
EVENT-02（プラットフォームURLからの参加者リスト自動取得）を、プロバイダー抽象化インターフェース + cheerioパーサ（純関数）+ scraper Edge Functionとして実装し、フィクスチャ自動テスト・実URL検証・devデプロイ後のDB保存までを機械検証する。Phase 1成功条件1の充足と、成功条件4のうちscraper分のデプロイ。

Purpose: プロジェクト最大リスク（Twiplaスクレイピング）の実証。将来のPeatix/ジモティー拡張に耐えるインターフェース確立。
Output: providers一式（types/registry/twipla）、匿名化フィクスチャ + テスト2本、scraper関数のdevデプロイ
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-scraping/01-RESEARCH.md
@supabase/functions/deno.json
</context>

<interfaces>
<!-- Plan 01-01が作成済みの前提。executorはこの契約に対して実装する。 -->

From supabase/functions/deno.json (Plan 01-01):
```json
{ "imports": { "cheerio": "npm:cheerio@1.2.0", "zod": "npm:zod@4.4.3", "@supabase/supabase-js": "npm:@supabase/supabase-js@2.108.1" } }
```

From dev DB schema (Plan 01-01 migrations):
- `event_platform_urls(id uuid pk, event_id uuid fk, platform text, url text unique)`
- `participants(id, event_platform_url_id fk, display_name, screen_name, profile_url, status check in ('attending','interested','declined','unknown'), source_platform, line_user_id nullable, confirm_status default 'pending', scraped_at, unique(event_platform_url_id, display_name))`

Twipla実HTML構造（RESEARCH.mdで実機検証済み・2026-06-12）:
- セクション: `div.float_left.member_list.round_border` × 3（先頭テキスト「参加者 (2人／定員15人)」「興味あり (2人)」「不参加 (0人)」）
- アンカー: `a.card.namelist` — `n`属性=表示名 / `s`属性=Xスクリーンネーム / `href`=/users/&lt;s&gt; / `title`=@&lt;s&gt;
- 落とし穴（実証済み）: `a.card.namelist` をページ全体に適用すると興味あり・不参加も全件ヒットする。必ず `div.member_list` 単位でスコープし先頭テキストでステータス判別する
</interfaces>

<critical_constraints>
- リポジトリはpublic。フィクスチャHTMLは実在参加者の表示名・Xアカウント名を含むため、コミット前に必ず匿名化する（構造・属性順は完全保持）。
- dev プロジェクト `cmsxvxtcdniqgvhxjqri` のみ。`--project-ref "$DEV_PROJECT_REF"` / `--db-url "$SUPABASE_DIRECT_CONNECTION_STRING"` を毎回明示。`supabase link` 禁止。
- テストは supabase/functions/tests/ に置く（関数フォルダ内に置くとデプロイバンドルに混入）。
- Twiplaへのアクセスは実URL検証時の必要最小限（テスト実行ごとに1リクエスト程度）。
</critical_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: プロバイダー型定義 + 匿名化フィクスチャ + パーサテスト（RED）</name>
  <files>supabase/functions/_shared/providers/types.ts, supabase/functions/tests/fixtures/twipla_event.html, supabase/functions/tests/twipla_parser_test.ts</files>
  <read_first>
    - supabase/functions/deno.json（imports確認 — Plan 01-01の成果物）
    - .planning/phases/01-foundation-and-scraping/01-RESEARCH.md の「Code Examples」（Twipla実HTML構造・検証済みパーサ核・Pattern 1の推奨シグネチャ）と「Common Pitfalls 1・7」
  </read_first>
  <behavior>
    - parseTwiplaHtml(fixtureHtml) は全セクションの参加者を status 付きで返す（attending / interested / declined）
    - attending のみをフィルタした件数が、フィクスチャの「参加者 (N人…)」ヘッダのNと一致する（興味あり・不参加の混入なし — 最重要ピットフォール）
    - 各 attending エントリの displayName は `n`属性値、screenName は `s`属性値、profileUrl は href と一致する
    - capacity がヘッダ「定員M人」のMとして数値で取れる（定員表記がないセクション構成でも null で壊れない）
    - 空の member_list セクション（不参加 0人）で例外を出さない
  </behavior>
  <action>
    **(1) 型定義（インターフェース契約を先に確定）:** _shared/providers/types.ts に RESEARCH.md Pattern 1 のシグネチャをそのまま定義する: `ParticipantStatus = "attending" | "interested" | "declined" | "unknown"`、`ScrapedParticipant { displayName: string; screenName: string | null; profileUrl: string | null; status: ParticipantStatus }`、`ScrapeResult { platform: string; sourceUrl: string; participants: ScrapedParticipant[]; capacity: number | null; fetchedAt: string }`、`ParticipantListProvider { readonly platform: string; canHandle(url: string): boolean; fetchParticipants(url: string): Promise<ScrapeResult> }`。

    **(2) フィクスチャ取得 + 匿名化:** `https://twipla.jp/events/731057` をfetchしてHTMLを取得（UA不要・UTF-8・200確認済み）。div.member_list 3セクションを含む形で保存する前に、全 `a.card.namelist` アンカーについて i 番目を: `n="テストユーザー{i}"` / `s="testuser{i}"` / `href="/users/testuser{i}"` / `title="@testuser{i}"` / アンカーテキスト=`テストユーザー{i}` に置換し、img の src / data-src を `https://example.com/avatar{i}.png` に置換する。セクションヘッダの人数表記（「参加者 (2人／定員15人)」等）と構造・属性順は保持する。supabase/functions/tests/fixtures/twipla_event.html に保存。**コミット対象に実名・実スクリーンネーム・twimg URLが残らないこと**（検証コマンドで機械確認）。

    **(3) パーサテスト（RED）:** tests/twipla_parser_test.ts を作成。`../_shared/providers/twipla.ts` から parseTwiplaHtml をimportし（この時点では未実装 — テストが先）、フィクスチャを Deno.readTextFileSync で読み込み、behavior欄の5項目をDeno標準 `@std/assert`（jsr:@std/assert）または素のassertで検証する。期待値（attending件数・名前等）はフィクスチャの実セクションヘッダと置換済みダミー値から具体的に書く。実行コマンドは `deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/twipla_parser_test.ts`。この時点ではimport先が存在せずFAILすることを確認する（RED確認）。
  </action>
  <verify>
    <automated>test -f supabase/functions/_shared/providers/types.ts && test -f supabase/functions/tests/fixtures/twipla_event.html && [ -z "$(grep -oE '(^| )s="[^"]*"' supabase/functions/tests/fixtures/twipla_event.html | grep -v testuser)" ] && [ "$(grep -c twimg supabase/functions/tests/fixtures/twipla_event.html)" -eq 0 ] && grep -c 'member_list' supabase/functions/tests/fixtures/twipla_event.html && ! deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/twipla_parser_test.ts 2>/dev/null && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - types.ts が ParticipantListProvider / ScrapedParticipant / ScrapeResult / ParticipantStatus をexportする
    - フィクスチャ内の全 `s` 属性値が `testuser` で始まり、`twimg` の出現回数が0（匿名化の機械検証）
    - フィクスチャに `member_list` セクションが3つ、`a.card.namelist` がオリジナルと同数存在する
    - パーサテストが「未実装のためFAIL」する（RED状態 — exit非0）
  </acceptance_criteria>
  <done>型契約・匿名化フィクスチャ・REDのパーサテストが揃い、匿名化が機械検証されている</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Twiplaパーサ + プロバイダー + レジストリ実装（GREEN）+ 実URL統合テスト</name>
  <files>supabase/functions/_shared/providers/twipla.ts, supabase/functions/_shared/providers/registry.ts, supabase/functions/tests/twipla_live_test.ts</files>
  <read_first>
    - supabase/functions/tests/twipla_parser_test.ts（Task 1の期待仕様 — これを通す実装を書く）
    - supabase/functions/_shared/providers/types.ts（実装すべき契約）
    - .planning/phases/01-foundation-and-scraping/01-RESEARCH.md の「Code Examples — Twiplaパーサ」（実機検証済みの核コード）と「Anti-Patterns」
  </read_first>
  <behavior>
    - Task 1のパーサテスト全項目がPASSする（GREEN）
    - twiplaProvider.canHandle は hostname が `twipla.jp` かつ pathname が `/events/数字` のURLのみ true（http/httpsのみ。それ以外のスキーム・ホストは false — SSRF防止の一次関門）
    - resolveProvider(url) は canHandle が true の最初のプロバイダーを返し、該当なしなら null
    - 実URL統合テスト: https://twipla.jp/events/731057 を fetchParticipants して participants.length &gt; 0、全要素の displayName が非空文字列、platform === "twipla"、fetchedAt がISO8601
  </behavior>
  <action>
    **twipla.ts:** RESEARCH.mdの検証済みコードを核に `parseTwiplaHtml(html: string, sourceUrl: string): ScrapeResult` を純関数で実装する。`$("div.member_list").each` でセクション走査し、先頭テキストノード（`$(section).contents().first().text().trim()`）の前方一致（参加者/興味あり/不参加）で status を判定、配下の `a.card.namelist` から `n`属性（フォールバック: text）・`s`属性・hrefを抽出。capacity はヘッダの `定員(\d+)人` 正規表現で抽出（全角／区切りに注意）、なければ null。`twiplaProvider: ParticipantListProvider` を export し、fetchParticipants は fetch（リダイレクト追跡は `redirect: "error"` で無効化 — SSRF対策）→ res.ok 検査 → parseTwiplaHtml。cheerio は deno.json の bare specifier `cheerio` でimport。

    **registry.ts:** `const providers: ParticipantListProvider[] = [twiplaProvider]` と `resolveProvider(url: string): ParticipantListProvider | null` を実装。URL解析は `new URL(url)` で行い、解析失敗は null を返す。

    **twipla_live_test.ts:** behavior欄の実URL統合テストを実装（`Deno.test` 名は "live: twipla event 731057"）。具体的な参加者名・件数の固定値アサートはしない（実イベントは変動するため形状と件数&gt;0のみ）。実行は `deno test --config supabase/functions/deno.json --allow-net=twipla.jp supabase/functions/tests/twipla_live_test.ts`。

    canHandle単体の境界テスト（twipla.jp以外のホスト・/events/以外のパス・ftpスキームが false）を twipla_parser_test.ts に追記してよい（同ファイル管轄のため）。
  </action>
  <verify>
    <automated>deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/twipla_parser_test.ts && deno test --config supabase/functions/deno.json --allow-net=twipla.jp supabase/functions/tests/twipla_live_test.ts && echo GREEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - パーサテスト（フィクスチャ）が全件PASS — attending のみ抽出・interested 非混入を含む
    - 実URLテストがPASS — participants.length &gt; 0・displayName 全件非空・platform === "twipla"
    - twipla.ts に `member_list` でのセクションスコープ処理が存在する（`grep -c member_list supabase/functions/_shared/providers/twipla.ts` ≥ 1）
    - registry.ts が `resolveProvider` をexportし、twipla.ts が `parseTwiplaHtml` と `twiplaProvider` をexportする
  </acceptance_criteria>
  <done>フィクスチャテスト・実URLテストが両方GREENで、Phase 1成功条件1（パース正当性 + 実URL取得 + プロバイダー抽象化）がローカルで充足</done>
</task>

<task type="auto">
  <name>Task 3: scraper Edge Function + seed + devデプロイ + DB保存検証</name>
  <files>supabase/functions/scraper/index.ts, supabase/functions/_shared/supabase.ts, supabase/seed.sql</files>
  <read_first>
    - supabase/functions/_shared/providers/registry.ts と types.ts（Task 2の成果物 — scraperはこの契約のみに依存）
    - supabase/migrations/ の create_core_tables マイグレーション（participants の unique 制約 = upsert の onConflict ターゲット確認）
    - .planning/phases/01-foundation-and-scraping/01-RESEARCH.md の「dev環境への非対話デプロイ・DB操作」「成功条件4の機械検証」セクション
  </read_first>
  <action>
    **_shared/supabase.ts:** `createServiceClient()` を実装 — `createClient(Deno.env.get("SUPABASE_URL")!, Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!)`（両変数はEdge Functionsに自動注入される。secrets setで上書きしないこと）。

    **scraper/index.ts:** `Deno.serve` でPOSTを受け、(1) `await req.json()` を zod で検証 — スキーマは `z.object({ url: z.url() })`（zod 4では z.string().url() でなく z.url()）。不正なら400。(2) `resolveProvider(url)` — null なら 400 で `{"error":"unsupported url"}`（hostname許可リスト = SSRF防止）。(3) `provider.fetchParticipants(url)` で ScrapeResult 取得。(4) service clientで `event_platform_urls` を `url` 完全一致で検索し、ヒットすれば participants へ upsert（行: event_platform_url_id, display_name=displayName, screen_name=screenName, profile_url=profileUrl, status, source_platform=result.platform, scraped_at=result.fetchedAt。`onConflict: "event_platform_url_id,display_name"`）して saved=true、なければ saved=false。(5) 200 で `{ platform, count: participants.length, saved }` を返す。参加者の生データ・シークレットをconsole.logしない（件数のみログ可）。Twipla固有のコードをこのファイルに書かない（registry経由のみ）。

    **seed.sql:** dev検証用の固定UUIDシード3行 — oa_configs（id '00000000-0000-0000-0000-000000000001', name 'dev-oa'）、events（id '...0002', oa_config_id '...0001', title 'dev-event'）、event_platform_urls（id '...0003', event_id '...0002', platform 'twipla', url 'https://twipla.jp/events/731057'）。

    **適用 + デプロイ + 検証:** `set -a; source env.dev; set +a` 後、(a) `supabase db reset --db-url "$SUPABASE_DIRECT_CONNECTION_STRING" --yes`（migrations + seed適用。devは使い捨てなので破壊的でよい）。(b) `supabase functions deploy scraper --project-ref "$DEV_PROJECT_REF" --use-api`（Docker不要）。(c) curlで実呼び出し: `https://${DEV_PROJECT_REF}.supabase.co/functions/v1/scraper` へ `Authorization: Bearer $SUPABASE_ANON_KEY` + body `{"url":"https://twipla.jp/events/731057"}` → 200 / saved:true / count&gt;0。(d) SSRF拒否: body `{"url":"https://example.com/"}` → 400。(e) psql（`$(brew --prefix libpq)/bin/psql` — Plan 01-01でインストール済み）で `select count(*) from participants` が curl応答の count と一致、かつ `status='attending'` の行が1件以上。
  </action>
  <verify>
    <automated>set -a; source env.dev; set +a; PSQL=$(command -v psql || echo "$(brew --prefix libpq)/bin/psql"); BASE="https://${DEV_PROJECT_REF}.supabase.co/functions/v1"; RES=$(curl -s -X POST "$BASE/scraper" -H "Authorization: Bearer $SUPABASE_ANON_KEY" -H "Content-Type: application/json" -d '{"url":"https://twipla.jp/events/731057"}'); echo "$RES" | grep -q '"saved":true' && CNT=$(echo "$RES" | grep -oE '"count":[0-9]+' | grep -oE '[0-9]+') && [ "$("$PSQL" "$SUPABASE_DIRECT_CONNECTION_STRING" -tAc "select count(*) from participants")" -eq "$CNT" ] && [ "$(curl -s -o /dev/null -w '%{http_code}' -X POST "$BASE/scraper" -H "Authorization: Bearer $SUPABASE_ANON_KEY" -H "Content-Type: application/json" -d '{"url":"https://example.com/"}')" = "400" ] && [ "$("$PSQL" "$SUPABASE_DIRECT_CONNECTION_STRING" -tAc "select count(*) from participants where status='attending'")" -ge 1 ] && supabase functions list --project-ref "$DEV_PROJECT_REF" | grep -q scraper && echo SCRAPER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase functions list --project-ref "$DEV_PROJECT_REF"` に scraper が表示される（成功条件4のscraper分）
    - 登録済みTwipla URLのcurl呼び出しが 200 + `"saved":true` + count&gt;0 を返す（呼び出しログが生成される）
    - twipla.jp以外のURLが 400 で拒否される（SSRF防止の動作検証）
    - dev DBの participants に status='attending' の行が1件以上存在し、count が応答値と一致する
    - scraper/index.ts に `resolveProvider` の呼び出しがあり、`twipla` という文字列リテラルが現れない（プロバイダー抽象化の維持）
  </acceptance_criteria>
  <done>scraperがdevで稼働し、実URL→パース→participants保存→SQL確認の一連がエンドツーエンドで機械検証された（EVENT-02実証）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| クライアント → scraper関数 | 任意URLを含むJSONボディが渡る（SSRFベクタ） |
| scraper → twipla.jp（外部Web） | 信頼できないHTMLを取り込む |
| リポジトリ(public) → 全世界 | フィクスチャHTMLに実在者の個人情報が混入しうる |
| scraper(service role) → Postgres | RLSバイパス権限での書き込み |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering (SSRF) | scraper/index.ts のurl入力 | mitigate | canHandle hostname許可リスト（twipla.jpのみ）+ fetch redirect:"error" + zod z.url()検証。400拒否をデプロイ後curlで実証 |
| T-01-06 | Information Disclosure | tests/fixtures/twipla_event.html | mitigate | 全n/s/href/title/text/imgをダミー置換。`s属性が全件testuser*` + `twimg出現0` をTask 1検証コマンドで機械確認 |
| T-01-07 | Tampering (SQLi) | participants書き込み | mitigate | supabase-jsのパラメタライズドAPI（upsert）のみ使用。動的SQL組み立てなし |
| T-01-08 | Information Disclosure | scraperのログ・レスポンス | mitigate | 参加者生データ・service roleキーをログしない（件数のみ）。レスポンスは platform/count/saved に限定 |
| T-01-09 | Denial of Service | twipla.jpへの過剰アクセス | accept | 手動/低頻度トリガー前提（月数回・1回1リクエスト）。リトライ実装なし（Phase 1）。低リスクと判断 |
| T-01-SC | Tampering | npm:cheerio等の依存 | mitigate | Plan 01-01のdeno.jsonでバージョン固定済み。全パッケージRESEARCH.md監査[OK]。新規依存追加なし |
</threat_model>

<verification>
- `deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/twipla_parser_test.ts` 全PASS（成功条件1前半）
- `deno test --config supabase/functions/deno.json --allow-net=twipla.jp supabase/functions/tests/twipla_live_test.ts` PASS（成功条件1後半）
- デプロイ後curl: 登録URL→200+saved:true、非許可ホスト→400
- dev DB participants に attending 行が存在（SQL確認）
- フィクスチャ匿名化の機械検証（testuser/twimoji grep）
</verification>

<success_criteria>
- Phase 1成功条件1: フィクスチャ自動テストPASS + 実URL取得確認 + プロバイダー抽象化経由 — すべて機械検証済み
- Phase 1成功条件4（部分）: scraperがdevにデプロイされ実呼び出しでログが生成される
- EVENT-02の中核実装が完成（管理画面トリガーはPhase 3で接続）
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-scraping/01-02-SUMMARY.md` when done
</output>
