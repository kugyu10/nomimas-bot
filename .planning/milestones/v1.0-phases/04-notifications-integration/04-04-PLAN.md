---
phase: 04-notifications-integration
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - admin/components/events/events-page-client.tsx
  - admin/components/events/event-form-dialog.tsx
  - admin/components/events/linking-tab.tsx
  - admin/components/events/events-table.tsx
  - admin/components/app-sidebar.tsx
  - admin/app/login/page.tsx
  - admin/app/(app)/events/loading.tsx
  - admin/app/(app)/events/[id]/loading.tsx
  - admin/app/(app)/oa/settings/loading.tsx
  - admin/components/oa-selector.tsx
autonomous: true
requirements: [NOTIF-01, OA-03]
must_haves:
  truths:
    - "CTA の文字列リテラルに二重プラスがない（Plus アイコンが + を表現 — top-3 #1）"
    - "ハードコード zinc accent（bg-zinc-900/hover:bg-zinc-800/hover:bg-zinc-700/border-zinc-900）が admin から消え、Button デフォルト variant / border-primary に統一されている（top-3 #3）"
    - "events / events/[id] / oa/settings の3ルートに Skeleton の loading.tsx があり、OAセレクタ切替中はスピナーが出る（top-3 #2）"
    - "URL 行のゴミ箱ボタンに aria-label、ログイン失敗文言は資格情報失敗のみ locked 文言、イベント一覧の死にステータス列が消えている（軽微 5a/5b/5c）"
    - "フェーズゲート: 成功条件1-4 の全自動コマンドが green（Deno 全件 / admin vitest / RLS_TEST / build / E2E 全鎖）"
  artifacts:
    - path: "admin/app/(app)/events/loading.tsx"
      provides: "イベント一覧スケルトン（UI-SPEC §3 構造）"
    - path: "admin/app/(app)/events/[id]/loading.tsx"
      provides: "イベント詳細スケルトン"
    - path: "admin/app/(app)/oa/settings/loading.tsx"
      provides: "OA設定スケルトン"
  key_links:
    - from: "admin/components/oa-selector.tsx"
      to: "router.refresh()"
      via: "useTransition で包み isPending 中 Loader2 表示"
      pattern: "useTransition"
---

<objective>
Phase 3 UI レビューの優先修正（top-3 + 軽微 a11y/コピー — 04-UI-SPEC §3〜§5 の確定ターゲット）を適用し、フェーズゲートとして成功条件1-4 の全自動検証を一括実行して Phase 4 を完成させる。

Purpose: CONTEXT「統合仕上げ」locked 決定の充足 + ROADMAP 成功条件4の最終ゲート。UI 修正は実コード照合済みの file:line ターゲット（RESEARCH Pattern 9）のみ — 残り11件の警告項目はスコープ外（Deferred）。
Output: UI 修正済み admin（build green）、フェーズゲート全 green。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-notifications-integration/04-UI-SPEC.md
@.planning/phases/04-notifications-integration/04-RESEARCH.md

前提: 04-02（webhook/scraper デプロイ + E2E 全鎖 green）と 04-03（テンプレート + root テスト green）が完了済み。本プランは admin の UI ファイルのみ変更し、supabase 側は実行のみ（ファイル不変）。

制約: dev のみ。env: `set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a`。Vercel デプロイ・実LINE受信・実ブラウザ確認は HUMAN-UAT（朝のTODO — スコープ外）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: CTA 二重プラス除去 + accent 統一 + a11y/コピー/死に列（top-3 #1/#3 + 軽微 5a/5b/5c）</name>
  <files>admin/components/events/events-page-client.tsx, admin/components/events/event-form-dialog.tsx, admin/components/events/linking-tab.tsx, admin/components/events/events-table.tsx, admin/components/app-sidebar.tsx, admin/app/login/page.tsx</files>
  <read_first>
    - .planning/phases/04-notifications-integration/04-UI-SPEC.md §4（二重プラス: 対象2箇所と Correct 形）/ Color「CTA cleanup」表（zinc 除去4箇所）/ §5a（ゴミ箱 aria-label — question-list-editor.tsx:174-183 の踏襲パターン全文）/ §5b（ログイン文言）/ §5c（ステータス列削除 — 列構成の確定形）
    - .planning/phases/04-notifications-integration/04-RESEARCH.md — Pattern 9（実測 file:line 表）/ Pitfall 9（login line 30 は OAuth 起動失敗 — locked 文言を当てない）
    - admin/app/login/page.tsx（エラーは useState ローカル — UI-SPEC の searchParams 前提と実装が異なる点に注意。実装構造を維持して文言のみ変更）
  </read_first>
  <action>
    Pattern 9 の実測ターゲットを修正（D: CONTEXT「統合仕上げ」locked）:
    1. events-page-client.tsx:41 の「+ イベントを作成」→「イベントを作成」、event-form-dialog.tsx:337 の「+URL追加」→「URL追加」（Plus アイコンは維持 — 文字列リテラルの "+" のみ除去）
    2. accent 統一: events-page-client.tsx:38 / event-form-dialog.tsx:374 の `bg-zinc-900 text-white hover:bg-zinc-800` と linking-tab.tsx:283 の `hover:bg-zinc-700` を className 除去で `variant="default"` に。app-sidebar.tsx:37 の `border-zinc-900 text-zinc-900 font-medium` → `border-primary text-primary`（**font-medium も除去** — タイポ契約は 400/600 のみ）
    3. 5a: event-form-dialog.tsx の URL 行ゴミ箱（lines 315-324 近傍）に `aria-label="URLを削除"` + Tooltip（question-list-editor.tsx:174-183 と同一パターン）
    4. 5b: login/page.tsx line 43（signInWithPassword 失敗）→「メールアドレスまたはパスワードが正しくありません」（locked）。line 30（signInWithOAuth 起動失敗）→「ログインに失敗しました。もう一度お試しください」（Pitfall 9 — locked 文言は資格情報失敗のみ。未認証リダイレクトの「ログインが必要です」は変更しない）
    5. 5c: events-table.tsx の全行 "—" のステータス列をヘッダー・セルとも削除（UI-SPEC auto 確定: 列構成は イベント名|開催日|参加者数|回答済み/総数|操作）
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/nomimas-bot/admin && ! grep -rn "bg-zinc-900\|hover:bg-zinc-800\|hover:bg-zinc-700\|border-zinc-900" components/ app/ && ! grep -rn "+ イベントを作成\|+URL追加" components/ && grep -q 'aria-label="URLを削除"' components/events/event-form-dialog.tsx && grep -q "メールアドレスまたはパスワードが正しくありません" app/login/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - zinc ハードコード4種の grep が admin/components・admin/app で 0 ヒット（exit 1 = 非存在）
    - 二重プラス文字列の grep が 0 ヒット、Plus アイコン import は残存
    - aria-label="URLを削除" と locked ログイン文言が grep で存在、「ログインが必要です」（未認証リダイレクト用）も残存
    - events-table.tsx に「ステータス」ヘッダーが存在しない: `! grep -n "ステータス" admin/components/events/events-table.tsx`
    - tsc --noEmit green
  </acceptance_criteria>
  <done>top-3 #1/#3 と軽微3点が UI-SPEC の確定形どおりに適用済み</done>
</task>

<task type="auto">
  <name>Task 2: loading.tsx ×3 + OAセレクタ切替フィードバック（top-3 #2）</name>
  <files>admin/app/(app)/events/loading.tsx, admin/app/(app)/events/[id]/loading.tsx, admin/app/(app)/oa/settings/loading.tsx, admin/components/oa-selector.tsx</files>
  <read_first>
    - .planning/phases/04-notifications-integration/04-UI-SPEC.md §3（3ファイルそれぞれのスケルトン構造仕様・wrapper 規約 `space-y-4 p-6`・aria 規約）/「OA セレクタ切替スピナー」（useTransition / Loader2 がシェブロンを置換 / text-muted-foreground / 非 disabled / aria-busy + 「OAを切替中...」）
    - admin/components/oa-selector.tsx（line 33 の router.refresh() — 現状 useTransition なし）
    - admin/components/ui/skeleton.tsx（Phase 3 インストール済み・未使用の Skeleton）
  </read_first>
  <action>
    UI-SPEC §3 のレイアウト指定どおりに3つの loading.tsx を新規作成（default export / `<div className="space-y-4 p-6">` wrapper / Skeleton のみでテキストなし / skeleton 要素に aria-hidden="true"・wrapper に aria-busy="true"）:
    - events: タイトル h-7 w-32 + CTA h-9 w-36 + テーブルヘッダー6列 h-4 + 行5本 h-10
    - events/[id]: タイトル h-7 w-48 + サブ h-4 w-24 + タブ3個 h-9 w-20 + 行4本 h-10
    - oa/settings: タイトル h-7 w-24 + カード3枚（h-48 / h-40 / h-64 rounded-lg）

    oa-selector.tsx: `React.useTransition` を導入し `router.refresh()` を startTransition で包む。isPending 中はトリガー内のシェブロンを `<Loader2 className="h-4 w-4 animate-spin" />`（text-muted-foreground）に置換、完了で復帰。トリガーは非 disabled のまま。aria-busy / aria-label「OAを切替中...」を isPending 中に付与
  </action>
  <verify>
    <automated>cd /Users/kugyu10/work/nomimas-bot/admin && ls "app/(app)/events/loading.tsx" "app/(app)/events/[id]/loading.tsx" "app/(app)/oa/settings/loading.tsx" && grep -q "useTransition" components/oa-selector.tsx && grep -rlq "Skeleton" "app/(app)/events/loading.tsx" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - loading.tsx 3ファイルが存在し、いずれも Skeleton を使い default export、テキストリテラルなし
    - oa-selector.tsx に useTransition + Loader2 + isPending 分岐が存在
    - tsc + `npm run build` green（ルートグループ (app) 配下で Next.js が loading を認識）
  </acceptance_criteria>
  <done>3ルートのローディングスケルトンと OA セレクタの切替フィードバックが UI-SPEC 契約どおり実装済み</done>
</task>

<task type="auto">
  <name>Task 3: フェーズゲート — 成功条件1-4 全自動検証の一括実行</name>
  <files>(変更ファイルなし — 検証のみ。失敗時の修正は該当ファイルへ最小差分)</files>
  <read_first>
    - .planning/phases/04-notifications-integration/04-VALIDATION.md（Full suite コマンド・Per-Task Verification Map）
    - .planning/ROADMAP.md Phase 4 Success Criteria（1-4 の文言）
  </read_first>
  <action>
    以下を順に実行し、すべて green であることを確認する（fail 時は原因を修正して再実行。スコープ外の新機能追加はしない）:
    1. `deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/` — Deno 全件（既存82+ + notify unit 3本）
    2. `cd /Users/kugyu10/work/nomimas-bot/admin && npx vitest run` — admin unit/integration（非RLS）
    3. `set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a; cd /Users/kugyu10/work/nomimas-bot/admin && RLS_TEST=1 npx vitest run` — RLS マトリクス（root 軸・テンプレート含む）
    4. `cd /Users/kugyu10/work/nomimas-bot/admin && npm run build`
    5. `set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a; E2E_TEST=1 LINE_DRY_RUN=1 deno test --allow-net --allow-read --allow-env --config supabase/functions/deno.json supabase/functions/tests/e2e_full_chain_test.ts` — 全鎖 E2E（成功条件1の窓内/窓外対比 + 成功条件4）
    6. UI grep ゲート（Task 1/2 の acceptance 再確認）

    成功条件との対応を SUMMARY に記録: 1=E2E (e)(f)+window unit / 2=template unit+RLS+data+UI build / 3=RLS root 軸 / 4=E2E 全鎖+data.test.ts。HUMAN-UAT 残項目（実LINE受信・Vercel デプロイ・実ブラウザのテンプレート操作）を SUMMARY の「朝のTODO」として明記する
  </action>
  <verify>
    <automated>set -a; source /Users/kugyu10/work/nomimas-bot/env.dev; set +a; deno test --config supabase/functions/deno.json --allow-read supabase/functions/tests/ && cd /Users/kugyu10/work/nomimas-bot/admin && npx vitest run && RLS_TEST=1 npx vitest run && npm run build && cd /Users/kugyu10/work/nomimas-bot && E2E_TEST=1 LINE_DRY_RUN=1 deno test --allow-net --allow-read --allow-env --config supabase/functions/deno.json supabase/functions/tests/e2e_full_chain_test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 上記5コマンドすべて exit 0
    - notification_logs ベースの成功条件1対比（窓内行あり / 窓外0行）assert が E2E 内で pass
    - RLS root 権限差対比（root=全OA / owner=自OAのみ）が pass
  </acceptance_criteria>
  <verify_human>
    <human-check>朝のTODO（HUMAN-UAT — 本プランでは実行しない）: ①LINE_DRY_RUN 解除後の実LINE通知受信 ②`cd admin && npx vercel`（要アカウント認証）③実ブラウザでテンプレート保存→別OA切替→適用</human-check>
  </verify_human>
  <done>成功条件1-4 の全自動コマンドが green で Phase 4 が /gsd:verify-work 可能な状態</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → login form | 失敗文言の変更のみ（認証ロジック不変） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Information Disclosure | ログイン失敗文言 | mitigate | 「メールアドレスまたはパスワードが正しくありません」は資格情報の存在を区別しない汎用文言（user enumeration 非助長）。OAuth 起動失敗も汎用文言 |
| T-04-16 | Tampering | UI 変更による既存フロー退行 | mitigate | 文言・className・列削除のみ（ロジック不変）。build + 全テストのフェーズゲートで退行検知 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージゼロ（shadcn add も不要 — UI-SPEC Registry Safety） |
</threat_model>

<verification>
- UI grep ゲート全項目（zinc 0 / 二重プラス 0 / aria-label 存在 / 文言 / ステータス列なし / loading×3 存在）
- フェーズゲート5コマンド green（成功条件1-4 のマシン検証）
</verification>

<success_criteria>
- top-3 + 軽微修正が UI-SPEC の確定ターゲットどおり適用され build green
- 成功条件1-4 が全自動コマンドで green（HUMAN-UAT 残項目は朝のTODOとして SUMMARY に記録）
</success_criteria>

<output>
Create `.planning/phases/04-notifications-integration/04-04-SUMMARY.md` when done
</output>
